# Generated by the Vite/Sierra build
web/dist/

# Runtime data — the database, the job queue and the audit trail are all
# created on first boot. `rm -rf db/basecamp.db* db/audit` resets to first-run.
db/basecamp.db
db/basecamp.db-*
db/basecamp-jobs.db
db/basecamp-jobs.db-*
db/audit/

# Generated by Sierra's route scanner — regenerated on every dev/build run.
# Checked in by the sierra example for reference; here it is derived output.
web/config/routes.js
