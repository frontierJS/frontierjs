# Issues — archive

**Closed items, moved out of `ISSUES.md` so the register stays the list of what
is live.** Nothing here is open. Ids are never reused and never renumbered, so a
`FJS-###` named anywhere in the repo resolves either there or here.

**When a row moves.** `ISSUES.md` § Closed keeps the most recent closures and is
trimmed when it passes ~40 rows. A count rule rather than an age rule, because
closures arrive in bursts — 37 of these were closed on one day, and a date rule
alone lets one busy day bury a month. Everything older comes here, copied
verbatim: a row is never rewritten or summarized on the way in.

**Why keep them at all.** The same reason § Closed exists: a stale document, an
old handoff, or a comment in code can name an id and imply it is still broken.
This is how that gets refuted with the evidence attached — what the defect was,
what changed, and how it was proven. It is also the record of what building each
package actually cost, which no single changelog carries.

Read it when tracing something specific, never cold. `ISSUES.md` is the file
that answers *what is wrong right now*.

248 rows · closed 2026-08-02 → 2026-08-16 · newest first.

| Id | Title | Closed | How |
| --- | --- | --- | --- |
| <a id="fjs-287"></a>FJS-287 | conduit — **`hooks: { onError }` is an Observer wearing the Hook name** | 2026-08-16 | The construction option is `observers:` and the type is `ConduitObservers`; every callback in it receives and cannot act, which is what `safe()` swallowing a throw already enforced. No alias and no deprecation path — pre-alpha, and the two in-tree callers (basecamp, the test suite) moved with it, which is how the rename was proven: basecamp's own typecheck went red on the excess property and green again after. **`management.hooks` keeps the word deliberately** — it IS Junction's hook pipeline and can refuse a call, so the two options now say two different things instead of one word covering both tiers. The rule from [FJS-D06](#fjs-d06) is stated where a new callback would be added: a new `on*` says which tier it is |
| FJS-132 | css — **a Popover needs two rules before the term is usable** | 2026-08-16 | `.popover-anchor` in [popovers.css](packages/css/src/components/popovers.css), mirroring `.tooltip-anchor` — a `<div>` where the tooltip's is a `<span>`, because a Popover is an `<article>` and a `<span>` cannot contain flow content: the same anatomy constraint pointing the other way. **The placement ships with it and is scoped to the anchor**, not written on `.popover`, so a popover placed by anchor positioning or a style attribute is untouched — opting into the anchor is what opts into the placement. The row's open question (a default offset on a term with four plausible edges) is answered *below the trigger, start-aligned, plus one modifier*: a popover in practice is a dropdown, the other three edges are what anchor positioning is for, and `.align-end` exists because it is the case the package's own demo needed — a trigger at the end of a bar. Named for what it does rather than reusing `.end`, which on Tooltip means the SIDE the attachment sits on. **The top-layer trap is enforced rather than documented**: the rules say `:not([popover])`, since an inset in the top layer resolves against the viewport. `demo/demo.css` is back to one rule and the guide's Popover page no longer hand-writes `position: relative` and an inline offset — both were consumers of the gap. Five geometry assertions in `components.spec.js` |
| FJS-158 | css — **a theme can retune the space ladder's SHAPE and it does nothing below the element it is written on** | 2026-08-16 | A rung is now `base × density` — `--space-sm: calc(var(--space-sm-base, 0.5rem) * var(--density))` — and the base is an ordinary inheriting property, so a theme sets `--space-2xl-base` on its own class and every descendant recomputes from what it inherited. Neither half resolves early, so neither hits the alias trap. Of the row's two options this is the first (derive the rung from a shape token) and not `@property`: registering the rungs would make them compute to lengths and inherit as such, which is the mechanism that breaks `.dense`. **The old behavior is pinned rather than removed** — a rung declared DIRECTLY on an ancestor still stops there, because the `*` rule is what makes `--density` follow a `.dense` region at all, and a test asserts it so nobody "simplifies" the base indirection away. press.css now sets a shape as well as a density: tight small rungs, generous large ones, which is exactly the editorial-vs-console difference the row described |
| FJS-159 | css — **the theme surface has no border width** | 2026-08-16 | `--border-width`, default 1px, read by 15 structural sites across surface, buttons, pills, fields, tables, frame, code, popovers, disclosure, bars, lists, facts, tabs and a11y. **The row's warning was the design**: a Field's border, a Card's hairline and a Table's divider are three decisions, so `--field-border-width` and `--table-border-width` exist and fall back to it — a heavy theme gets heavy fields and heavy dividers free and can pull either back. Two things the sweep is deliberately not. **Geometry does not scale**: a spinner ring, a tooltip arrow and a step marker's disc are drawn WITH `border` and are shapes, so widening them distorts rather than thickens — pinned by a test. And **one pair had to stay related**: the tab indicator is `calc(var(--border-width) + 1px)` and bleeds by the strip's own weight, because at 3px structural the old literal 2px underline draws a GAP in the line on the selected tab. `--table-border-width` also found the general rule — a token declared ON the component (`.table { --table-border-width: … }`) beats the same token set on an ancestor and is therefore unreachable from a theme, so it is read at the use site instead |
| FJS-160 | css — **the elevation tokens do not reach the Block tier** | 2026-08-16 | `--surface-shadow`, read by the surface lineage's base rule in [surface.css](packages/css/src/foundation/surface.css) and `none` by default — so the resting Block tier stays flat, which was always the right default and was not STATABLE. `.raised` keeps `--shadow-md` and is still the elevation ladder; a theme setting both gets the stamp at rest and the ladder on lift, which is asserted rather than assumed. press.css takes its own printing offset at rest now, closing the case the row described: the offset landed on every button and every dialog while the cards it was designed for stayed flat |
| FJS-161 | css — **the app frame has no background of its own** | 2026-08-16 | `--app-bg`, `--topbar-bg`, `--sidebar-bg` in [frame.css](packages/css/src/components/frame.css), `--dialog-bg` in dialogs.css and drawers.css, each a use-site fallback to the neutral it used to hardcode — a `:root` declaration would resolve once and inherit past every `.theme-*`. basecamp.css has its prototype's three dark surfaces back: T.sidebar (#0e1019) on the topbar and sidebar, T.modal (#12151f) on the modal, where its own header had recorded losing both to `--surface`. The Dialog half assigns `--surface-ground` and NOT `--surface-bg`, one step earlier in the fallback chain: `.outlined` and `.ghost` set `--surface-bg` in surface.css's base layer and dialogs.css is a later one, so the obvious spelling would have beaten both treatments on every dialog — silently, because a dialog with a background looks right. **A ground carries no ink, and the row's premise that this closes dark-shell/light-content is only half true**: `--sidebar-bg` alone leaves the nav labels reading the light ramp. That case needs no new mechanism — a theme is a class of inheriting tokens, so `<nav class="sidebar theme-dark">` inverts the whole ramp for the region, which frame.css now says and `theming.spec.js` pins. Grounds separate surfaces within one ramp; a theme class inverts one |
| FJS-069 | mesa — **the White Paper's header version was never updated** | 2026-08-16 | Closed as **the document does not exist**. `Mesa_White_Paper_v1_0.md` is not in the tree, tracked or untracked, and `git log --all --diff-filter=A` over every branch shows it was never added — so the row asked for an edit to a file no clone has ever held, and `mesa/PROJECT_STATE.md` § White Paper declared a version (v1.7) for it anyway. Both are gone. What the paper was for is covered by `packages/mesa/docs/VISION.md` (the language, numbered rules) and `website/mesa.html` (the public story), neither of which carries a hand-maintained version header. The one surviving mention is prose in `DECISIONS.md` 2026-08-06, which is a settled argument about the word *Signal* and is left verbatim — the archive's rule. **This is the class the register cannot see**: an id can name an artefact that lives outside the repo, and nothing fails when it stops existing |
| FJS-027 | css — **`.btn.outlined` fails WCAG AA in all six themes** | 2026-08-16 | **Closed wider than filed, because measuring the whole grid moved the diagnosis.** The row described untoned `.btn.outlined` — the accent on `--surface`, 1.99–4.40:1 — and proposed `.ghost`'s answer: neutral ink untoned, the raw tone where a tone class is present. Measured across 9 themes × (untoned + 7 tones), untoned was one column of eight: `.btn.outlined` was under AA on **34 of 72 pairs** (worst 1.19:1, `dark`/`secondary`), `.btn.link` on 30 and `.btn.ghost` on 24 — every ghost failure toned, which is exactly the half the proposed fix leaves painting `var(--bg-mix, …)` raw. **A tone is tuned as a fill behind white text and is a different colour when it IS the text**, which `code.css` had already ruled (`DECISIONS.md` 2026-08-08) under a code-only name. That window is now `--tone-l-min`/`--tone-l-max` and the derivation is `--tone-ink` in `tones.css` — the tone with its lightness clamped, hue and chroma untouched, declared on `*` like the tint ramp so it is guaranteed-invalid untoned and `var(--tone-ink, X)` states the untoned look. The three variants differ only in `X`. **Clamped rather than blended, re-measured on the new grid**: `--tint-ink`'s 55% toward `--ink` puts `sunset`/`warning` at 4.05:1, where the clamp's worst case is 6.02:1. `.outlined`'s border and the loading spinner take the same colour — a boundary at 1.99:1 is the variant not being drawn (WCAG 1.4.11). 30 new assertions in `contrast.spec.js` over 10 themes, including a hue no theme defines through both the light and the inverted window; `website/site.css`'s local override is deleted. Breaking on the token rename, so `@frontierjs/css` is v0.16. The sweep also found one pair under AA on the toned-BLOCK path, which is a ramp decision rather than this one: [FJS-288](#) |
| FJS-D20 | auth — **the developer-facing auth API** | 2026-08-16 | **Ruled and built.** The line: a request that ESTABLISHES a session is a raw route, because login cannot be gated by login; anything that can be refused for want of one is an ordinary service. So register / login / logout / password-reset / email-verify stay under `/auth/*`, `GET /auth/me` becomes `account.get('me')`, and the three things an app could not do at all become services — `sessions` (where else am I signed in, revoke one, revoke the rest), `api-keys` (issue, list, revoke — the raw key exists in the create response and nowhere else) and `account.changePassword`. Each is scoped to the CALLER: nothing takes a user id, and the ownership tests name somebody else's session and key id and assert they still work. **The open question was the wrong half.** There was no browser-facing auth API at all: `client.authenticate()` signed in and stored nothing, Sierra's `login(token)` stored a token and signed nobody in, and the two dogfood apps carried 87 and 201 lines of `session.js` to bridge that — differently. Neither had ever called `POST /auth/logout`, so a sign-out dropped the local token and left the session row valid until it expired, in both apps, for the life of the framework. `client.auth.*` is the wire half (on the client that holds the token, so it works with no UI framework), Sierra's reactive `session` + `ready` is the UI half, and `tokenStorage` gives the token ONE owner. Both apps' files are now 1 and ~120 lines, the second being workspace tenancy alone. Three corollaries, each measured: `account.me` publishes a LEVEL only when the app passes its own resolver (a default would be a second role→level mapping disagreeing with the one every request is graded by); a service-name collision is refused at boot naming the option (the registry is a Map — basecamp's own `api-keys` is a workspace's keys, and it turns auth's off by name); and a 401 now keeps the server's own sentence, which is most of what a hand-written sign-in page was. 45 new tests. Open by choice: OAuth, and a bus for many observers ([FJS-042](#closed) ruled single handlers) |
| FJS-233 | litestone — **`@derived(expr)`, a clock-dependent value that is still filterable and sortable** | 2026-08-15 | **Scoped by a ruling first**: the row contradicted itself across amendments — one half wanted the expression shipped so a Mesa component could recompute against the viewer's clock, the other described it as materialising a property. Ruled server-computed only, and the schema ships **a flag, not the expression**: `readOnly` + `x-litestone-kind: 'derived'`, plus `x-litestone-volatile: 'clock'` when the expression reads `now()` — the one fact a consumer cannot work out for itself, since the value goes stale with no write and no event. That avoids a third implementation of the expression language in the browser, beside `compileSql` and `evalJs`. **It rides `@from`'s seam**, built into the same map rather than beside it, which is why it reaches every read with no new plumbing — pipeline, `search()`, cursor, all three include shapes, `select`, `distinct`, `findManyAndCount`. No parameters: compiled once at startup, with `now()` emitting SQLite's own clock, which SQLite fixes per statement. `aggregate`/`groupBy` build their own SELECTs and needed the expression substituted where a column name goes — without it `MAX("urgency")` answered `'urgency'`, [FJS-202](#closed) arriving through a new field kind one day after it was closed. `auth()`/`check()` refused, naming [FJS-228](#closed) as the per-caller tier — which is also what keeps the expression static. **The declared type is checked against the branches**, the obligation [FJS-234](#closed) brought: disagreeing branches, a `Boolean` field producing an `Int`, and an enum field naming a non-member are schema errors. Inference is partial on purpose — unknown never fails |
| FJS-026 | repo — **nested directories are invisible to the `packages/*` glob**, so uninstalled, untested and unrunnable | 2026-08-15 | Closed on both halves. **Detection**: the `coverage` phase names a new one and cites this id — proved by planting `packages/mesa/probe-nested` and reading it back (*has a package.json and is not a workspace member — uninstalled and untested*), then removing it. **The four that remain are each deliberate and named** in `scripts/ci-allowances.json` § nonMembers, and there is no fifth — the glob was walked and the two lists agree exactly: `mesa/mesa-bench` pins its own vite toolchain for a benchmark CI cannot assert on, `oracle/mockup` and `orion/mockup/api-engine` are V2 under [FJS-D14](#fjs-d14), and `toolbelt/mockup/datetime` is the parked prototype the `/datetime` kit is being rebuilt from. The original defect was a package that slipped below the glob and was silently dead — `mesa-vite`, closed 2026-08-10 by deleting it and making it `@frontierjs/mesa/vite`. **What the deferral still costs is stated rather than hidden**: orion's engine carries 8.6k lines of vitest tests that nothing runs, accepted until core leaves alpha | 
| FJS-234 | litestone — **a ternary in the policy expression language** | 2026-08-15 | `cond ? a : b`, looser than ` \| \| ` and right-associative so `a ? x : b ? y : z` nests into the else — a four-value ladder with no CASE keyword. Both obligations in the row are met. **Both compilers**: `CASE WHEN … THEN … ELSE … END` in `compileSql` with params in emission order, `?:` in `evalJs`. The first draft landed in NEITHER — an insertion aimed at `evalJs` matched an identical `case 'now'` line in a helper added earlier the same day, so every create was allowed while the SQL half did all the work; found by running it. **And it is gradeable**: `verifyRowPolicies` had no case for a ternary in its value seeder, so every row fell on one side and the policy came back ungraded rather than passing — verified by swapping the branches in one half and watching the oracle fail. Two adjacent things fixed on the way: a parenthesised group is now an operand on BOTH sides of a comparison (only the left took one, so `ownerId == (open ? auth().id : auth().adminId)` was a parse error on the right and legal on the left — harmless while the language was predicate-only), and an ORDERING comparison is seeded on both sides of its literal (`level > 5` seeded `level = 5`, the excluded side, so admitted rows were whatever the factory happened to generate — a policy graded by luck). The branch type-checking the row asks for belongs to [FJS-233](#), which is what gives a branch a declared type to check against |
| FJS-070 | repo — **`drift-report.md` line numbers reflect the 2026-07-31 tree** | 2026-08-15 | Closed as **not reproducible**: the report cites no line numbers. Grepped all 383 lines for `file.ext:N`, `line N` and `LN` — the only `:N` in the file is the ratio `~0.85:1`. The audit named files and symbols instead, which is why it has not rotted against four months of tree. So the row warned about a hazard the document does not have, and the warning was itself the stale thing — repeated in `CLAUDE.md` § Read next, where a reader would meet it first. Both now say what is actually true and what actually matters: the report is dated, it is **not the register**, and a finding still open has an id here while a settled one has a ruling in `DECISIONS.md`. The eight rows this file inherited from it are already tracked separately, five of which were wrong when re-probed. **The document itself was retired later the same day** — its synthesis is `IDEAS/coherence-review.md` and the rest is gone (`DECISIONS.md` § Repo conventions) | 
| FJS-228 | litestone — **`@@scope(name, expr)`, a named predicate the browser can name** | 2026-08-15 | The policy compiler made explicit and opt-in rather than implicit and always-on, in the same expression language, asked for as `where: { $scope: 'overdue' }`. Built although `createClient({ scopes })` already chains, for the reason the reinstatement turned on: **a browser cannot invoke `db.task.overdue()`** — a client sends a `where` OBJECT over HTTP, so a JS-declared scope is server-only and `$scope` is the one spelling that travels. **Implemented by desugaring `$scope` into `$raw`** before the where is built rather than by adding a case to the where builder, which is what makes it compose for free: conjunction with ordinary keys, nesting under `AND`/`OR`/`NOT`, several names ANDing, and positional parameter order all keep the owner they already had. A disjunction goes INSIDE a scope, where both compilers see it. Invariant 8 at the site that invites breaking it — the value is caller-supplied and is a KEY looked up in the declared table, never interpolated; an unknown name is refused before any SQL exists, listing what the model declares, and `db.$scopes(accessor)` publishes that list as source text on every flavor of client, which is what `$checkWhere` validates against. Needed [FJS-227](#closed) first, since a scope multiplies the `now()` occurrences in one statement. **It also closed a hole `@@allow` had all along**: a policy naming a column the model does not have reached SQLite as `"nope" > 1`, read as a string CONSTANT, so the filter silently admitted or excluded every row — refused at startup now, for scopes and policies alike |
| FJS-230 | litestone — **`orderBy: { $raw: sql`…` }`, the hatch the sort side did not have** | 2026-08-15 | Everything monotonic in a stored column already sorted; *snoozed last regardless of due date* and a weighted score could not be said at all. The fragment is the whole ORDER BY tail, direction included, because a sort no builder can express usually needs several keys in an order only the caller knows, and it composes with ordinary keys in the position written. **A plain string is refused by name** — the `sql` tag's static text is the author's and its values are bound, so a bare string is exactly how a caller-supplied one would arrive, and accepting it would turn the hatch into an injection. Its params travel in their own array spliced in at the ORDER BY, not into the statement's as it is built: ORDER BY comes after the WHERE *and* after the row policy ANDed onto it, and positional binds make the order the correctness ([FJS-215](#closed) was the same lesson). Refused where it cannot mean anything — with a cursor, which encodes every sort key's value off the last row and an expression is not a column it can read back, and on `groupBy`, whose ORDER BY is over group keys and aggregates. `$checkOrderBy` passes it through, the standing `where`'s `$raw` has |
| FJS-057 | sierra — **`widget` was a declared target that did not build** | 2026-08-15 | `createSierraViteConfig` accepted it, returned a vite config, and its own comment said widget builds were *"handled by a separate build loop"* that did not exist — no discovery, no entry, no mount, and a `shadowDOM` CSS plugin keyed on `@unocss-placeholder` that nothing ran. Now: one `.mesa` in `src/Embeds/` (or a directory holding `index.mesa`, for a widget with parts) becomes one self-contained IIFE — component, Mesa runtime and CSS in one file a host page loads with a single `<script src>`. It mounts in a shadow root through a **custom element**, with a CSS **selector** as the second spelling, because host markup often lives where its author cannot edit it; a MutationObserver covers an element that arrives after the script. Props are `data-*`. `sierra widgets` runs the loop: N widgets is N library builds, since a self-contained IIFE is precisely what a bundler's shared chunks are not, and the cost — the runtime once per widget — buys the property the target exists for. **Modeled on a real production embed system** (`my.maid.tech/embeds`, eight widgets, class-scanned and shadow-mounted) rather than invented. Three defects on the way, all now asserted: the entry passed the CSS placeholder through a comparison the bundler folded away, so every widget shipped unstyled; the runtime held the whole placeholder and, being bundled in, had it replaced too, so the widget compared its stylesheet against itself and dropped it; and discovery was nearly a glob, which would ship a form's four components as four half-widgets. `tests/fixtures/widget-site/verify.mjs` found the first two — 21 assertions in real Chrome against a plain host page with hostile CSS, negative-controlled (removing the observer fails exactly its four). sierra 939 unit tests + `bun run test:widgets` |
| FJS-059 (event names) | **jetty spoke the in-process BUS vocabulary on the WIRE** | 2026-08-15 | It subscribed to four composed names — `posts:created`, `:patched`, `:updated`, `:removed` — wrong twice over: a colon is the bus spelling and the wire carries a space, and **a channel is not an event** (you join `posts` and receive `posts created`), so there was no channel per event for any of the four to match. One subscription now, to the channel, and the event decides; `wireEventMethod()` splits it the way Junction's own client does and answers null rather than guessing. **The event name is now carried the whole way** — adapter → `fanOut` → `channel:event` → `meta.event` — which it was not, so even a correct subscription could not have told a create from a remove. The OLD TEST PINNED THE BUG: it asserted four subscriptions to names the server never publishes, and passed; the new one reads `AUTO_EVENT_MAP` out of Junction's source rather than restating it. Uncovered [FJS-279](#s3--medium). The `src/resources/` duplication half of FJS-059 stays open |
| FJS-278 | litestone — **four write paths gave four answers to one question, and two were silent** | 2026-08-15 | Found probing [FJS-204](#closed) rather than filed: against a soft-deleted row holding a `@unique` value, `create` and `createMany` threw SQLite's raw message, **`upsert` returned `null` having written nothing**, and **`upsertMany` wrote the update INTO the soft-deleted row and reported `{count: 1}`** — a write landing where no read returns it, with `deletedAt` never cleared, so it was invisible for good and the caller was told it succeeded. Two different causes: `upsert`'s race-recovery fallback assumes a UNIQUE conflict means a LIVE row appeared between its `findFirst` and its insert, so it retried as an `update` that filtered the deleted row and matched nothing; `upsertMany` is `ON CONFLICT DO UPDATE`, resolved by SQLite, which has never heard of soft delete. All four answer `SoftDeletedUniqueError` now — the upsert fallback re-throws rather than swallowing it (an upsert may not resurrect a row nobody asked it to), and `upsertMany` asks before the statement runs, since afterwards the write has already happened. The race the fallback exists for is pinned by its own test |
| FJS-204 | litestone — **a soft-deleted row kept its `@unique` slot and said so in SQLite's words** | 2026-08-15 | **Ruled 2026-08-15: it keeps the slot**, recorded in [DECISIONS.md](DECISIONS.md) § Query & write semantics. The alternative — a partial unique index freeing the slot — was rejected because it makes `@unique` FALSE for any read that includes deleted rows: `findUnique(withDeleted: true)` would legitimately match two, and every export, audit query and migration reading with deleted rows would see duplicates on a column declared unique. It also makes `restore()` conditionally impossible, which is soft delete's whole contract. So no DDL changed, and the defect was never the behavior: it was `UNIQUE constraint failed: doc.code` against a table `count()` says is empty, which sends every first diagnosis to the index. `SoftDeletedUniqueError` (409, not retryable) names the field, the value and the holding row's id, and names both ways to release a slot deliberately — `update({ …, withDeleted: true })` to move the value, `delete({ …, withDeleted: true })` to stop keeping the row. Composite `@@unique` names both columns. An ORDINARY conflict is untouched: the re-read only runs on the failing path and only reinterprets a conflict a DELETED row caused. Sibling: [FJS-278](#closed) |
| FJS-042 | **`@frontierjs/auth` emitted no events at all** | 2026-08-15 | Split first: the original claim was wrong — `Session`/`User` carry `@@log(audit)`, so a sign-in WAS recorded, anonymously ([FJS-276](#closed)), and a failed one was not recorded at all ([FJS-277](#closed)). What remained was the actionable half. **Ruled: callback options on `createLitestoneAuth`** — `onLogin`, `onLoginFailed`, `onLogout`, `onRegister`, the same shape `onPasswordResetRequested` already had, so no new vocabulary and identical with or without Junction (`createLitestoneAuth` works standalone, so anything on `app.events` would exist in one configuration and not the other). **Awaited, and a throw refuses**, which is what puts lockout at the auth layer rather than only in front of the route. One ordering rule: **a hook runs before the thing it can refuse**, so none is handed what its refusal would have prevented — the hook is the gate, `db.$audit` is the record. `login.failed` is written before `onLoginFailed` runs, so swapping the 401 for a 429 cannot erase the attempt. **Left open deliberately**: a bus for observers with many listeners, which is a different question |
| FJS-205 | litestone — **the policy language had no membership operator, so a list-valued column could not express tenancy** | 2026-08-15 | `in`, ruled 2026-08-15, with **the list always the RIGHT operand** — which is what makes one operator enough for all three shapes rather than two facing opposite ways: `auth().id in memberIds` (the row holds the list), `ownerId in auth().ownedIds` (the principal holds it), `status in ['draft', 'review']` (written literally, retiring the ORed-comparisons spelling). `contains` was out before the choice — it already means string `LIKE` in the `where` language. An array column compiles to the `json_each` EXISTS that `where: { col: { has } }` already produced, so membership has one definition; the other two compile to `IN (?, …)`, and an empty list is answered before any SQL is built, since `IN ()` is a syntax error. **In both compilers**, because a form in one is [FJS-195](#closed) repeating. **And `verifyRowPolicies` could not see it when it arrived** — its seeder took values off the predicate, so it put the principal's scalar id into an `Int[]` column, the insert failed, and every run reported one row all on the excluded side: not a wrong grade, no grade. It seeds `[value]` for an array column now, and for a scalar column a member of the list plus a numeric miss, since the string sentinel is refused by an `Int` and `null` by a required column — before which the excluded side existed by luck. Verified by inverting `evalJs` by hand and watching the oracle catch it. What the schema can decide is refused at **startup**, naming the model and quoting the expression back, because a wrong policy is an empty screen with a 200: a right operand that is not an array field, an array on the left (list-vs-list overlap is a different question, still unexpressible), both sides naming a column on the same row, and an `@encrypted`/`@hashed` list column. The parse error names the operator and lists what is legal — was `Expected RPAREN, got 'in'`. `policyExprToString` moved from `access.js` into `core/policy.js` on the way, because the startup check quotes expressions and `access.js` is by its own header never imported by production code |
| FJS-035 | repo — **no tsconfig in `email-kit`, `ui` and `toolbelt`** | 2026-08-15 | Each gets the same shape litestone, sierra and mesa already use: extend `tsconfig.base.json`, `checkJs: false`, an explicit `include`. That is the whole fix — editors get import resolution and go-to-definition, any `.ts` added later is checked for real, and `bun run typecheck` reports clean in all three, so no baseline moves and Invariant 14's ratchet keeps its floor at 0. **The measured cost of `checkJs: true` is in each comment rather than left to be rediscovered**: email-kit 33, toolbelt 95, ui 615 — 84% of ui's and almost all of email-kit's are implicit `any` on untyped parameters, and nearly all of toolbelt's are inside `glow`, which was adopted rather than written here. The two counted as README-only packages in the old row are `oracle` and `orion`, still [FJS-D14](#fjs-d14)'s | 
| FJS-083 | sierra — **URL search params were not reactive, not on `page`, and conflated with the path captures where they were** | 2026-08-15 | `page.query` (the filters, coerced) and `page.directives` (`{limit, offset, orderBy, select, …}`), assigned on every navigation, so a filtered, sorted, paged list is `resource.load(page.query, page.directives)` and lives in its URL — surviving a reload, a back button and a pasted link. The split is the API realm's own and is made **over the same table**: `@frontierjs/toolbelt/directives` is now the one owner of the `$` convention, read by junction's bridge off a request and by sierra's router off a search string, which is what Invariant 10 asks for at a second boundary. It went to toolbelt for `/inflect`'s reason — the `$` names were already written in four places inside junction alone, and a key one reader does not know about does not fail, it becomes a WHERE clause on a column nobody declared. The write direction stays where it is (the client builds `$` names from a typed `FindParams` on two paths that share nothing) and junction's suite now asserts every name either path emits is one the table strips, negative-controlled. **Breaking: `page.params` is PATH captures alone** — the search params used to be merged in, so `?id=99` on `/orders/7/` answered 99 to `page.params.id`; nothing in the repo read a query param off it. Both names joined `PAGE_RESERVED`, so frontmatter claiming one is warned about rather than silently overwritten, and they are reassigned only when the search actually changed, because a layout outlives a navigation and a filter bar watching `page.query` would otherwise re-ask the server on every navigation under it. 11 tests in `tests/page-query.test.js`, 13 in toolbelt, 2 in junction; sierra 930 pass, junction 1086, toolbelt 40 |
| FJS-058 | sierra — **undeclared runtime dependencies** | 2026-08-15 | Closed as no longer true, verified rather than assumed. The row read *declares exactly one runtime dep (`js-yaml`), and `src/` imports two more*; `package.json` now declares `@frontierjs/mesa`, `@frontierjs/junction`, `@frontierjs/litestone` and `vite` as **peers**, which is the right shape for a framework package — the app supplies them — and `exports.snapshot.md` commits that list, so a range that drifts is a diff. The substance of the row was *an installed copy would not resolve these*, and two things answer it: `fli new` writes junction, litestone, mesa and sierra into the app's own `package.json` ([project/new.md](packages/cli/commands/project/new.md#L152)), and CI's `scaffold` phase installs such an app from tarballs and BUILDS it, which runs sierra's mesa and junction imports for real. Nothing was done to close this; it was fixed on the way past by the work that made a scaffolded app install at all ([FJS-251](#closed)) |
| FJS-270 | junction — **a live list ignored `orderBy` and `limit`, so a pushed row was in the query and in the wrong place in it** | 2026-08-15 | `Store.upsert` appended: a list loaded `orderBy: '-createdAt'` received new rows at the bottom and one loaded `limit: 20` grew past 20. `Store.place(record, idField, cmp, max)` inserts at the sorted position and MOVES a row whose sort key a patch changed, in one notification. **The comparator reads `orderBy` the way the server does** — `core/sort.ts` is `parseSort`, so `-createdAt` has one meaning, and `compareValues` follows SQLite rather than JavaScript: NULLs first ascending (last descending), numbers before text, a Boolean as the 0/1 it is stored as, ISO-8601 DateTime as text. Where it cannot be exact it says so: a non-BINARY collation, and UTF-16 code units versus bytes past ASCII. **Paging is unanswerable and is not guessed at**: on the first page the overflow row genuinely belongs to page 2, so trimming is right; past page 1 a new row is REFUSED, because nothing in a browser knows whether it belongs on an earlier page, and a removal that leaves a gap only the server can fill is counted the same way. That count is `stale` on the resource — `{ get, subscribe }`, the same shape as a store, so it bridges to a UI signal unchanged — cleared by `load()`, for a view to render as *3 new — refresh*. An unordered list that outgrows its page counts rather than dropping a row at random: throwing away the row the user just created to honor a page size is the worse half of the trade. 25 tests in `tests/live-order.test.ts`; junction 1084 pass, sierra 919, typecheck unmoved, `example`: `verify:live` 14/14 |
| FJS-276 | **An audit row for a sign-in named no actor** | 2026-08-15 | Ruled: litestone gains `db.$audit()` rather than auth writing to the log model directly — that model is an ordinary accessor, and two writers with no shared definition is how a second `operation` vocabulary starts drifting. It takes an `actorId` (defaulting to the calling client's principal, which a system context does not have), refuses unknown keys by name, lets a stated value beat `onLog`'s, and **throws** where `@@log(audit)` is fire-and-forget — there the record is a side effect of a write that already succeeded, here it is the point. Auth records `login.succeeded` beside the `create:session` row: that one records the WRITE and cannot name the actor, this one records the EVENT and does |
| FJS-277 | **A failed login recorded nothing at all** | 2026-08-15 | A failed attempt performs no write, so `@@log(audit)` could never see it. Auth records `login.failed` through `db.$audit` with the reason and the attempted address — a spray across many addresses is invisible without it, and it is the only identifier an attempt on an unknown user has; the attempted password appears nowhere and a test asserts that. Every refusal still answers the same error, so the three branches are distinguishable in the trail and nowhere else. Also `logout`. Two softenings on the login path, both tested: an app need not declare a logger database, and a failed audit write is reported rather than failing the request |
| FJS-273 | litestone — **`aggregate` and `groupBy` read columns `read()` strips, and `_stringAgg` over one is not an aggregate but a dump** | 2026-08-15 | Found asking the second half of [FJS-202](#closed)'s question — if a name that is not a column answers a constant, what does a name the caller may not READ answer? `applyFieldPolicyTo` is the one owner of *may this caller see this field*, and it answers per ROW; an aggregate builds no row, so nothing asked. Measured against a `@@gate`d model as an ordinary signed-in caller: `_max` over a `@guarded` salary answered 900, `_sum` answered the total, `_max` over `@omit(all)` answered it, `@encrypted` answered ciphertext — and the two that are not aggregates at all, `_stringAgg: { field: 'salary' }` answering `"100.0,900.0"` and `groupBy: { by: ['salary'] }` answering every distinct value with a count, which is the whole column either way. Same family as [FJS-223](#closed), where a `@from` field returned `@guarded(all)` and `@encrypted` values: **protections live in `read()`, and anything that assembles an answer without it has to ask by hand.** Fixed with the guard [FJS-202](#closed) needed, one tier further: `fieldReadRefusal` mirrors `applyFieldPolicyTo`'s strip ladder over a NAME, and a field-level `@allow('read', …)` is refused rather than guessed at, because it is a predicate over a row and an aggregate has no row. `asSystem()` still reads everything it may. 8 arguments × 4 field kinds, executed as a grid — a new argument that can carry a field name is added to the table |
| FJS-255 | litestone — **the three lock errors reached a caller as 500** | 2026-08-15 | All three now declare `status = 409`, beside the transition and version classes that already did. `retryable` stays and is the half a status cannot carry: `LockNotAcquiredError` is 409 + retryable (someone holds it, and `expiresAt` says for how long), the other two are 409 + not (the work is done under a lock that is no longer yours, and repeating the release cannot make it true again). 503 was the alternative for the first and was not taken — it says back off from the SERVER, and the caller has to back off from one key. `errors.snapshot.md` flipped the three rows on regeneration, which is what the file exists for; junction's test asserted the 500 and is promoted, plus a new assertion that **no** Litestone class in that table may land on 500, since every one of them is a class this repo owns |
| FJS-202 | litestone — **an aggregate over a name that is not a column answered a constant** | 2026-08-15 | Wider than filed. It was not only `@computed`: `@from`, a relation and a plain **typo** did the same, because SQLite reads an unresolvable `"name"` as a string literal — `_max` answered the name, `_sum` answered 0, and `groupBy: { by: [...] }` refused only by accident, with a raw `no such column: order.label` naming a table rather than the model. Eight arguments reach SQLite inside a quoted identifier and every one was unchecked: `_min`/`_max`/`_sum`/`_avg`, `_stringAgg.field`, `_stringAgg.orderBy`, a named aggregate's field, `_count: { distinct }`, plus `groupBy`'s `by` and `interval`. One guard now, two tiers — **naming** (a real column, which `by` and `distinct` take, because grouping stored text is self-consistent) and **value** (also refuses the opaque bucket of [FJS-200](#), since MAX over a JSON array orders `'[10]'` below `'[9]'` and SUM answers 0). The crossing matrix promoted 8 cells; `encrypted × aggMax` and `encDet × aggMax` moved from `ok` to `ref`, which is the grid catching a behavior change nobody would have thought to assert. Sibling of [FJS-273](#closed) |
| FJS-272 | **Seven defects in published junction, each one only a type error until somebody read it** | 2026-08-15 | Found by driving junction's consumer surface to zero for `FJS-268`. In severity order. **(1) STARTTLS was broken against every submission server.** `src/mail/smtp.ts` called `socket.startTls(...)`; probed on Bun 1.3.11 that property is `undefined` and the real API is `upgradeTLS`, which takes the handlers again and returns a PAIR. So any host advertising STARTTLS — Gmail, SendGrid, Postmark, Office 365, anything on port 587 — died with `socket.startTls is not a function`. Unseen because the only mail server the drives talk to is the dev sink, and a sink advertises no capabilities. **(2) `timestamps()` silently skipped every row of a bulk create**: `ctx.data` is a row OR an array, and un-narrowed the hook set `created_at`/`updated_at` as properties of the ARRAY. `allow()` had the same shape and filtered nothing on a bulk payload. **(3) An error's stack never reset the terminal colour** — `line += '\n' + COLOR + stack ?? fallback + RESET` parses as `(concat) ?? (...)`, so the fallback was dead and `RESET` was on the wrong side: an error with no `.stack` logged the word `undefined`, and every line after ANY error stayed red. **(4) `plugins/email/campaign/sender.ts` imported `App` from `../../core/app.ts`**, one `../` short — a type-only import, so it never broke at runtime, and every type in that file was dead. Fixing it revealed the file reaching `app.conduit.resolve` with no guard, which is a `Cannot read properties of undefined` the first time campaign email is configured without conduit. **(5) The OpenAPI generator could not express a header parameter**, so the `X-Service-Method` line — the entire custom-action calling convention — was an excess property the spec dropped. Path-level `parameters` were untypeable for the same reason. **(6) `RateLimitHookOptions` was exported twice from `index.ts`**, a duplicate identifier; `RouteSegment` was imported from `router.ts`, which does not export it. **(7) junction's `tsconfig.json` was a stale COPY of `tsconfig.base.json`** with `DOM.Iterable` missing from `lib`, so `headers.entries()` was an error against working code — the exact case the base's own comment warns about. It extends the base now. Also: `HookType` never admitted `'method'`, which `runPipeline` sets while the method runs. Nine `as Record<string, unknown>` casts were reaching fields the types already declared, and twelve more were an undeclared middleware→transport side channel (`__cors`, `__securityHeaders`, `__rateLimit`, `__correlationHeaders`, `__pendingCookies`, `__status`) now written down on `TransportContext`, so a middleware that misspells a bucket no longer drops its headers in silence. `tests/smtp-starttls.test.ts` pins (1), out of process — `tests/email.test.ts` mocks the smtp shim process-wide, so an in-process version graded the mock and passed. Junction 1059 pass; `example` verify 37, verify:jobs 8, verify:live 14, verify:notify 9 |
| FJS-041 | sierra — **a prefetched route fetched signed-out, and the refusal was cached and then served** | 2026-08-15 | `runPrefetch` handed `load()` `window.fetch`; the router hands a navigated `load()` `sierraFetch`. Two fetch paths, so they disagreed, and the answer to the wrong one was cached: hovering a link could make the page you then visited render as signed-out. It is now `sierraFetch` in both, which was always available — the deferral said the token was not reachable at module init, and the token is read per CALL from localStorage, so there was never a cycle to avoid. **The token alone is half of it**: a payload answers *what may this person see*, so `invalidatePrefetch()` drops every cached payload and the per-URL gate with it (or a URL could never be prefetched again this session) on `login()`, on `logout()` and on the client's mid-session `unauthorized`. Component chunks are kept — a route's JavaScript is the same file whoever asks, and dropping it would throw away the half of prefetch that was never wrong. Six tests in `tests/prefetch-auth.test.js`, negative-controlled (three fail against the old line); sierra 918 pass, `example`: `verify` 37/37. **No app in this repo prefetches a protected route**, so nothing here drives it in a browser |
| FJS-011 | **Live-store filter leak** — a store held rows its own query excluded | 2026-08-15 | `matchesQuery(fields, record, query)` in sierra's `field-rules.js`, passed to junction's `resource(name, idField, { match })`. `true` upserts, `false` REMOVES — a patch is how a row leaves a list and there is no removal event for that, which was the half that read as an update rather than as junk. The operators are exactly what `parseWhere`/`translateOps` accept and `buildWhere` compiles, `$`-prefixed and bare alike, with SQL's null semantics rather than JavaScript's (`$ne` misses a NULL column, `$nin` matches it). **Three answers**: `null` is *cannot be decided from this record* — a `select` that dropped the filtered column, a filter over a relation, `$search`, `$raw` — and the store reloads once per burst instead of guessing, because a matcher forced to return a boolean guesses silently. A decided `false` still wins, so a miss costs no request. The query is recorded with the rows inside the `FJS-082` stamp check. Junction holds no schema, so the decision is passed in and a caller passing none gets the old behavior exactly. 32 tests in sierra, 8 in junction; `example`: `verify` 37/37 and `verify:live`. Ordering and paging are the part no client matcher can answer and are now [FJS-270](#s3--medium) |
| FJS-265 | **A `.lite` import was a path, so a package could not ship a schema fragment** | 2026-08-15 | `import "@frontierjs/auth/schema.lite"` looked for `db/@frontierjs/auth/schema.lite`, so the only way to use a package's models was to copy them and an upgrade reached nothing. A non-relative specifier resolves through node from the importing file, so the package's `exports` decides what is importable; the error names both causes (not installed / not exported) **always**, because node distinguishes them and bun collapses both into `MODULE_NOT_FOUND` — branching would make the message depend on the runtime. `import "..." into <db>` is the other half: the one parameter that varies, since a shipped fragment must spell some database name and only the app knows its own. Nearest wins — an inner `into` beats an outer, any `into` beats a `@@db` in the file, one file imported twice under two `into`s is an error. `inlineImports` follows a package specifier and applies `into` on text with the same rule, so `release`'s baseline and `createTestEnv` see what `parseFile` sees. `fli auth:install` emits the import instead of writing `db/auth.lite`, and `--db` became `into <db>`. Twelve tests, negative-controlled |
| FJS-039 | caravan — **`cancel()` on a RUNNING job was reverted by the attempt still in flight** | 2026-08-15 | `AND status = 'running'` on `markDone` and `markFailed` ([db.ts](packages/caravan/src/db.ts)), which is what the comment beside `cancel` had claimed for months and no code did. The failed half carried more than the done half: without the guard a cancelled job whose handler then threw was written back to `pending` and **ran again**, which the new test catches ('running', not 'done'). The worker now reads `changes` off the UPDATE and announces nothing when it is 0 — a `caravan.job.done` telemetry event for a job that ended up cancelled is the same lie one layer up. Two tests in `tests/caravan.test.ts`, both verified failing against the unguarded statements; 81 pass, typecheck clean, `example`: `verify:jobs` 8/8 |
| FJS-258 | repo — **the substrate package's purity was prose, and the ruling rested on it** | 2026-08-15 | `substratePurity()` in the `hygiene` phase ([scripts/ci.mjs](scripts/ci.mjs)), over a `SUBSTRATE` list so a second substrate package is one line rather than a new check. Two halves: the manifest declares no `dependencies`, `peerDependencies` or `optionalDependencies`, and no file under `src/` reads a clock, calls `Math.random`, touches `process`, fetches, loads a module at runtime or reaches for a global. **The import rule is deliberately blunt — a substrate file may import RELATIVELY and nowhere else** — which catches a node builtin, a workspace sibling and a registry package in one test instead of three lists that go stale. Comments and strings are stripped before the patterns run, or the rule fails on the paragraph explaining it; the import scan keeps strings and drops only comments, because the specifier IS a string. **Proved by breaking it three ways** — a `Date.now()`, a `node:fs` import and a `lodash` dependency — each reported by name and reason, then reverted | 
| FJS-269 | **`fli check` had never run — the arch-test surface the docs point every app at threw on its first line** | 2026-08-15 | `commands/fli/check.md` calls `resolve(global.fliRoot, 'core/checks.js')` and imports `resolve` from nowhere; there is no `commands/fli/_module.md` to supply it. Every invocation died with `resolve is not defined`, in this repo and in a client app alike, since the command shipped. The exact shape `FJS-167` named and the parse sweep cannot see: **it compiles each command with no namespace module**, so a free identifier parses clean and throws on the first run. Nothing noticed because CI's `structure` phase imports `core/checks.js` directly — the engine was green the whole time and only the door was broken, which is the argument for two callers stated backwards. Found by putting `fli check` into a scaffolded app's `bun run check` and running it. Now imported explicitly with a comment saying why it is not assumed, and the `scaffold` CI phase runs `bun run check` inside the app it builds, from an INSTALLED cli rather than this repo's bin |
| FJS-264 | **Three readers ignored `import` in a schema, each silently** | 2026-08-15 | `createClient` resolved imports through `parseFile`; three readers did not. **litestone's CLI** — `loadSchema()` read the root file, so `db push` reported *already in sync* while the imported tables were never created, and every snapshot generator behind it described half a schema. **sierra's build** — the browser got a `$defs` table with those models missing, so `modelNameFor` missed, `createResource` degraded to a bare `make()`, and a generated `<Form>` rendered nothing against an app that built clean. **`createTestEnv`** — graded a partial schema and PASSED: a green `verifyGateLadder` over models it never saw, which is the thing that exists to catch the other two. All three take a path and use `parseFile` now; `createTestEnv` splices to one text (it is the template cache key) and REFUSES an import it cannot read. `inlineImports`/`inlineImportsFromDisk` in `parser.js` own following an import line as text, for `release`'s baseline (which inlines at the REF) and `createTestEnv`. `parseFile` also stopped throwing a `ParseError` where `parse` returns one — that mismatch is how this surfaced. Nine tests across litestone and sierra, each checked against a negative control |
| FJS-038 | **The auth schema was hand-copied into `cli/commands/auth/install.md`** | 2026-08-15 | Two walls held the copy up, not one: `fli` is global, so the package is not beside it — and `fli` runs on **node** while `packages/auth/schema.ts` is TypeScript, so even resolved it could not be imported. `@frontierjs/auth` now ships `db/user.lite` + `db/auth.lite` as the source and `schema.ts` reads them; `auth:install` installs the package, resolves the subpaths through auth's `exports` and reads the same bytes. Split by owner: `User` is appended to the app's `schema.lite`, the three `@@gate("8")` models go to `db/auth.lite` and are imported. One rule is still restated — the `@@db(main)` swap — and auth's suite lifts the CLI's arrow out of the markdown and runs it. `FJS-265` is the end state |
| FJS-192 | sierra — **inflection rule sets resolved one invariant and the weakest was the one `createResource` used** | 2026-08-15 | Filed as four; there were **five** — litestone's `pluralizeWord` and `toSingular`, junction's `deriveModelName`, sierra's `_pluralOf` and the inline ternary in `createResource`. All five now call `@frontierjs/toolbelt/inflect`, unblocked by [FJS-D26](#fjs-d26). The shared module is the UNION of what the copies knew, not the intersection: litestone's 21 irregulars **and** junction's `us`/`is`/`as`/`ss` guards, which is why `status` no longer singularises to `statu` and `people` now resolves `Person` with no `{ model: … }`. **It also fixed litestone's table silently**: the irregular table was consulted LAST, behind the sibilant rule, so seven of its own entries were unreachable — `index` pluralised to `indexes` while the table said `indices`. It is consulted first now; no schema in the repo names an affected model, checked before the change, and `@@map` is the escape for one that does. The table stays whole-word only, so `audit_index` is still `audit_indexes` — reaching inside a compound would rename tables in schemas that already exist. Ten cases in `inflect.spec.js`, one of which asserts the structural limit (`lens` → `len`, because `pens` is a real plural) rather than hiding it. Green: toolbelt 27, litestone 2093, junction 1048, sierra 877 + `test:safety`, typecheck baselines unmoved |
| FJS-263 | litestone — **`aggregate` and `groupBy` ignored `withDeleted`/`onlyDeleted` and `withTemplates`/`onlyTemplates`, answering the opposite of the question** | 2026-08-15 | Both pinned the mode to `live`/`instances` regardless of their args, so `aggregate({ _count: true, onlyDeleted: true })` counted the LIVE rows and `onlyTemplates` counted the instances — from the two methods whose whole output is a number nothing can cross-check against a list. Split out of FJS-176 because it is not a template bug: it hit soft delete identically, and fixing it for one family would have left the other wrong. Both now read `sdMode(args)`/`htMode(args)` like every other read. Found by asking the same question of both families at once |
| FJS-176 | litestone — **`@@hasTemplates` blocked every write, so a template could be deleted but never edited** | 2026-08-15 | **Ruled: a template is a live parallel class, not an end state, so writes take the same flags reads take.** `update`, `updateMany`, `upsert`, `remove`, `delete` and `deleteMany` accept `withTemplates`/`onlyTemplates`; `@@softDelete`'s `withDeleted`/`onlyDeleted` got the same treatment, having been accepted on a write and silently dropped (not in the destructured signature, so it went the way an unknown key goes, while `take`/`skip` on the same call throw by name). That closes the promotion trapdoor too — `update({ isTemplate: true })` always worked and nothing could reach the row again, so demotion is now `update({ isTemplate: false, withTemplates: true })`. **One behavior change, in the safe direction**: a hard `delete`/`deleteMany` applies the template filter, so an ordinary cleanup stops destroying rows no read of the model returns. It still bypasses the SOFT-DELETE filter — that is its stated contract and why it exists beside `remove` — and a flag narrows that too, so `deleteMany({ where, onlyDeleted: true })` is how a purge is spelled. The distinction is the ruling itself: a deleted row is out of play with `restore()` as its way back, which is what makes *uneditable* coherent there and wrong for a template. `asSystem()` lifts neither filter, and never did — they shape which rows a statement is about rather than who may read them; the flags are the only way past, for every client. **Documented at last** in `docs/schema.md` § Templates: the attribute appeared in no file under `docs/`, which is why no intent existed to check any of this against. 10 new tests, and 5 fixtures that meant *all rows* now say so |
| FJS-D23 | repo — **nothing could say "this payload key is not a column"** | 2026-08-17 | Ruled and built as `@transient`, the mirror of `@computed`: a field with no column, written by the caller and never read back, where `@computed` is a field with no column that is read and never written. Refused instead: a service-level `accepts:`, because `autoValidate` is derived from a MODEL and runs on create/patch/update alone — a service with no model already passes every key through, so the option would have added vocabulary for a surface with no problem. `autoValidate` validates the key with the model's own rules (`@length`, `@required`, the `@label` in the message) and then LIFTS it onto `ctx.transients`, a fifth context field: call input the framework parsed, the way `directives` is, where `locals` is scratch a hook keeps. It must leave the payload because the Data boundary refuses a transient key by name. A bulk write carrying one is refused rather than lifted — the surviving rows are the ones that passed validation, so an index-aligned array would pair one row's credential with another row. It is emitted `writeOnly` into the create/update JSON Schema and absent from the read modes, out of the row type and out of `Where`, out of the DDL, out of the audit trail, and refused by name in a `where`, an `orderBy`, an aggregate and a policy predicate. `isStoredField` in `ddl.js` is now the one answer to what becomes a column — `CREATE TABLE` and the rebuild's `INSERT … SELECT` were asking separately and had drifted. What the declaration buys is the browser: sierra registers the create-mode schema, so `<Form>` renders a control and `createResource` validates it, where a hook-held field was stripped in the browser before the request was made. Proven in basecamp's `channels`, where `captureCredential` is deleted and `ctx.transients.secret` replaces it. 16 litestone tests, 6 junction, 3 sierra; ruling in `DECISIONS.md` § API design |
| FJS-D22 · FJS-095 | repo — **nothing could say "the system writes this column, not the user"** | 2026-08-15 | Ruled and built as `@system`: readable by anyone, refused on write by name, the orthogonal sibling of `@guarded` (which locks both directions). The application fills it by naming the column on the call — `update({ where, data, system: ['trackingCode'] })` — which keeps the gate, the row policies, soft-delete and the audit actor, where `asSystem()` drops all four to write one value. It reaches the client as `readOnly` + `x-litestone-kind: 'system'`, so Sierra's control table has no control for it, `<Form>` does not render one and `make()` does not seed it; and it is **out of create-mode `required`**, which is what refused every create in the browser naming fields the caller was never meant to send. A refused write THROWS for `@system` and still DROPS for a dynamic `@allow('write', …)` — basecamp's three `auth().isSystemAdmin` columns round-trip through an ordinary member's profile save, and making both loud would 403 every one of them. `@guarded(all) @system` is now spellable, which [FJS-235](#) recorded as impossible. Proven in `example`: the order form carried the repo's only hard-coded field name and now carries none, and the courier job writes the column through a `recordTracking` action rather than a patch — `verify` 37/37, `verify:jobs` 8/8, `verify:build` 37/37. 18 tests. Ruling in `DECISIONS.md` § Access control; `FJS-D23` is deliberately still open, being the same seam from the other side |
| FJS-260 | example — **`verify:ui`'s empty-state case asserted something the seed contradicts, so the drive could not pass** | 2026-08-15 | `emptyState.shows` picked `retired` and waited for `.empty`; the catalogue seeds `Discontinued Cap` with `active: false`, so that filter matches exactly one row and the wait timed out — a broken expectation reading as a broken component. Fixed as the row proposed, by asking for a combination that really is empty rather than deleting the case: an ACTIVE product's name (`Field Notebook`) **and** the retired status. An empty filter result and an empty catalogue are different situations and the page says so, which is the thing worth driving. The two option clicks are scoped to `.fjs-combobox-panel` and `.fjs-multiselect-panel` — both controls render `[role="listbox"]` only while open, and an unscoped selector would race the first panel's teardown. Expected values unchanged. Drive run against live servers: **all 27 assertions pass** | 
| FJS-262 | litestone — **`findManyCursor` and `search()` applied neither the global filter nor the ROW POLICY, and `aggregate`/`groupBy`/`search` never reached the `@@gate`** | 2026-08-15 | Found while closing FJS-215, by asking which methods evaluate the global filter. Measured: on a model with `@@allow('read', ownerId == auth().id)` and a tenant filter, `findMany` answered **1** row and `findManyCursor` and `search` answered **3** — another owner's row and another tenant's included. Separately, `aggregate`, `groupBy` and `search` never called `plugins.beforeRead`, so a model at `@@gate("7")` answered a level-4 caller's COUNT, its GROUP BY and its full-text search; the row policy compiles into the WHERE and DID apply on those two, which is what hid it — the gate is the layer that refuses outright. Both paths build their own SQL rather than going through `buildSQL`, which is the fourth time that shape has been found missing something every other read has (FJS-173, FJS-178, FJS-185, FJS-216). Fixed by composing in the same ORDER `buildSQL` does — filters, soft-delete, `@@hasTemplates`, caller where, then the policy appended last with its params after the cursor's, because positional binds make the order the correctness. `search` merges once and both of its steps use it, so the pre-filter and the row fetch cannot drift. **The grid is the fix, not the two methods**: a new test asserts every read method — `findMany`, `findFirst`, `findUnique`, `count`, `exists`, `findManyAndCount`, `findManyCursor`, `search`, `query`, `aggregate`, `groupBy` — against one seeded model where exactly one of three rows is visible, and every one of them against a gate. 4 tests |
| FJS-215 | litestone — **a global filter naming a `@computed` field silently emptied every read of that model** | 2026-08-15 | The static form was refused at startup on 2026-08-12; the FUNCTION form takes a `ctx` and had no answer until a query asked it, so it went unjudged entirely and kept the whole defect alive behind one spelling. `resolveGlobalFilter()` is now the one place a global filter becomes a value — six evaluation sites funnelled into it — and it applies the startup rule at the first moment there is something to judge. **The unknown key was the other half.** It was excluded from the refusal in both forms, and it is the same defect: SQLite reads an identifier it cannot bind as a string literal, so `{ nope: 'x' }` compiles to `'nope' = 'x'` and empties the model, while `{ nope: 'nope' }` compiles to `'nope' = 'nope'` and returns **every** row past a filter meant to narrow. Refused now in both forms, with edge namespaces folded into the legal keys the way `withArgValidation` already folds them for a caller's `where`. Unknown stays a WARNING in a caller's `where` — that trade is ruled (sorting.md) and the reasoning does not carry: a caller has a hint and a stack, while the filter is app configuration applied to every read for the life of the process, with nobody to warn. 5 tests. Tier 3 (FJS-202) is untouched and still open |
| FJS-261 | mesa — **every `<` and `&` in a highlighted markdown code fence reached the reader as literal entity text** | 2026-08-15 | `<div>x</div>` rendered as `&#x3C;div>x&#x3C;/div>` and `a && b` as `a &#x26;&#x26; b`, in every fence of every `.md` page mesa compiles. rehype writes both characters in their **numeric** forms, `compiler-md.js`'s decode table knew only the named ones, and the survivors were tokenised as three punctuation tokens in three `<i>` elements — which is also why no browser could reassemble them. Fixed in two owners because neither half works alone: `decodeEntities()` replaces the table with **one pass** over named, decimal and hex forms (a chain of replaces decodes its own output — numeric-then-named turns a literal `&lt;` into a `<` nobody typed), and `glow`'s `encode()` now escapes `&`, first, so decoding `&#x26;` cannot leave a bare ampersand on the page. `renderRow` also encodes the text BETWEEN tokens, which was raw and looked safe only because `<`, `>` and `&` are punctuation rules in most languages. Found by [FJS-191](#) removing the fork that was hiding it |
| FJS-226 | litestone — **`datetime('now')` could not match a stored DateTime, so a `$raw` clock predicate was right for old rows and wrong for today's** | 2026-08-15 | **`now()`**, exported beside `sql`, emits `strftime('%Y-%m-%dT%H:%M:%fZ','now')` — SQLite's clock in the format the column holds. Its modifiers are BOUND rather than spliced (`strftime` takes them as parameters), so Invariant 8 holds inside the escape hatch; that is what a `SqlFragment` is, litestone's own SQL, and nothing built from caller text can be one. Being SQLite's clock rather than a JS timestamp, every occurrence in one statement is the same instant — SQLite fixes `'now'` per statement, which two `new Date()` calls cannot promise. Stated cost: `createClient({ now })` does not reach it, and a test needing a frozen instant binds its own ISO string. `now()` also works as a **token** in raw SQL (`where: "dueAt < now()"`), because a `@from` string and a plain-string `$raw` cannot interpolate and a refusal naming a spelling the caller cannot write does not help. **Six spellings are refused by name** — `datetime('now')`, `date('now')`, `time('now')`, `CURRENT_TIMESTAMP`, `CURRENT_DATE`, `CURRENT_TIME` — at all three doors raw SQL comes through: the `sql` tag, a plain-string `$raw` (which skips the tag, and is the form written when nothing is interpolated), and a `@from(where: …)` string, refused at `createClient` because the subquery is built once at startup. `julianday`/`unixepoch`/`strftime` are untouched: they answer numbers or the caller's own format, and comparing like with like was always correct. `docs/gotchas.md` taught the broken spelling in two worked examples. Also stamped `_litestone_seeds.appliedAt` in ISO to match `_litestone_migrations`, which was the same defect between two internal ledgers. 12 new tests |
| FJS-191 | mesa — **`glow` was forked, and mesa ran the copy with the bugs the other one had fixed** | 2026-08-15 | Closed by deleting the fork. `packages/mesa/src/glow.js` is gone and `compiler-md.js` imports `@frontierjs/toolbelt/glow` — the export surface was identical, so it was one import and a deletion, and what it removes is 211 lines that could drift again. Both live defects go with it: encoding moves from per-token `elem()` into `renderRow`, so a rule matching more than one character can no longer send raw markup to the page, and `isTrailingComment()` stops a mid-line `/*` swallowing the code before it. **Unblocked by [FJS-D26](#fjs-d26)**, which ruled toolbelt substrate below the graph rather than a workspace dependency in Invariant 1's sense — mesa declares it and stays a leaf. Three cases in `test/compiler.test.js` pin the fence path; 1083 mesa tests green. The third pins [FJS-260](#) instead, which the fix exposed |
| FJS-078 | ui — **`<Select>` did not derive its options from the schema** | 2026-08-15 | `fields.status.enum` exists because `{"$ref":"#/$defs/OrderStatus"}` is resolved against the registry, and every enum select in the repo mapped it by hand anyway. Inside a `<Form>` an unstated `options` now falls back to the rule's `enum`, which makes the option list a schema fact like the label and the `required` beside it. Stated options still win, and a select over anything that is not an enum is untouched. It landed as the prerequisite for `<Form>`'s generated field list — an enum column had to be able to render itself before a generator could emit one. `RadioGroup` is the same fix and is part of what `FJS-077` still holds. Proven in `example`'s `verify` (`form.statusOptions`) |
| FJS-254 | cli — **`fli project:map` and `project:view` derived what a service IS from regexes over service source, and could not have been right** | 2026-08-15 | Junction decides at CONSTRUCTION whether a key is an option or an action (`collectActions`, read back through `svc.describe()` — Invariant 4), `svc.pipelines()` resolves the hook chain, and `apiPrefix` moves every route. None of those is a fact about how a file reads, so the scan could not agree in the general case: an action assigned from a module-level const takes no visible `ctx`, a `methods:` list built from a variable is not a literal to match, and a hook added by a plugin or by `app.hooks()` is in no service file at all. The result was a viewer drawing a confident picture with nothing able to contradict it. `extractServiceMeta` is deleted; both commands now read the committed `surface.snapshot.md`, which `junction surface` writes off a BUILT app and the `snapshots` CI phase fails when stale. **Absent means absent** — no snapshot yields no services and a warning naming the command, because falling back to a scan is how the viewer gets to be confidently wrong again. What the surface carries that no scan could: `alerts · model AlertRule` (the scanner read the accessor), five actions per basecamp service, the hooks that run around EVERY call, 27 mounted routes and the plugins in configure order. **`extractResourceMeta` stays and was rewritten**, because a Resource has no committed artefact — it is constructed in the browser, so the `createResource(...)` call's literal arguments are the only thing there is. It now reads the CALL rather than the file: comments stripped first (a `model:` in prose is not a declaration — the body-tag-in-a-comment shape), a balanced argument list rather than `[^)]*`, and hooks read per phase and method instead of swept for lowercase identifiers minus a skip list. Measured against the sweep it replaces on six shapes and wrong on all six — a second phase or method written on ONE line took the whole block with it and answered empty, an inline arrow contributed its argument name, and a qualified `session.stamp` came back as two hooks · [_module.md](packages/cli/commands/project/_module.md) · [map.md](packages/cli/commands/project/map.md) · [view.md](packages/cli/commands/project/view.md) |
| FJS-037 | cli — **a sixth reserved-key list, in a heuristic that could not import the shared set** | 2026-08-15 | Closed by deleting what held it. The list was the `RESERVED` set inside `extractServiceMeta`, which existed to re-apply junction's option-or-action rule from outside junction — so the right fix was never to share the set but to stop asking the question here. See `FJS-254`: the surface is read off a built app, where `collectActions` already answered it |
| FJS-194 | litestone — **an authored message on an array validator was emitted to the client and ignored by the server** | 2026-08-15 | `@minItems`/`@maxItems`/`@uniqueItems` and the `Int[]`/`String[]` element checks moved out of `client.js`'s `writeData` into `validate.js` — `VALIDATORS` + `DEFAULT_MESSAGES` + a `validateField` case, like every other rule — so `attr.message` is honored and the family has a default for `x-messages` to fall back to. Four things came with it: **`@uniqueItems` takes a message now** (the only validator that did not, which made *every validator takes a trailing message* untrue in the docs); the default wording lost its field-name prefix, matching the rest of the table; every array error comes back at once rather than the first throw; and `buildValidationMap` flags a model with any array field, because the inline checks ran unconditionally and `validate()` does not — a model whose only rule was a bare `String[]` would have stopped being checked. `generateValidationCases` now reads the table instead of restating the wording, which is the second copy that found this in the first place, and `uniqueItems` joined the `x-messages` keyword table. 7 new tests |
| FJS-241 | cli — **a scaffold made with local sources could not be containerised, so `fli deploy:local` could not be run against what this repo produces by default** | 2026-08-15 | `fli new --source local` writes `link:@frontierjs/junction` and four siblings, and an app inside the workspace writes `workspace:*`; neither resolves inside a Docker build, which failed once per package at the `bun install` layer. The answer is **not** to change what the app depends on — a `link:` spec is what makes an edit visible with no reinstall, and a `file:` tarball in the same place goes stale on the first save and says nothing — so the swap happens at BUILD time. `packages/cli/core/vendor.js` packs every publishable package sharing a scope with a linked one into `deploy/generated/vendor/` and writes `app-manifest.json` pointing at the tarballs, `overrides` included, since the packages depend on each other and a range left alone resolves from npm and quietly mixes a published sierra into a local mesa. **One owner, three callers**: `fli deploy:vendor`, which `deploy:local` and the pipeline's build step run before they build; and `packages/basecamp/deploy/build.mjs`, which had the only working implementation and now calls it. The generated Dockerfile installs from that directory on **both** source modes — with nothing linked it is a verbatim manifest copy with the lockfile beside it, and the freeze is conditional on the lock being there, because a template with a branch in it is wrong for half the apps that copy it and the source mode changes long after `make:deploy` ran once. `deploy:doctor` fails a Dockerfile that installs from `package.json` while a `link:`/`workspace:` spec is declared, reading it with comments stripped — the template EXPLAINS the path in its own header, so asking the whole source passes for a Dockerfile that only talks about it. **Proven by running it**: `fli new --source local` → `make:deploy` → `deploy:local` builds, boots, migrates and answers health, which was impossible before. CI's `deploy` phase now runs both sources — npm keeps the only test of the PUBLISHED framework (FJS-252), local is the working tree containerised — and the `scaffold` phase packs through the same module rather than its own copy |
| FJS-224 | litestone — **`@from(first/last)` picked the row before the target's policy was known, so a denied row read as `null`** | 2026-08-15 | The pick is redone in `resolveFromRowRefs` whenever the target declares a read policy: the policy goes into the WHERE and `ROW_NUMBER() OVER (PARTITION BY <fk>)` chooses one row per parent — the answer a direct `findFirst` gives. The window runs INSIDE the policy, not over it: ranking first and filtering after would rank rows the caller cannot see and then delete the winner, which is the same null wearing a second hat. Not the per-request subqueries the row proposed — the static SQL stays, and its id is simply discarded on a policied target, so the no-policy path is untouched and no params thread through the six SELECT builders. `@@softDelete`, `@@hasTemplates` and a declared `where:` are lifted out of the static subquery and restated with the table in place of its alias (a policy compiling `check(parent)` names the table, and an alias puts it out of scope). The repick correlates on the column the target points back at, so `parseSelectArg` injects it the way it injects an FK — `select: { lastOrder: true }` alone still repicks and does not get the key back in the answer, which is where the residue would otherwise have lived. 10 new tests |
| FJS-235 | litestone — **`@guarded` hid a column on read and let any client write it** | 2026-08-15 | `writeData` refuses a non-system write that names a `@guarded` column, with an `AccessDeniedError` naming every field — one owner, so `create`, `update`, `updateMany`, `upsert` and both bulk paths are covered. **Refused rather than dropped**, which is the opposite of the field `@allow('write', …)` beside it: that predicate is per-caller, so the same form body is legitimate one level up and dropping the key is right there; nothing may write a guarded column, so naming one is a mistake worth hearing about. Ruled: **`@allow` still cannot sit beside `@guarded`** — two answers to one question, and the column picks the one that describes it. `@encrypted` alone is deliberately outside the lock — it used to imply `guarded: 'all'` in the field policy map, which would have made every encrypted column system-write-only and broken an admin adding a secret through a scoped client; the read strip already had its own `encrypted` branch, so decoupling changed no read behavior and `@secret` (which synthesises a real `@guarded(all)`) is locked both ways. Two consequences: a **required** guarded column makes its model uncreatable below level 8, and `verifyGateLadder` now reports that create column ungraded rather than reading the field lock as a gate verdict; and a seed writing a guarded column through the default client needs `asSystem()`. 10 new tests. **The spelling half closed 2026-08-15**: the row noted that a column invisible to a client AND unwritable by one could not be declared at all, because field `@allow` is refused beside `@guarded`. `@guarded(all) @system` is now legal and means exactly that ([FJS-D22](#)) |
| FJS-248 | litestone — **`docs/schema.md` called `@guarded` *excluded from all operations*, which was false in the dangerous direction** | 2026-08-15 | Closed by making the sentence true rather than by rewriting it: see FJS-235. `schema.md`, `encryption.md`, `access-control.md`, both `CLAUDE.md`s now say system-context column, read **and** write — and `encryption.md`'s claim that bare `@guarded` is *returned by findUnique* went with it, which was wrong against the code and against its own test |
| <a id="fjs-d26"></a>FJS-D26 | repo — **May a core package import `@frontierjs/utils`?** | 2026-08-15 | **Ruled 2026-08-15: yes — it is substrate, not a member of the graph.** Invariant 1 now states the dependency direction alone; the *zero workspace deps* clause is gone and with it the contradiction. What keeps the exemption safe is that the substrate itself depends on nothing and stays pure — a `Date.now()` in its `src/` retires the argument, and nothing checks that yet ([FJS-258](#)). Unblocks [FJS-191](#) and [FJS-192](#); the package's NAME is [FJS-D14](#fjs-d14)'s. `DECISIONS.md` § Dependencies & the ecosystem. Was: Invariant 1 said *Mesa is a leaf with zero workspace deps*, and CLAUDE.md's package table repeats it. `packages/utils/README.md` already claims the exemption in the other direction — utils holds "the same standing that Mesa holds", sits below the whole tree, and "any package may import it, including litestone" — so the two documents disagree about a rule one of them calls an invariant. **Nothing about the utils package can proceed until this is answered**, because mesa, litestone and sierra hold most of the duplication it exists to absorb: `FJS-191` (the forked highlighter) and `FJS-192` (four inflection rule sets) are both one import away and both blocked here. The substance of the ruling is what "leaf" means — *no dependency at all*, or *no dependency on a framework package*, with utils defined as substrate below the graph rather than a member of it. The second reading is what the README assumes and what makes the package usable; the first is what is written down. Whichever wins, the loser's wording needs correcting rather than leaving both in place, and if utils is admitted as substrate the purity rule stops being prose and needs a check in `scripts/ci.mjs` § hygiene — a single `Date.now()` in `src/` retires the argument that made the exemption safe — [IDEAS/shared-pure-functions.md](IDEAS/shared-pure-functions.md) · [toolbelt/README.md](packages/toolbelt/README.md) · [CLAUDE.md](CLAUDE.md) § Invariants 1 |
| <a id="fjs-d02"></a>FJS-D02 | junction — **Custom service methods are called `actions`** | 2026-08-15 | **Ruled 2026-08-15: they are methods; there is no fourth noun.** The declaration (`methods:`), the wire (`X-Service-Method`), the context (`ctx.method`) and the refusal (`MethodNotAllowed`) all said *method* already; `action` was a second word for a set the CRUD list defines by exclusion. "Custom method" is the adjective where the distinction matters. One public rename — `svc.action(…)` → `svc.invoke(…)` on the browser client and on a Sierra resource's `service` — and `describe()`/`/metrics`/`surface.snapshot.md` report `customMethods`. No wire change — [DECISIONS.md](DECISIONS.md) § Naming & vocabulary |
| <a id="fjs-d03"></a>FJS-D03 | repo — **Rule the hook context shape.** | 2026-08-15 | **Ruled 2026-08-15: Context is per-realm, plural and DOCUMENTED — not unified.** The filed premise does not hold: two things called `ctx` are not the same noun (litestone's is per **client scope** and carries the compiled maps; mesa's compile ctx has no caller at all), so they cannot conform as subsets. Each package documents its own **by LIFETIME** — what creates it, how long it lives, what it is not — because lifetime is what prevents the real mistakes. **Naming**: path captures are `ctx.route` on both of Junction's contexts (the transport's `params` renamed) and `page.params` in Sierra; `params` stays out of Junction because the Feathers idiom keeps arriving and a field of that name holding something else is how a role check reads `undefined` and passes. **And one line of the stated contract was false** — `auth` was documented as propagating and did not, so every call chain began at STRANGER(0) unless threaded by hand, which is the same root cause as the Caravan job hazard. It propagates now, through the ALS store the transport already opens; `{ user: null }` is still anonymous, because absent is not null. Enforced by `tests/context-contract.test.ts` rather than a check rule — propagation and freezing are runtime behaviors — verified by breaking it — [DECISIONS.md](DECISIONS.md) § Naming |
| <a id="fjs-d12"></a>FJS-D12 | ui — **i18n must be decided before schema→UI ships** | 2026-08-15 | **Ruled 2026-08-15: English only for alpha, and the seam is reserved by six constraints rather than by a catalogue.** The premise expired first — schema→UI shipped without it, because the generator authors no string (a label is `@label` or the title-cased column name, the same resolution a hand-written control already did), so what generation multiplies is call sites. The survey is what makes deferring safe: ZenStack takes a literal `message` per validation attribute and has no i18n at all, Rails proves the derived key (`human_attribute_name` + messages keyed by validator name, zero cost untranslated), Payload keeps configuration labels and localized DATA as two mechanisms, and Drupal's interface/configuration/content split is the shape. **The six that bind now**: `@label` is a default English string and never a key, the address being derived `Model.field.label`; an error carries a stable code and params, the sentence being a fallback; configuration strings and content are never one attribute; `/inflect` is structural English and never takes a locale (a locale there renames tables); a kit component's strings are props with English defaults, so `@frontierjs/ui` gains no dependency; formatting gets one owner (there is no `Intl.` in the tree — keep it so). Catalogues, negotiation, pluralisation, per-locale data and `lexicon` are V2. Reserved as ours: a seed-derived `strings.snapshot.md` gated by the `snapshots` phase, `db.$setLocale()` as a client flavor, and per-locale prerender on the `static` target. `DECISIONS.md` § Dependencies & the ecosystem — [DECISIONS.md](DECISIONS.md) · [IDEAS/ecosystem-gaps.md](IDEAS/ecosystem-gaps.md) 4 |
| <a id="fjs-d13"></a>FJS-D13 | junction — **Does a stream sit outside the result envelope, or does the envelope grow a third `kind`?** | 2026-08-15 | **Ruled 2026-08-15: outside. `kind` stays `single \| list`.** The envelope answers what a call RETURNED — one record or a page, with `total`/`limit`/`offset` — and a stream returns nothing and then repeatedly. A third value would be branched on at ten sites and land in every one of them as *not a list*, silently. The framework already streams and already answered it the other way: `publish()` is an after-hook, so a pushed frame IS `ctx.result` and `unwrapResult` strips the envelope at the wire. **Corollary with teeth**: each FRAME is a result and the stream is not, so a stream of records belongs where the pipeline produced it — `ctx.sse()` on a raw route has no `gateAuth`, no `protect()` and no app hooks. Enforced rather than asserted: `wrapResult` refuses a Response / ReadableStream / async iterable by name, where it used to wrap one into `data: {}` and answer 200 with the stream destroyed. Not decided: whether a method may DECLARE itself streaming, and per-subscriber frame filtering ([FJS-011](#) residue) — [DECISIONS.md](DECISIONS.md) § API design |
| <a id="fjs-d14"></a>FJS-D14 | repo — **Four folders under `packages/` are claimed and not built — name them or remove them.** | 2026-08-15 | **Ruled 2026-08-15, and closed.** All four named: `toolbelt` is the substrate package (it absorbed `@frontierjs/utils`, which is deleted) and `datetime-kit` is a subpath of it rather than a folder — **one package, one kit per subpath**. `orion` (the automations engine, basecamp's partner app) and `oracle` (the solutionizer — a requirement in, the mental model of it out) are **V2, deferred until FrontierJS core leaves alpha**: both would be built ON the framework, so neither is a gap in it, and each waits on a core decision anyway — orion's main trigger needs [FJS-010](#)/[FJS-D04](#fjs-d04), oracle would be the first `fli` command to call an LLM. They stay claimed folders, exempt by name in `scripts/ci-allowances.json`, and that exemption is the answer rather than a placeholder. `DECISIONS.md` § Repo conventions — `packages/orion/README.md` · `packages/oracle/README.md` |
| <a id="fjs-d18"></a>FJS-D18 | mesa — **`$: { (a, b) }` vs `$: (a, b)`** — semantically different, not called out in the spec | 2026-08-15 | **Ruled 2026-08-15: braces mean *run code*, parens mean *watch*, and an inert block is refused by name.** `$: (a, b)` is a multi-path watch; `$: { (a, b) }` is a compile error, as are `$: { }`, `$: { count }` and `$: { cart.total }`. The two forms parse to the SAME AST — a `SequenceExpression` of identifiers — so the parens are the only separator and the check reads them from source position. Effects don't drive renders in Mesa, so a block with no side effect is unobservable, and the old behavior was worse than either answer: it compiled to `orderedGroup([{ deps: [a], handler: <the value of b> }])` and threw `fn is not a function` the first time `a` changed. Corollary: the handler shorthand is unbraced-only (RULE 52) — `$: a, syncFn` yes, `$: { a, syncFn }` refused, because `{ a, syncFn }` and `{ a, b }` cannot be told apart. Spec is VISION §4.4/§4.8 (RULE 14b, 50, 52) and the §4.9 table; pinned in `test/inert-block.test.js` — [DECISIONS.md](DECISIONS.md) § UI substrate (Mesa) |
| <a id="fjs-d19"></a>FJS-D19 | litestone — **Litestone `Plugin` has no `name`; rename the concept** | 2026-08-15 | **Ruled 2026-08-15: name yes, rename no.** Two questions filed as one, with opposite answers. The name defaults to the class name and a stated `name = '…'` wins (a minifier rewrites `constructor.name`); `db.$plugins` lists what is installed in run order on every flavor of client, and its first useful answer is that a gated schema auto-installs `GatePlugin`, so what you passed is not what is running. **The rename does not survive contact with what ships**: `GatePlugin`, `FileStorage` and `ExternalRefPlugin` each attach a capability, hold config and have an `onInit` lifecycle — Junction's definition of Plugin exactly — and litestone already has `hooks` one option key away, so Plugin→Hook would collide with a live concept in the same package. What differs is the interception surface, which is a realm difference. Still to converge: ordering, which takes Junction's `requires: string[]` when it comes — [DECISIONS.md](DECISIONS.md) § Naming |
| FJS-061 | cli — **`POST /api/env` threw `ReferenceError` on every call, and two commands claimed one alias** | 2026-08-14 | `writeFileSync` was never imported — the `fs` import brought in `readFileSync, readdirSync, existsSync` — while `mkdirSync`, one line above and identically broken, had been patched with a dynamic `await import('fs')` by somebody who did not read the next statement. Both are static imports now. The alias half was four collisions, not one: `dev`, `new`, `audit` and `doctor` were each claimed twice, and the winner came out of `readdirSync` order, so `fli new` meant `project:new` on one checkout and `make:command` on another out of the same tree. `find()` sorts, which makes the resolution reproducible; then the less-typed side of each pair was renamed, so no alias is contested — `mkcmd`, `site:setup`, `ports:claim`, and `deploy:doctor` with no short form |
| FJS-210 | litestone — **a string operator on a column that is not text answered the wrong question instead of refusing — on an array column it substring-matched the JSON document** | 2026-08-14 | `contains`/`startsWith`/`endsWith` compile to `LIKE`, which asks about TEXT. Refused now on the four kinds where the text is not the value, each naming what was meant: an array (`has`/`hasSome`/`hasEvery`), a `Json` document (`@type(...)` and a path), a `File` reference, and a `Boolean` stored as 0/1. **`Int` and `DateTime` keep the answers they already gave** — SQLite's coercion answers what was asked and `{ when: { contains: '2024-01' } }` is a real way to ask for a month — so the grid did not move for them; a path INTO a typed `Json` column is untouched. `buildWhere`'s `arrayFields` Set became a `fieldKinds` Map of what the column HOLDS, which is the same fact its bare-array question already needed, so the signature did not grow and `buildArrayMap` is deleted rather than left beside a second map of one fact. **Both `where` refusals are `ValidationError`s now**, the pre-existing array-operator guard included: junction maps the name to a 400, and a bare `Error` was a 500 saying the server broke. Six `210:ref` matrix cells promoted; one regression caught by the suite, a schema `view` was still handed the old `Set` |
| FJS-253 | litestone — **`$rotateKey` re-encrypted `@secret` only, then swapped the client's key for ALL encryption, abandoning every other encrypted column** | 2026-08-14 | Measured across five column kinds and **four of the five broke**: a plain `@encrypted` read `null`, a deterministic one read `null` and filtered to 0 rows, `@secret(rotate: false)` read `null`, and `@hashed` went from matching to matching nothing — permanently, since there is no plaintext to re-key. Only `@secret(rotate: true)` survived. Not a stale-key artefact: the bytes were never rewritten, so a fresh client with the new key could not read them either. **Ruled and built**: rotation now carries every key-reversible column (`@encrypted` in both modes, `@secret`), and **refuses before the first write** when the schema declares one it cannot carry, naming each column and its reason — a rotation that rewrites half a database and then complains leaves it in two keys with nothing recording which rows are in which. `{ orphan: ['Model.field'] }` is the deliberate opt-in, **a list of names rather than a boolean** so acknowledging one column cannot silently acknowledge a second added later. `classifyForRotation()` is the one answer to *can rotation carry this column*, asked by the refusal and the loop so the two cannot disagree. `@secret(rotate: false)` **keeps its name and loses its promise**: the docs said *stays bound to the original encryption key*, which one key cannot deliver, and now say what it does. Verified: deterministic columns stay filterable after a rotation (the half a wrong re-encryption mode would break silently); 1980 tests green — [client.js](packages/litestone/src/core/client.js), [encryption.md](packages/litestone/docs/encryption.md) |
| FJS-246 | litestone — **`litestone backup` copied the wrong databases on any multi-database schema, and reported `✓`.** Two faults, compounding. **(1)** `loadConfig()` always answers a `db` — `./development.db` when nothing said otherwise — and `createClient({ db })` names MAIN and overrides a declared `database main`. So `backup` opened a client at a file the schema never named, createClient CREATED it, and the empty result was snapshotted as `main`. Proven with a marker row: `bk/main.db` had none, the real `main.db` did. **(2)** the SQLite arm then reopened a client per database as `createClient({ parsed, db: info.path })` — but that argument also names MAIN, so each client was main-at-another-database's-path, and each held every declared SQLite connection open, tripping `$backup`'s multi-database branch. Output was `bk/main.db/` and `bk/analytics.db/` as DIRECTORIES, each containing a copy of both, and `bk/analytics.db/main.db` was analytics filed as main. Fixed at the owners: `declaresDatabases()`/`clientDb()` in `cli.js` (the rule `openSqliteDbs` already applied for the migrate commands, now shared — **six commands forwarded `cfg.db` blindly**: studio, seed, optimize, backup, replicate, db push), and `$backup(dest, { only })` in `client.js` so one client answers for one database. `--db` was overloaded the same way — a name filter on a multi-database schema, a path on a single one — and read as a name it matched nothing. **This is what `05-backup` calls for the pre-deploy snapshot**, so the deploy's one guard against a bad migration was writing a wrong or empty file and reporting success. Sits directly beneath `FJS-240`, which made a partial backup fail loudly: this one was not partial, it was wrong | 2026-08-14 | [cli.js](packages/litestone/src/tools/cli.js) · [client.js](packages/litestone/src/core/client.js) |
| FJS-236 | litestone — **`$rotateKey` left every client already handed out decrypting with the old key** | 2026-08-14 | `$rotateKey` ended with `ctx.encKey = newKey` on the ROOT context, and every derived client — `asSystem()`, `$setAuth()`, `$scopedBy()` — is a SPREAD of that context. A spread copies a string by value, so the rotation reached the root and nothing else; `read()`'s catch then turned the failed GCM tag into `null`, and the field read as **empty** rather than as broken, which is the whole reason it survived. `asSystem()` is memoised in `_systemProxy`, so it stayed wrong forever; `$setAuth` is not, so a client made after the rotation worked and one made before did not — the difference that made it look intermittent. **The key is now a cell**: `ctx.enc = { key }`, shared by reference through every spread, read at all ten sites (`client.js` ×8, `policy.js` ×2). One assignment reaches every client that exists, and a context added later inherits it without anyone remembering to. Reproduced before and after: `tok-abc` → `null` → `tok-abc`. **The suite passed over it because the only assertion was that the ciphertext CHANGED** — satisfied by a rotation that scrambled every row beyond recovery. That test now reads the value back, and three more pin the derived clients; two of them were confirmed to fail against a simulated copy-by-value before being kept. Found `FJS-253` underneath — [client.js](packages/litestone/src/core/client.js), [policy.js](packages/litestone/src/core/policy.js) |
| FJS-250 | litestone — **`litestone types` emitted a client interface no app could use** | 2026-08-14 | Found by wiring it into basecamp, the first app to use it. `TableClient` was missing six methods a real accessor has — `findManyAndCount`, `exists`, `aggregate`, `groupBy`, `query`, `transitions` — so 28 correct calls in that app typed as errors; `LitestoneClient` was missing `$scopedBy`, `$checkWhere`, `$checkOrderBy`, `$rotateKey`, `$rawDbs`, `$walStatus`; `CreateClientOptions` declared `onEvent` twice (a TS2300 that makes the whole file unusable) and named `encryption: { key }`, which `createClient` does not destructure — the real option is `encryptionKey`; and a nullable column was typed `T` on write, so **clearing one did not compile** although an explicit `null` is the only way to clear a column (Invariant 9). **A generated .d.ts that omits a member is worse than no types**: the app cures it by casting the client back to `any`, which is everything the file was for. Three tests hold it — two ask a LIVE client what it has and require the emitted interface to declare each name, one pins the options shape — so a method added to the client and not to the generator now fails. basecamp's typecheck went 63 → 20 on the fixed output |
| FJS-200 | litestone — **sorting by an array, `Json`, `File` or encrypted column silently ordered rows by the stored text, and `$checkOrderBy` said the key was fine** | 2026-08-14 | `sortableKeysFor()` grew the fourth bucket the row asked for: a column whose stored TEXT is a storage detail rather than the value. One bucket, one `reason: 'opaque'`, one message per kind — an array or `Json` document is a serialization (`[10]` sorts before `[9]`, a document sorts by whichever key serialized first), a `File` is a storage reference, `@encrypted` is ciphertext that reshuffles on every re-encryption and `@hashed` is a digest. Sorting *within* an array is still undefined and deliberately so: the question was only whether the column may be a sort key, and the answer is no. Junction's `autoSort` reads the new `reason` and says its own sentence, so the 400 distinguishes *no such field* from *not a sort key* — the `$checkOrderBy` bridge kept in step on both sides. **`File` was added on the row's own principle** rather than named by it. Found while filling the crossing grid; the eight `200:ref` cells in `matrix.test.ts` are promoted to `ref`. One regression caught by the existing suite and fixed: an implicit m2m (`Tag[]`) is an array in the AST and a join table in SQLite, so it is claimed as a RELATION before the array bucket sees it |
| FJS-275 | cli — **`_steps/` was inherited by every sibling command in its directory, so `deploy:local`, `deploy:status` and `deploy:logs` each ended by running the legacy CapRover deploy against `ssh undefined` and printing `✓ Deployed to undefined in NaNs`.** The command whose entire purpose is a safe local rehearsal claimed a deployment that never happened. **Only the directory's index is the orchestrator now**, and a non-index command opts in by naming the folder (`steps: _steps-docker` in its frontmatter); a declared folder that does not exist is an error rather than a silent skip. Every other `_steps` folder in the tree — `db/import`, `db/reset`, `npm/release`, `workspace/publish` — sits beside nothing but an index, so the narrower rule is what they already relied on and none of them moved. Two more defects on the same command, both found while building the CI phase on top of it: **every `deploy:local` failure path exited 0** (`log.error` writes a line, the exit code comes only from a throw — so a failed health check reported failure and succeeded), and **`--port` had never worked** — the argv parser types a value by how it looks, before the command's declaration is consulted, so `--port 7100` reached a `type: string` flag as a number and was refused. That last one is fixed at the owner in `runtime.js` by coercing toward the DECLARED type, which also unbroke `deploy:logs --tail 200` — the command's own documented example — and `cloudflare:dns`. Pinned by four scenarios in `tests/zz-steps.test.js` over `tests/fixtures/sibling-steps/`, checked against a negative control | 2026-08-14 | [runtime.js](packages/cli/core/runtime.js) · [local.md](packages/cli/commands/deploy/local.md) · [zz-steps.test.js](packages/cli/tests/zz-steps.test.js) |
| FJS-036 | cli — **the scaffold templates had been updated twice and never run through `fli scaffold <Model>`** | 2026-08-14 | The `scaffold` phase already packed the working tree, installed a fresh app from the tarballs and built it. It stopped one step short of the thing this row is about: **growing** the app. It now runs `fli scaffold Note --fields 'title:string body:text'` against the installed app and builds again — four generated files across all three realms (a schema stanza, `api/src/services/notes.service.ts`, `web/src/resources/Note.mesa`, `web/src/routes/notes/index.mesa`), each named individually rather than trusted to the exit code, because `fli scaffold` reports per file and a step that wrote nothing would otherwise pass. Two of the four names are Invariant 19 in executable form. The second build is what makes them more than files on disk. Verified: green, ~40s. Deploy is covered by its own phase — `fli new` → `make:deploy` → `deploy:local` builds an image, starts it and gets a health answer on :7100, run end to end here — [scaffold-build.mjs](scripts/scaffold-build.mjs) |
| FJS-157 | repo — **`bun.lock` never converged, so no install could be verified and CI could not use `--frozen-lockfile`** | 2026-08-14 | Two causes, both closed. **Ours**: seven packages pinned `bun-types` / `@types/bun` at `"latest"`, which is nondeterministic by definition — two `@types/bun` copies (1.3.13 and 1.3.14) were resolved in one tree at once. Now `^1.3.14`, so the lockfile is the authority rather than the registry's mood. **The churn that looked like bun's**: `sierra` and `jetty` declare `@frontierjs/mesa` as a *peer* and neither had it as a devDependency, so bun re-resolved the peer on every install and, on the second pass, wrote a nested `@frontierjs/sierra/@frontierjs/mesa` tree — dragging mesa's own devDependencies (vitest 2.1.9, vite 5, esbuild 0.21.5 and 23 platform binaries) into a lockfile the first install had not mentioned. Adding `"@frontierjs/mesa": "workspace:*"` and `"@frontierjs/junction": "workspace:*"` to both packages' devDependencies pins the peer to the workspace — which is the pattern sierra already used for litestone and had simply not applied to its other three peers. **Measured**: two consecutive installs now produce byte-identical lockfiles, `--frozen-lockfile` passes in place, and it passes on a pristine copy with no node_modules — the case the row says has never worked. `.github/workflows/ci.yml` installs frozen now, so the next divergence fails a build instead of being discovered by whoever clones next — [ci.yml](.github/workflows/ci.yml), [bun.lock](bun.lock) |
| FJS-247 | example — **`verify:public` had been failing three assertions since the seed gained an inactive product** | 2026-08-14 | The drive expected *4 of 4 products can be ordered today*; the page correctly said 3 of 4, because `seed.ts` seeds *Discontinued Cap* with `active: false` and `LiveStock.mesa` renders `{sellable.length} of {live.length}`. The drive was the stale half and is now 3 — with the reason written beside the number, which is the part that was missing: **at 4 of 4 the assertion cannot tell an island that filters by `active` from one that renders everything it was handed**, so the gap is the test. Verified by running it against a real API and a real build: 21/21. It matters under Invariant 16 rather than as a framework defect — this is one of the two drives that prove prerendering and islands, and a permanently red one teaches everybody to read red as normal, which is the state a real regression would arrive into — [verify-public.mjs](example/web/test/verify-public.mjs) |
| FJS-213 | litestone — **`cli-smoke`'s studio test picked a random port from a range nobody reserved and failed intermittently under a full suite run** | 2026-08-14 | `5100 + Math.random() * 800` collided with whatever else `bun run ci` had in flight — twice in four runs, 3/3 green when the file ran alone, which is contention wearing a product defect's clothes. The test now asks for **port 0** and reads the bound port back out of the studio's own output, so there is no number to collide over. That needed a one-line fix in the product too: `cmdStudio` printed the REQUESTED port, so `--port=0` announced `http://localhost:0` — `server.port` is what it binds. `--port=0` is now a usable way to ask for any free port rather than a way to be lied to. No new reservation in `ports.js`: a fixed number is still a number two things can want — [cli.js](packages/litestone/src/tools/cli.js), [cli-smoke.test.ts](packages/litestone/test/cli-smoke.test.ts) |
| FJS-193 | litestone — **`migrate apply` reported "no migration files found" for a directory holding a real migration, and basecamp's was invisible in two ways at once** | 2026-08-14 | `listMigrationFiles` matches only `<14-digit>_<lower_snake>.sql` or `.js`, and `apply()` read an empty list as *there are none* rather than *none matched*. `unmatchedMigrationFiles()` now names every `.sql`/`.js` the pattern rejected; a directory holding candidates where none match is a **refusal** carrying `unmatched: true`, which the CLI prints as `✗` and exits 1 on — the two used to share one `✓` and one exit code. A misnamed file beside valid ones is named too, since silence one file at a time is the same omission. `status()` reports it as a `skipped` row and `verify()`'s drift branch names it, so all three readers agree. **The register named one cause and there were two**: basecamp declares `database main`, so `migrate apply` reads `db/migrations/main/` while `db/generate.js` wrote to `db/migrations/` — the file was both misnamed and in the wrong directory, and `createTestEnv` reads loosely enough to replay it either way, which is exactly why 68 green tests never noticed. Basecamp's migration is now `db/migrations/main/20260801000000_initial_schema.sql` and **applies** (verified: 1 migration applied, exit 0, against the real schema). `schema.test.ts` asks `listMigrationFiles` where the file is instead of spelling it, and a new test pins that a deploy can find it at all. `createTestEnv` warns once per file when it replays something `migrate apply` will refuse — the two readers disagreeing is now audible rather than silent — [migrations.js](packages/litestone/src/core/migrations.js), [testdb.js](packages/litestone/src/testdb.js), [generate.js](packages/basecamp/db/generate.js) |
| FJS-249 | cli — **`fli auth:install` scaffolded an `auth.ts` that could not import, and a gate resolver that disagreed with the schema it wrote** | 2026-08-14 | Found by running the generated shape rather than reading it. `createFjsAuth` / `createFjsAuthPlugin` are not exported by `@frontierjs/auth` (`createLitestoneAuth` / `createAuthPlugin` are), `createClient('./db/schema.lite', {…})` passes no schema because `createClient` destructures one options object, and `encryption: { key }` is not an option — three failures at the first run, in a command whose whole job is a working starting point. `project/_module.md` detected installed auth by grepping for the same two names that do not exist. Also aligned: the generated `getLevel` graded `userType === 'admin'` while the generated `schema.lite` bounded `User` with `auth().isAdmin`, so the level and the policy disagreed about who an administrator is — silently, since a policy filters rather than refuses. Resolver now grades standing and the generated `auth.ts` projects the app's own meaning of 'admin' onto it in `sessionFields`. Verified end to end against real packages: boot, register, login, the projection reaching the session, an admin writing another user's role and an ordinary caller not |
| FJS-214 | litestone — **a `@@allow` comparing an `@encrypted` column denied every row to everyone, because policy predicates were compiled without the operand rewrite a `where` gets** | 2026-08-14 | Policy predicates now take the same rewrite: `comparisonEncoderFor()` in the new [encryption.js](packages/litestone/src/core/encryption.js) is the one owner of *how a value becomes the bytes a column holds*, and `client.js`'s `rewriteEncryptedWhere` and `policy.js`'s `compileSql` both ask it rather than each deciding. So `@@allow('read', owner == auth().email)` over `@hashed` or `@encrypted(deterministic: true)` answers — verified with rows on both sides, across read, update, delete and create. The startup refusal stays for the three shapes an encoding cannot answer, and now names which: plain `@encrypted` (a random IV, so nothing can be encoded to match it), any operator but `==`/`!=`, and a column compared to a column. **`post-update` is refused outright** — it is evaluated in JS against the row read BACK, and an encrypted column is `@guarded(all)`, so it would be stripped and deny every write; `create` is exempt because its JS evaluation sees the plaintext data |
| FJS-177 | basecamp — **`User` updated at USER(4) with no row policy, so any signed-in caller could rewrite any other person's row — the hub-tier column included** | 2026-08-14 | The row was filed as a stale test assertion and the diagnosis was wrong in the part that mattered: it said basecamp keeps `@@gate("8")` on its own `User`, as [FJS-170](#) also says, and **it does not** — the hand copy in `packages/basecamp/db/schema.lite` moved to `"4.4.4.5"` with auth's, in one line of a mixed commit, and no doc moved with it. What the failing test was really reporting is that the level had changed underneath a schema whose whole hub tier is written around *even SYSADMIN cannot read User*. **Measured against the app's own resolver rather than reasoned about**: a `developer` listed every user in the database, rewrote another user's row, and set `isSystemAdmin: true` on their own — which `basecampGateLevel()` grades SYSADMIN(7), so the ladder graded a caller on a column the caller could write. Unreachable over HTTP (there is no `users` service and every User write in `api/src` goes through `asSystem()`), which is why this closes as a hole in the schema rather than an incident. Closed by two declarations, neither of them a level: `@@allow('update', id == auth().id)` for whose row, and `@allow('write', auth().isSystemAdmin)` on `isSystemAdmin`, `status` and `kind` for which columns — the three the resolver reads. `asSystem()` passes both, so `/setup` still makes the first administrator and `/hub/users/` still grants, suspends and creates bots: `bun run verify` 271/271 unchanged, `bun run test` 68/68 (three tests, one of them the rewritten assertion, all reading the row back through `asSystem()` because a policy filters rather than throws). The access snapshot carries all four lines. `@guarded(all)` was tried first and does **not** block a write — [FJS-248](#) |
| FJS-251 | sierra — **`bun run build` failed for every app installed from npm.** The mesa plugin's node_modules allowance tested `/node_modules/sierra/` for a package called `@frontierjs/sierra`, so Sierra's own `RouterView.mesa` and `ChainRenderer.mesa` went to rolldown untransformed and the build died on `JSX syntax is disabled` at line 1 of a `.mesa`. The literal is now `SIERRA_PKG`, named rather than inlined because that is what drifted. **Reproduced first**: `fli new demo --yes --auth --source npm` against the freshly published 0.1.2, `bun run build` → exit 1 on `RouterView.mesa:1:1`; patching the installed copy → exit 0, 4 route chunks, sitemap, speculation rules. Pinned by `tests/node-modules-allowance.test.js`, which drives the real `transform` with ids shaped the way an INSTALLED app produces them — a test written with workspace paths passes against the bug, which is why nothing here saw it — and was checked against a negative control rather than trusted for passing. The deeper ask stands: nothing in CI scaffolds an app and builds it, which is what `FJS-241` and `IDEAS/deploy-plane.md` both want. **Needs a sierra publish to reach anyone** | 2026-08-14 | [mesa-plugin.js](packages/sierra/src/build/mesa-plugin.js#L242) · [node-modules-allowance.test.js](packages/sierra/tests/node-modules-allowance.test.js) · [FJS-241](#) |
| FJS-242 | litestone — **`litestone replicate` replicated one database, found from a config file the scaffold does not write.** Now schema-driven, like `litestone backup`: one `dbs:` entry per declared SQLite database, each replicating to `<url>/<name>` so two databases cannot overwrite each other's generations. `--schema` and `--url` mean it runs against a scaffolded app with no config file at all, and the positional config path still works. **Litestream replicates SQLite, so a jsonl or logger database CANNOT be covered** — reported by name with what to use instead, because a replication report listing only what it did reads as though it did everything. Resolution moved to `cli.js`, which owns `loadSchema`/`createClient`; `replicate.js` is the litestream driver and no longer parses a transform-pipeline config. Verified against a three-database schema with a stub binary, asserting the generated YAML | 2026-08-14 | [replicate.js](packages/litestone/src/tools/replicate.js) · [cli.js](packages/litestone/src/tools/cli.js) · [FJS-246](#) |
| FJS-044 | junction — **a filtered bulk PATCH walked past the state machine, and neither PATCH nor DELETE could report a per-row failure.** The row was filed as an ergonomics gap; probing it found an enforcement one underneath. Litestone does not run `@@transitions` on `updateMany` — deliberately, as a power tool whose caller "takes responsibility", with its own note saying to loop `update()` where transition safety matters — and Junction's bulk patch called `updateMany` directly, so that responsibility was never taken. Verified through a real Junction service over a real Litestone client: `PATCH /orders/1 {status:'shipped'}` was refused `TransitionViolationError` and `PATCH /orders?status=draft` with the identical move answered `{count:2}` and wrote both rows. `@version` went the same way — bumped on a bulk write, never required — so optimistic concurrency was off for exactly the writes touching the most rows. Both are properties of `update()`, so both return by calling it: a filtered patch/remove now selects its targets and writes them one at a time, answering the same `{data, errors}` list envelope bulk create already did. Gate and row policy were always enforced on the bulk path and are unchanged; cascade on `removeMany` was correct too | **2026-08-14** | [litestone.ts](packages/junction/src/core/litestone.ts) `bulkByRow` · ruled by [FJS-D11](DECISIONS.md) · 15 tests in [bulk-partial-success.test.ts](packages/junction/tests/bulk-partial-success.test.ts) |
| FJS-245 | junction — **every filtered restore was a 500, on every service, since the method was written.** `PUT /{service}?filter` reached `restoreImpl`'s bulk branch, which called `table.restoreMany({ where })` — a function a Litestone table does not have. `LitestoneTable` declared it, so nothing typed the mistake, and a Litestone client throws on an unknown property rather than answering undefined, so the symptom was `TypeError: table.restoreMany is not a function` rather than a quiet no-op. Found while making bulk patch/remove answer rows, by asking what the third filtered write does. `table.restore({ where })` already takes a multi-row where and answers the rows, so the fix is one call and it lands restore in the same envelope; the phantom declaration is gone from the interface. No app in the repo sets `allowBulk`, which is why no drive ever hit it | **2026-08-14** | [litestone.ts](packages/junction/src/core/litestone.ts) `restoreImpl` · 2 tests in [bulk-partial-success.test.ts](packages/junction/tests/bulk-partial-success.test.ts) |
| FJS-244 | litestone — **Two concurrent `$transaction` calls silently became one, so a second request's writes rode the first request's rollback.** The depth counter is per CLIENT ([client.js:6735](packages/litestone/src/core/client.js#L6735)) and one connection holds one transaction, so *am I nested?* could not be answered by the counter: a second REQUEST arriving while the first awaited looked exactly like a genuinely nested call and was treated as one. Reproduced before fixing — A opens `BEGIN IMMEDIATE` and awaits; B sees `depth > 0`, takes a `SAVEPOINT` **inside A's transaction**, commits it and is told it succeeded; A rolls back and B's rows are gone. B could also read A's uncommitted rows, because the read router sends every read to the write connection while `depth > 0`. Latent only because few call sites use `$transaction` — basecamp has three, including `/setup`. **Closed by asking the async context instead of the counter**: a module-level `AsyncLocalStorage` holds the `txState` objects the current context owns, so a nested call (which runs inside the outer callback and inherits the store) still takes a SAVEPOINT, and anything else waits on a per-client FIFO lock. Both halves are load-bearing in opposite directions — serializing everything would deadlock `/setup` four models deep, nesting everything is the original defect. `tx.wrap`, the SYNCHRONOUS batch wrapper behind `createMany`/`upsertMany`, had the same hole and now awaits the acquire while its body stays sync. This serialises only what SQLite already serialises: two `BEGIN IMMEDIATE`s cannot overlap on one connection, and the old code avoided the error by enrolling the second caller in a transaction it could not see | 2026-08-14 | **fixed 2026-08-14** · [makeTxManager](packages/litestone/src/core/client.js#L1769) · [litestone.test.ts § $transaction under concurrency](packages/litestone/test/litestone.test.ts) |
| FJS-232 | cli — **`make:deploy` wrote a Dockerfile against a layout `fli new` does not produce** | 2026-08-13 | It copied `api/package.json`, `api/bun.lockb*` and `api/tsconfig*.json`, ran `bun run src/server.ts`, and never copied `db/` — while the scaffold writes ONE manifest at the app root, `api/index.ts` as the entry, and the schema under `db/`. **The root `README.md` § Project Structure already ruled this** (Invariant 3), so the decision was to make the template obey the canonical layout rather than move the app: `COPY package.json bun.lock* ./`, `COPY api ./api`, `COPY db ./db`, `CMD bun run db:migrate && bun run start`. `db/` is load-bearing twice over — the entrypoint migrates and the pre-swap backup runs `litestone backup`, and both resolve databases by reading the schema. `fli new` now writes the `db:migrate` and `db:backup` scripts the entrypoint calls (`--schema db/schema.lite`, which also fixes the migrations directory since litestone resolves it as a sibling), and `deploy:doctor` checks the ROOT manifest for `db:migrate` + `start` and warns when the Dockerfile has no `COPY db`. **Verified by building it**: the image builds, `bun run db:migrate` resolves the schema and creates the database inside the container, and the app boots and answers its health endpoint. Two things came out of running it — the doctor rewrite left a dangling `apiPkg` reference that only execution caught, and `FJS-241`, which is why nobody had run this path before · **closed 2026-08-13** \| 2026-08-13 · [make/deploy.md](packages/cli/commands/make/deploy.md) · [project/new.md](packages/cli/commands/project/new.md) · [doctor.md](packages/cli/commands/deploy/doctor.md) |
| FJS-240 | litestone — **`litestone backup` reported `✓ backup complete` and exited 0 after skipping a declared database** | 2026-08-13 | Every arm of `cmdBackup` logged its failure and continued: a `$backup` throw, a logger directory that is not there, a failed zip. The summary line then printed unconditionally. Found while pointing the deploy's pre-migration snapshot at it (`FJS-239`) — from the wrong directory it copied `main`, warned on `audit`, said complete and returned 0, which is the exact failure the snapshot exists to prevent, and over SSH nobody reads the ⚠. **Database paths resolve against the process CWD, not the schema file** — documented in `example/db/schema.lite`'s own comment — so the wrong cwd is a partial backup rather than an error. Closed by collecting what did not make it and refusing at the end: `✗ backup INCOMPLETE`, each database named with its reason, *whatever was written is a PARTIAL copy*, exit 1. Verified both ways against `example` — wrong cwd exits 1, `--schema db/schema.lite` from the app root backs up `main` + `audit` and exits 0 · **closed 2026-08-13** \| 2026-08-13 · [cli.js cmdBackup](packages/litestone/src/tools/cli.js) |
| FJS-239 | cli — **the pre-deploy backup needed a host binary nobody installed, and could only ever have copied one of the databases** | 2026-08-13 | `05-backup` shelled out to `sqlite3 … '.backup …'`; `deploy:setup` installed docker/nginx/git/bun/rsync and never sqlite3, and the step is `optional` — so on a server this tool provisioned, the snapshot warned once and the deploy carried on into `06-swap`, whose container runs migrations in its entrypoint. The one step whose purpose is to run before something irreversible was the one not running. **And the config shape could not have been right anyway**: `deploy.db.file` names ONE file, while `example` and `basecamp` both declare `main` plus an `audit` logger — so the trail nobody may lose was the part not copied. **Closed by having the app back itself up**: `docker exec {appId}-api … litestone backup`, run before the swap so it captures the OLD container while it is still serving. Litestone already owned this — `$backup` is a hot copy safe under active writes and `litestone backup` walks every declared database including the JSONL/logger directories. No host binary, nothing reimplemented. First deploy has no container and skips by name. `fli db:backup` had the same bug plus hardcoded `development.db`/`test.db` — names the CLI invented, since a litestone app's paths come from `database` blocks — and now delegates too. `sqlite3` stays in `deploy:setup` for an operator's shell, with the comment no longer claiming the pipeline needs it. Exposed `FJS-240` on the way · **closed 2026-08-13** \| 2026-08-13 · [05-backup.md](packages/cli/commands/deploy/_steps-docker/05-backup.md) · [db/backup.md](packages/cli/commands/db/backup.md) |
| FJS-238 | cli — **`make:deploy` pointed the health check at `/health` while the scaffold serves `/api/health`, so the pipeline rolled back deploys that worked** | 2026-08-13 | `healthPlugin()` registers through `app.get()`, the one owner of `apiPrefix`, so a prefixed app answers at `{prefix}/health` and **nothing written through the shortcuts can answer at a bare `/health`** — the remedy `make:deploy` printed could not produce the path `make:deploy` configured. `07-health` polled the 404 for 20s, then stopped the new container and restored `_replaced`, reporting a healthy API as a failed deploy after steps 01–06 had all passed. **Closed by resolving the prefix rather than defaulting it**: `resolveHealthPath()` reads `apiPrefix` from `api/src/app.ts` then `api/config/junction.config.js` (createApp merges opts over the file, so app.ts wins) and writes the full path into the deploy block, naming the source in the output. Three downstream copies of the wrong assumption went with it — `deploy:doctor` only accepted `healthPlugin()` as evidence when the path was literally `/health`, so it warned on every prefixed app; and `04-build-api`, `deploy:local` and `doctor` all defaulted `dockerfile` to `api/deploy/Dockerfile`, which `make:deploy` has never written. `07-health` now prints the URL it polled and points at `apiPrefix`, because a rollback that names nothing reads as the app's fault. Verified by scaffolding both shapes and running `make:deploy` + `deploy:doctor` against them · **closed 2026-08-13** \| 2026-08-13 · [make/deploy.md](packages/cli/commands/make/deploy.md) · [07-health.md](packages/cli/commands/deploy/_steps-docker/07-health.md) · [doctor.md](packages/cli/commands/deploy/doctor.md) |
| FJS-237 | cli — **a step that threw skipped the cleanup step written to run on failure, leaking the deploy lock** | 2026-08-13 | `09-cleanup` declares `runOnAbort: true` so the lock is released on a bad deploy, but the runner only honored it for the `context.config.abort` *flag*: `runOneStep` re-threw for any non-`optional` step and no `try`/`finally` wrapped the group loop, so the throw exited `runSteps`. `07-health` sets the flag **and then throws**, so the throw won in the one place the flag existed for — and `{serverPath}/.deploy.lock` survived, making the next deploy refuse while naming one that had finished minutes earlier. **Closed in the runner, not the steps**: a throw now records the error, sets `abort`, and lets the loop continue — every remaining step self-skips unless it declared `runOnAbort` — then re-throws after the loop so the exit code and message are unchanged. `runOnAbort` now means *runs on abort or throw*, for every `_steps` command. Pinned by `tests/fixtures/cleanup-on-throw/` + scenario 11 in `zz-steps.test.js`: cleanup runs, the ordinary step in between does not, and the command still fails · **closed 2026-08-13** \| 2026-08-13 · [core/runtime.js](packages/cli/core/runtime.js) · [zz-steps.test.js](packages/cli/tests/zz-steps.test.js) |
| FJS-017 | junction — **Two pipeline systems, one vocabulary — three rate limiters, and the copies had already drifted.** The row named three symptoms. **One:** `rateLimit` (transport middleware) took `limit` / `keyFn` / `window: 60_000`, `rateLimitHook` (pipeline) took `max` / `key` / `window: '15 minutes'` — same algorithm, so learning one taught you nothing about the next. **Two:** `@frontierjs/auth` carried a third copy, on the stated grounds that the shared hook *"operates on ServiceContext, which has `ctx.params.ip`"* — a ServiceContext has no `params` at all, so the premise was wrong and the real gap was one accessor. That copy had drifted: it returned before checking the limit on a fresh bucket, so `max: 0` let one request through. **Three:** `apiPrefix` hand-resolved in four files with two defaults, which closed separately as [FJS-012](#). **Closed as one definition and three adapters.** `core/rate-limit.ts` owns counting, the window, the sweep, the teardown and the refusal; the hook, the middleware and auth differ only in how they read a key and whether they can set headers — the transport tier keeps `x-ratelimit-*` because it runs where a response is still being assembled, and it is the tier that can count a flood which never reaches a service. One vocabulary (`max` / `window` / `key` / `message` / `skip`), `window` now taking a TTL string at both tiers, and `retryAfter` on the 429 from both. `clientIp(ctx)` reads either context shape — a TransportContext carries `ip`, a ServiceContext splits client facts into `ctx.client` — and the hook reaches `auth` optionally, because a sign-in route has no principal by definition. **The old option names throw rather than being ignored**: a silently dropped `limit:` would leave `max` undefined, and `count > undefined` is never true, so the limiter would accept everything and say nothing. **Not done, and deliberately**: middleware and hooks are NOT merged into one concept — [FJS-D01](#) refused that once already, and the tiers differ for a reason the transport limiter demonstrates. Baseline 199 → 198 | 2026-08-13 | **fixed 2026-08-13** · [core/rate-limit.ts](packages/junction/src/core/rate-limit.ts) · [tests/rate-limit.test.ts](packages/junction/tests/rate-limit.test.ts) · [auth/plugin.ts](packages/auth/plugin.ts) |
| FJS-225 | junction/cli — **A second registration of the same route was accepted in silence, and which copy survived depended on something the caller could not see.** A FIXED path is written into a map by `build()`, so the LAST registration won; a DYNAMIC one is pushed onto a list `lookup()` scans in order, so the FIRST won and the later handler never ran. Same mistake, opposite outcome, decided by whether the path happens to contain a parameter. It surfaced as doubled CORS — `cors()` registers `OPTIONS /*`, and `fli new`'s own scaffold called it by hand beside the `middleware.cors` config entry that installs it at startup, so every app FJS scaffolded ran doubled CORS from its first boot. The headers came out right, which is why nobody looked; found by running `fli api:routes` against a fresh scaffold ([FJS-091](#)), where the duplicate is two identical lines. **Closed with the general fix rather than making `cors()` idempotent**: `Router.add()` refuses a duplicate by name at REGISTRATION time, so the stack points at the caller rather than at startup. Keyed on the route's SHAPE, not its literal path — `/a/{id}` and `/a/{name}` accept exactly the same requests and differ only in a name nothing outside the handler reads — with each segment carrying its type, because `{}` is a legal static segment (`PARAM_PATTERN` needs a character between the braces) and a placeholder spelt `{}` made `/a/{}` collide with `/a/{id}`. The scaffold half was already fixed 2026-08-13; this closes the framework half, and it covers any plugin claiming a path another already owns | 2026-08-13 | **fixed 2026-08-13** · [router.ts](packages/junction/src/transport/router.ts) · [tests/duplicate-routes.test.ts](packages/junction/tests/duplicate-routes.test.ts) |
| FJS-045 | junction — **A service declaring `channel:` AND running the `publish()` hook broadcast every mutation twice, and the register's own remedy was "grep before merging".** Two mechanisms announce: `channel:` is sent by `callService` at the single announcement point, and the exported `publish()` hook sends its own frame from `after`. Carrying both puts the same record on the wire twice, every subscribed tab applies it twice, and a non-idempotent client handler — an append, a counter, a toast — shows it twice. **Verified unrealised before fixing**: 17 basecamp services use the hook and not one also declares `channel:`, so nothing doubles today and nothing stopped it either. **Closed by asking the question where it can be answered.** `publish()` marks the hooks it produces in a `WeakSet`, and `svc.pipelines()` — the one place the FULL effective chain is known, since it merges the app-level map — refuses a service that carries both, naming the method. App-level `after: { all: [publish(…)] }` is the shape that would have doubled a whole app at once, and it is caught. Marked hooks and never names: an app may call its own hook `publish`, and suppressing a real one on a name collision would silently stop broadcasting, which is this defect inverted | 2026-08-13 | **fixed 2026-08-13** · [service.ts `refuseDoubleBroadcast`](packages/junction/src/core/service.ts) · [channels.ts `isPublishHook`](packages/junction/src/transport/channels.ts) · [tests/double-broadcast.test.ts](packages/junction/tests/double-broadcast.test.ts) |
| FJS-211 | litestone — **`@encrypted(searchable: true)` was one-way — the plaintext was destroyed on write and could never be read back — and the documentation said the opposite.** The column held `v1s.<base64url(HMAC-SHA256)>` and nothing else, so an `asSystem()` read handed back the digest, `$rotateKey` could not re-key it, and [encryption.md](packages/litestone/docs/encryption.md) claimed the HMAC was stored *alongside the ciphertext* — with `email` as its example. **Closed by replacing the attribute with two that each say what they do**, on the axis that actually decides it: `@encrypted(deterministic: true)` (AES-256-GCM, IV derived from the plaintext under its own salt — equality-filterable **and** readable, rotatable, the construction Rails ships) and `@hashed` (HMAC-SHA256, one-way by name — matchable, never readable, not rotatable, and refusing to compose with `@encrypted`/`@secret`/`@guarded`/`@allow`). Every read path refuses a digest including `asSystem()`, and `select`/`groupBy`/`aggregate` now **throw** rather than projecting the column — those three answered the digest, which is exactly how a value that looks like data gets displayed, mailed and written into the next table. `@secret(deterministic: true)` composes, and `$rotateKey` re-encrypts in the mode the field was DECLARED with. The old spelling is a parse error naming both replacements rather than a silent translation. **A column already holding `v1s.` values is unrecoverable** — an HMAC has no inverse, so nothing reads the prefix. The pinning test at `litestone.test.ts:4854` now asserts the address reads back; the crossing matrix gains a `hashed` kind whose write cells verify by matching, since reading back is the one thing that column does not do | 2026-08-13 | **fixed 2026-08-13** · [CHANGES.md](packages/litestone/CHANGES.md) · [encryption.md](packages/litestone/docs/encryption.md) · [FJS-236](#) |
| FJS-229 | litestone — **`@@order(name, [col dir nulls, …])` — a named sort order, columns only.** Ruled in [DECISIONS.md](DECISIONS.md) 2026-08-13. "Most urgent first" is three keys with NULLS placement and is otherwise copy-pasted into every call site, where it drifts; declaring it once gives the rule one owner. Columns only on purpose — the expression form would make an order bind parameters, the plumbing that made `FJS-224` hard. Verified that the three-key form already produces the wanted order against a real client, so this is ergonomics over a working mechanism, not new capability **Closed without building it.** A named order is a scope carrying only `orderBy` — `db.task.urgent()` returns the three-key NULLS-placed order today, verified. The row was filed before that was known. | 2026-08-13 | **already exists 2026-08-13** · [DECISIONS.md](DECISIONS.md) |
| FJS-231 | junction — **Every autoloaded `createBaseService` service ran its gate and its validator twice per request.** `createBaseService` returns the MERGED hook map; the autoloader spreads a base back through `createService`, which forwarded that map into a second `createBaseService`, which appended the derived layer again — `before.find` was `[gateAuth, autoFilter, autoSort, gateAuth, autoFilter, autoSort]`. Not wrong on the wire, which is why nothing caught it: two `@@gate` grades, two schema validations and two `$checkWhere`/`$checkOrderBy` round trips per request to every one-line service, the shape the framework recommends. **Closed by MARKING the four derived hooks** (a WeakSet, so nothing a spread or a serialiser can see) and skipping one already present among marked hooks. Marked, never matched by name: a user hook called `gateAuth` is not ours, and letting it suppress the real one is fail-open on access. Found while planning [FJS-D01](#) | 2026-08-13 | **closed 2026-08-13** · [service.ts derivedHooks](packages/junction/src/core/service.ts) · [derived-hooks-once.test.ts](packages/junction/tests/derived-hooks-once.test.ts) |
| FJS-016 | junction — **Service conflated definition with runtime.** One object carried the declaration, the runtime methods, the compiled hook state and — through `[method: string]: unknown` — every custom action, so "is this key an option or an action" was answered by exclusion and six consumers re-applied that rule. **Closed by the split ruled in [FJS-D01](DECISIONS.md).** `methods:` now DECLARES (Feathers 5's shape) and the scan is the fallback; one `collectActions` step builds `_actions` at construction and dispatch, `/manifest`, the OpenAPI spec and `/metrics` read the table. `describe()` is the one answer to *what is this service*, so those three readers stopped reaching into `_meta`/`_schemas`/`_hookMap`. `pipelines(appHooks)` replaced the hand-invalidated cache, the registry's `hooks()` monkey-patch and the three-way ladder — memoised on both inputs, so staleness is unreachable rather than remembered. `Service` lost its index signature (`ServiceDefinition` keeps it — you *write* actions there), which is what took the baseline from **211 to 199**. Inline actions stay supported and no app was migrated | 2026-08-13 | **closed 2026-08-13** · [service.ts](packages/junction/src/core/service.ts) · [service-actions.test.ts](packages/junction/tests/service-actions.test.ts) · [service-describe.test.ts](packages/junction/tests/service-describe.test.ts) |
| FJS-D01 | junction — **The structural refactor: go or no go?** ~~Open~~ **Ruled 2026-08-13: go on the definition/compiled split ONLY.** No export tiering ([FJS-046](#)), no middleware/hook renaming ([FJS-017](#)) — both stay open on their own merits. Inline action keys are supported permanently and apps are not migrated. Closed [FJS-016](#); `DECISIONS.md` § API design | 2026-08-13 | **closed 2026-08-13** · [DECISIONS.md](DECISIONS.md) |
| FJS-227 | litestone — **`now()` binds a different instant for every occurrence in one expression.** `policy.js` compiles the `now` node by pushing `new Date().toISOString()` at the point it is reached, so `@@allow('read', startAt < now() && now() < endAt)` binds two timestamps microseconds apart, and `evalJs` calls `new Date()` again on its own side. Harmless for an access check and wrong in principle; fatal for the reporting shape `FJS-D28` rules for, where a query has to have one "as of" instant or its rows can satisfy a contradiction and its totals will not reconcile with a re-run. **One resolution per request**, cached on the ctx, is also the only place an injectable clock can hang — a frozen `now` is what makes a time-dependent test deterministic and a report reproducible. Blocks `@@scope`/`@@order` ([DECISIONS.md](DECISIONS.md)), which multiply the number of `now()` occurrences in a single statement | 2026-08-13 | **closed 2026-08-13** · [DECISIONS.md](DECISIONS.md) |
| FJS-D28 | litestone — **A derived field that depends on the clock has no tier.** `urgency`, `pastDue`, `isSnoozed`, `inProgress`-style fields are the ones a task list is *sorted and filtered by*, and they are the ones litestone cannot express anywhere but JS-after-the-read. `@generated` is out by SQLite's own rule — `CASE WHEN dueAt < datetime('now')` is refused at DDL with *non-deterministic use of datetime() in a generated column*, verified — and `@from` aggregates a relation rather than a row. So they land on `@computed`, which cannot be filtered, cannot be sorted, and therefore cannot be paginated by: a screen that says *overdue first, 20 per page* has to read the whole table and sort in JS, which is the shape every one of these apps needs on its main list. Deterministic derivations already have a home and it works — a `@generated` CASE over columns is filterable and sortable, verified. The question is only what to do about `now()`. Candidates: a declared **parameterised** derivation the query planner binds the clock into at read time (the WHERE knows `now`, so `orderBy: { urgency: 'desc' }` compiles); a **materialised** column plus a sweep that keeps it fresh, which trades staleness for indexability; or a blessed `now()` inside `@from`-style `where:` strings so at least filtering works. Ruling matters beyond tasks — due dates, trials, subscriptions, SLAs and retention are all this shape | 2026-08-12 | **ruled 2026-08-13 — [DECISIONS.md](DECISIONS.md)** · [DECISIONS.md](DECISIONS.md) |
| FJS-084 | junction — **The browser client cannot ask for a relation.** `FindParams` was `{query, offset, limit, orderBy, select}` while the bridge parsed `$populate` and the service destructured `include` — so the wire and the server both supported it and only the client could not say it, which meant a component could not declare its own data shape and over/under-fetching was decided server-side in a hook. **Closed by adding `populate` to `FindParams`**, emitted by both builders (`$populate` on the query string and on the WS frame — the client prefers the socket whenever one is up, so a directive on one path is a difference nothing can see from the call site). The server's own spelling travels unchanged — `'customer'`, `['a','b']`, `'customer:name+email'` — so there is one grammar rather than a client dialect. **A by-id `get` carries params too**: they were accepted and dropped on that path, which is the shape a detail page wants most. Sierra's `createResource` needed no change — `findParams` was already threaded — only its docs. 7 tests, the server half against a real Litestone client with a real relation, because a plain-object fake cannot have one | 2026-08-13 | **closed 2026-08-13** · [client/index.ts FindParams](packages/junction/src/client/index.ts) |
| FJS-091 | junction/cli — **There is no way to ask what the HTTP surface is.** `routePaths(method)` existed and nothing assembled it; `fli` had no command that called it. The surface is emergent — services auto-mount, plugins register their own — and `hasRoute()` is a *matching* question, so "is this mounted" had no cheap answer. **Closed with `buildRoutes(app)` and `fli api:routes`.** `buildRoutes` reads the router rather than rebuilding from the registry, so a path appears whether or not anything meant to mount it, and splits `service` (the `{service}` templates) from `raw` (what a plugin or the app registered) because the second is the surface nothing else describes. It rides `/manifest`, so a running app can be asked over HTTP; `fli api:routes [--raw] [--method] [--json]` prints it. **`manifestPlugin()` is now in the scaffold and in `example/`**, which is what makes the command work on a fresh app rather than being a command about a plugin nobody configures. Verified against both. It earned its keep on first run: two identical `OPTIONS /*` routes on a scaffolded app — [FJS-225](#) | 2026-08-13 | **closed 2026-08-13** · [manifest/index.ts buildRoutes](packages/junction/src/plugins/manifest/index.ts) · [commands/api/routes.md](packages/cli/commands/api/routes.md) |
| FJS-143 | junction — **`IEventBus` can say whether anything is listening, never how much.** `hasListeners(event?)` was the only introspection there was, and the `handlers` Map is closure-private — so *the bus is idle* and *four things are subscribed to three events* were the same answer, which is the wrong resolution for anyone chasing a missing announcement. **Closed with `stats()` → `{ events: Record<string, number>, total }`**, alongside `hasListeners` rather than replacing it. An event whose last handler unsubscribed is omitted rather than reported as zero (`off()` deletes the handler and leaves the Set), and the two wildcard channels keep their own keys, because a subscriber to everything is a different fact from a subscriber to one thing. Basecamp's hub card stated the gap on the screen; it now prints the number. 5 tests | 2026-08-13 | **closed 2026-08-13** · [events/index.ts](packages/junction/src/events/index.ts) · [hub/index.mesa](packages/basecamp/web/src/routes/hub/index.mesa) |
| FJS-222 | litestone — **A row from `@from(X, last: true)` silently carries none of X's own derived fields.** `account.lastOrder.lineCount` is `undefined` where a direct read of the same `Order` answers `2`, and an `include: { orders: true }` of the same rows answers `2` — so the field exists on the model, resolves everywhere else, and is absent only here. By construction: the subquery builds `json_object(...)` from the target's **scalar** fields, filtering out `@computed`, `@from`, `@generated` and `@funcCall`, and a `@from` cannot appear in it because SQLite takes no correlated subquery inside `json_object` correlated to a row the outer query has not yet named. The value is not wrong, it is missing, and missing reads as `undefined` — which for a count is indistinguishable from a field the caller misspelled, and one arithmetic step later is `NaN`. `first`/`last` is the one `@from` shape that returns a whole row, so it is the one that can disagree with a direct read of that row. Three ways out and none is obviously right: hydrate the derived fields in JS after the read, the way `hydrateFromFields` already does for a write's `RETURNING` (one extra SELECT, only for a target that declares `@from`); or refuse at parse time when the target declares `@from`, which is honest but takes away a useful field for a reason the caller did not cause; or keep it and make the absence loud by throwing on the read of a field the row could never carry, which is the `@computed` `needs` treatment. **Pre-existing, not introduced by the alias fix** ([FJS-220](#)) — verified against the same schema before and after | 2026-08-12 | **closed 2026-08-13** · [client.js:813](packages/litestone/src/core/client.js#L813) · [FJS-220](#) |
| FJS-223 | litestone — **`@from(X, first/last: true)` returns the target row's `@guarded(all)`, `@omit(all)` and `@encrypted` fields to any caller.** Verified on a `$setAuth({ role: 'member' })` client, not the root and not `asSystem()`: a direct read of the `Order` answers `{id, accountId, amount, lineCount, label}` and the same row reached through `account.lastOrder` answers `{…, secretNote: "CONFIDENTIAL", hidden: "HIDDEN", card: "v1.…"}` — a field declared `@guarded(all)`, one declared `@omit(all)`, and the raw ciphertext of an `@encrypted` column, which is `@guarded(all)` by implication. The whole-row shape is built as `json_object('field', _from."field", …)` over the target's fields, and that list filters out `@computed`, `@from`, `@generated` and `@funcCall` — the **virtual** attributes — while `@guarded`, `@omit`, `@encrypted` and field-level `@allow` are protections applied by `read()`, which this path never reaches. So the one `@from` shape that returns a row is the one that returns it raw. Breaks Invariant 6: the protection is declared at the Data boundary and a second SELECT path walks around it, the same shape as [FJS-216](#) and with the same cause — a hand-built SQL path that reimplements part of a read. **[FJS-222](#) is this construction seen from the other side**: the fields it wrongly omits (`@computed`, `@from`) and the fields it wrongly includes are one list, applied once, by a function that is not `read()`. One fix answers both — resolve `first`/`last` as a real read of the target rather than a JSON encoding of its columns. Not reachable over HTTP unless an app declares a `first`/`last` `@from` on a model with a protected column; nothing in this repo does, which is why it survived | 2026-08-12 | **closed 2026-08-13** · [client.js:813](packages/litestone/src/core/client.js#L813) · [FJS-222](#) |
| FJS-012 | junction — **No `apiPrefix` on plugin routes.** A plugin calling `app.get()`/`app.post()` landed at the root while every service beside it moved, so an app with `apiPrefix: '/api'` served its login at `/auth` — and four in-repo plugins (webhooks, manifest, openapi, channels) each hand-resolved `app.config.apiPrefix` to compensate, which `@frontierjs/auth` did not and could not know to. **Closed by giving the prefix ONE owner**: `app.get/post/put/patch/delete` apply it (`core/app.ts`, `normalizePrefix` + `prefixPath`), `registerServiceRoutes` no longer does — it registers bare `/{service}` paths like everyone else — and the four hand copies are deleted. A route that must sit at a fixed path goes to `app.http.router` directly, the layer beneath the shortcuts, which applies nothing. The browser client's `authPrefix` now composes with `apiPrefix` (`/api` + `/auth` → `/api/auth/login`) because the plugin's option is relative to the app's, the same as it is on the server. Ripples through the apps that set a prefix, which is the fix working: `example` moved `/auth`, `/session`, `/jobs`, `/health` under `/api`, one vite proxy entry replaced four, and `fli new`'s scaffold and caravan's `CLAUDE.md` said the old thing. Proven by junction 942 pass + a new test asserting a plugin route moves and the unprefixed path 404s, then `example`: `verify` 37/37, `verify:live` 14/14, `verify:jobs` 8/8 against a restarted API | 2026-08-13 | **closed 2026-08-13** · [app.ts route shortcuts](packages/junction/src/core/app.ts) · [client/index.ts:410](packages/junction/src/client/index.ts#L410) |
| FJS-013 | junction — **A model service whose `$defs` lookup misses accepts unvalidated input, silently.** The accessor half was already fixed; what remained was that `autoValidate` stored `null` for a miss and said nothing — neither a throw nor a warning — so a service reaching no definition enforced no `@email`, `@length` or `@gte` and reported it nowhere. **Closed by making the silence impossible**: both `null` branches (no matching definition, and a definition that would not compile) now warn once per model, naming the service, the spellings tried and, for a compile failure, the compiler's own message. It warns only when the accessor DOES resolve to a table on the client — a service with no model at all is a supported shape (custom actions only, or its own `create()`), and `getTable`'s diagnostic already covers a genuinely missing model — so the warning means *this write reached a model and nothing checked it*. 4 tests, including the two negative controls that keep it quiet | 2026-08-13 | **closed 2026-08-13** · [litestone.ts `_warnUnvalidated`](packages/junction/src/core/litestone.ts) |
| FJS-088 | junction — **The inbound idempotency key is plumbed and inert.** `ctx.idempotencyKey` was read off the header, declared on the request-metadata type, and consumed by nothing, so a double-submitted create ran twice while the request carried the value that says not to — with Conduit spending the opposite side of the same contract outbound. **Closed by implementing it**: `core/idempotency.ts` claims a key at `callService`, the one path every transport takes, so a repeat replays the first call's result without running the pipeline — no second hook, no second write, no second announcement. The key is scoped to `(service, method, principal, key)` because replay skips the pipeline and therefore the auth checks in it; a failed call RELEASES the key, since a failed request is one the caller may retry; and a duplicate arriving while the first is in flight is a **retryable** 409 rather than a parallel execution. The in-flight marker carries its own short TTL (2 minutes, default) so a throw between claim and settle cannot leave a key answering 409 for a day. Config is `config.idempotency` (`enabled`/`ttl`/`pendingTtl`), on by default and inert unless a caller sends a key. **The WebSocket path now establishes request metadata at all** — it wrapped nothing in `runWithMeta`, so `requestMeta()` was undefined for every socket call and the correlation id was as HTTP-only as the key; the frame carries both under `meta`. 11 tests | 2026-08-13 | **closed 2026-08-13** · [core/idempotency.ts](packages/junction/src/core/idempotency.ts) · [service.ts callService](packages/junction/src/core/service.ts) |
| FJS-220 | litestone — **`@from` on a self-relation counts nothing, because the subquery's correlation is shadowed by its own FROM.** `model Task { children Task[]  childCount Int @from(Task, count: true) }` reads `0` on a parent with two live children. The emitted SQL is `SELECT "task".*, (SELECT COUNT(*) FROM "task" WHERE "taskId" = "task"."id") AS "childCount" FROM "task"` — and inside that subquery `"task"."id"` resolves to the **inner** `task`, the subquery's own FROM, not the outer row. So it counts rows whose `taskId` equals their own `id`: self-parented rows, of which there are none, hence 0 for every row in every schema. Proven in raw SQLite: the identical statement with the inner table aliased (`FROM "task" AS _f WHERE _f."taskId" = "task"."id"`) answers 2. **The FK inference is not at fault** — `inferFromFk` returns `{fkCol: 'taskId', refCol: 'id'}`, which is the correct correlation for counting children; an earlier note here blamed relation resolution and was wrong. The fix is one line in `buildFromMap`: alias the target table in every `@from` subquery, self-referential or not, so a correlation can never be captured by its own scope. It touches every `@from` in the repo, which is the reason to do it once rather than only when `targetTable === selfTable` — two code paths for one rule is how the next crossing gets missed. Separate from [FJS-221](#), which is about *which* relation an `@from` means | 2026-08-12 | **closed 2026-08-12** · [client.js:798](packages/litestone/src/core/client.js#L798) · [parser.js inferFromFk](packages/litestone/src/core/parser.js) |
| FJS-221 | litestone — **`@from` picks the first relation that fits and never says which — so with two relations between the same pair of models, one of them is unaskable.** `model User { sent Message[] @relation("sent")  received Message[] @relation("received")  msgCount Int @from(Message, count: true) }` compiles without complaint and counts **sent**: verified, a user who sent 2 and received 1 reads `msgCount: 2`, and there is no spelling of `@from` that asks for received. `inferFromFk` returns on the first field of the target whose type is this model and which owns a FK, and a second candidate is not an error, not a warning, and not visible in the value. Nothing about the read distinguishes *the count you meant* from *the other one*. Two halves, and both are needed: an ambiguous `@from` should be **refused at parse time**, naming the candidates, because picking one silently is the failure; and there should be a way to say which — `@from(Message, count: true, via: relationField)`, mirroring `recursive`'s `via:`, which solved the same question for the tree walk. Self-relational models make it routine rather than exotic — `parent`/`children` is two relations to one model before an app has written anything unusual — but the sender/recipient case above has no self-reference in it at all. Distinct from [FJS-220](#), which is a correlation bug under whichever relation gets chosen | 2026-08-12 | **closed 2026-08-12** · [parser.js inferFromFk](packages/litestone/src/core/parser.js) · [client.js:784](packages/litestone/src/core/client.js#L784) |
| FJS-216 | litestone — **`findMany({ recursive })` is a second implementation of `findMany`, and it enforces no access control past the anchor row — `@@gate` is never consulted at all, and `@@allow` filters only the starting row, so a caller reads an entire subtree of a model it is refused on.** Both verified by running: on `@@gate("5.5.5.5")` an anonymous `db.task.findMany()` throws `ACCESS_DENIED` and `db.task.findMany({ where: { id: 1 }, recursive: true })` returns the child row's contents; on `@@allow('read', ownerId == auth().id)` a caller who owns row 1 reads `[1]` normally and `[2, 3]` — both owned by someone else — recursively. One root cause with several faces: the CTE path at [client.js:3837](packages/litestone/src/core/client.js#L3837) builds its own SQL and returns before the plugin runner is ever reached, so `GatePlugin.onBeforeRead` does not run; and the policy filter it does build is composed into the CTE's **anchor** SELECT only ([client.js:3867](packages/litestone/src/core/client.js#L3867)), never into the recursive step or the outer join, so every row below the anchor arrives unfiltered. It fails **open**, the opposite direction from [FJS-214](#), and it breaks Invariant 6 — access is declared in the schema and enforced at the Data boundary — for one method's one option. **Not reachable over HTTP today**: junction forwards no `recursive` directive and nothing in this repo calls it, which is the only reason this is a hole waiting for its first caller rather than a live exposure. The fix is not to thread the three missing filters through: it is that a second SELECT path exists at all. Either the recursive step carries the same policy SQL and the whole path runs through the plugin runner like every other read, or `recursive` becomes a where-shape the normal query pipeline compiles. Same family as [FJS-217](#), [FJS-218](#), [FJS-219](#) | 2026-08-12 | **closed 2026-08-12** · [client.js:3837](packages/litestone/src/core/client.js#L3837) · [gate.js:305](packages/litestone/src/plugins/gate.js#L305) |
| FJS-217 | litestone — **A recursive read returns soft-deleted rows, because the `@@softDelete` filter is applied to the anchor and to nothing below it.** On a three-row chain 1 → 2 → 3 with row 2 soft-deleted, `findMany()` answers `[1, 3]` and `findMany({ where: { id: 1 }, recursive: true })` answers row 2 *carrying its `deletedAt`* plus row 3. So the one method that walks a tree is the one that shows deleted rows, and it shows them with no flag asked for — `withDeleted` is not what produced them. Same line as [FJS-216](#): `injectSoftDeleteFilter` composes into the CTE's anchor SELECT only. There is a real question underneath the bug, which the fix has to answer rather than assume: whether a soft-deleted node should hide its whole subtree (a deleted parent's children are unreachable) or only itself (children reparent to the tree). Litestone's `@@softDelete(cascade)` already takes the first position for writes, so the read should match it | 2026-08-12 | **closed 2026-08-12** · [client.js:3864](packages/litestone/src/core/client.js#L3864) |
| FJS-218 | litestone — **A cycle in a self-referential table is not detected and not refused — the query silently returns `maxDepth` rows, defaulting to 1000 copies.** `WITH RECURSIVE … UNION ALL` with no cycle guard: two rows pointing at each other answer **1000 rows**, `_depth` counting to 1000, in 2ms; a single row that is its own parent (`id: 1, parentId: 1`) answers 1000 copies of that one row. Both directions, descendants and ancestors. On the two-row fixture it is fast and merely wrong; the cost is a property of the fan-out, so a wide graph with one cycle in it multiplies rather than repeats, and the failure arrives as a hung request rather than an error. Nothing prevents the cycle being created either — `parentId` takes any id including the row's own, and no FK, `@@check` or write path refuses it. Two halves to fix and they are independent: the CTE should be cycle-safe (SQLite has no `CYCLE` clause, so this is a visited-path column or `UNION` over the id), and a self-referential write ought to be able to refuse a cycle at the boundary where it is created, which is the cheaper of the two and the one that gives a caller a sentence | 2026-08-12 | **closed 2026-08-12** · [client.js:3881](packages/litestone/src/core/client.js#L3881) |
| FJS-219 | litestone — **`recursive` silently ignores `select`, and `count`/`findFirst` silently ignore `recursive`.** `findMany({ where, recursive: true, select: { id: true } })` returns every column of every row — the CTE path emits `SELECT "table".*` and never reads `args.select`, so a caller that narrowed its read gets the whole row back and cannot tell. In the other direction `count({ where: { id: 1 }, recursive: true })` counts the **anchors**, not the subtree, and `findFirst({ where: { id: 1 }, recursive: true })` returns the anchor itself rather than anything below it: neither refuses the option they cannot honor, so both answer a plausible number for a question nobody asked. `@guarded(all)` and `@computed` are handled correctly on the recursive path (verified), so this is narrower than [FJS-216](#) — but it is the same root cause, a hand-built second read path that reimplements some of `findMany` and not the rest. Cheapest correct move for the two read helpers is to refuse `recursive` by name until they support it | 2026-08-12 | **closed 2026-08-12** · [client.js:3891](packages/litestone/src/core/client.js#L3891) |
| FJS-201 | litestone — **every filter an encrypted column could not answer failed silently, and `not` failed by returning EVERY row** **Fixed 2026-08-12.** Three silences, all verified through `asSystem()` so the `@guarded(all)` that `@encrypted` implies was not what was being observed. **(a)** Plain `@encrypted` has no index, so `{ ssn: '123-45-6789' }` compared plaintext against a stored `v1.UA31…` and matched nothing — `[]`, no warning. It now throws naming the field and the cure (`@encrypted(searchable: true)`), which is the Tier-3 ruling: an unknown key still only warns on a read, because that is a typo and the caller has a hint, but a key that is real and spelled right and can never match has nothing to notice. **(b)** `@encrypted(searchable: true)` answered `eq` through its digest and `{ in: ['s-alpha'] }` answered `[]`, because `rewriteEncryptedWhere` hashed only the scalar form — `typeof val !== 'object'` — so every operator spelling fell through unhashed. **(c)** the worst: `{ encs: { not: 's-alpha' } }` returned **both rows**, the excluded one included, because a plaintext never equals a digest so the negation was true for everything; a filter quietly returning too much is the failure row policies exist to prevent, reached with no policy involved. `equals`, `not`, `in`, `notIn` and the bare-array shorthand now each hash their operands; an operator a digest cannot answer (`contains`, `startsWith`, ordering) is refused naming the field and saying why — a digest preserves equality and nothing else. **Two existing tests asserted the silence as expected and were rewritten**, which the row said would be needed. The sorting half — an encrypted column sorts by ciphertext — is **not** fixed and moved into FJS-200, where it joins array and Json under one rule: a column whose stored text is a storage detail is not a sort key. 6 tests, mutation-checked (read-throw → 15 red; operator hashing → 2; bare-array hashing → 2) | 2026-08-12 | [client.js:3312](packages/litestone/src/core/client.js#L3312) · [client.js:1320](packages/litestone/src/core/client.js#L1320) |
| FJS-199 | litestone — **a plain object in a bind position dropped every binding in the statement, WHERE included** **Fixed 2026-08-12.** `bun:sqlite` reads a plain object in the parameter list as a bag of NAMED parameters; a statement built with positional `?` matches none of its keys and runs with nothing bound at all, raising no error — `SELECT ? IS NULL` given `{x:1}` answers `1`, and `UPDATE t SET a = ? WHERE id = ?` given `({x:1}, 1)` changes no rows because the id was voided along with the value. One mechanism, four symptoms that looked unrelated: `update` returned `null` (its WHERE matched nothing, which litestone reports as *no such row*, one branch away from a spurious `@version` conflict), `updateMany` reported `NOT NULL constraint failed` on a column that was never bound, `create`/`upsert` threw the driver's raw `Binding expected string, TypedArray, …` naming no field, and a **read** answered `[]` — the worst, because a read has no `changes` to notice, so `{ meta: { equals: {tier:3} } }` missed the row holding exactly that. Now refused naming the field at both funnels: `checkBindable` in the where builder (which already caught undefined, functions and symbols — the object was the one unbindable value the driver does not throw on) and after the write pipeline's serialization, where a Json column is already text so anything still an object is genuinely unbindable. The write message names the shape that gets here — `{ views: { increment: 1 } }` — and says litestone has no atomic update operators (`FJS-D27`). Functions and symbols are named on the write path too rather than left to the driver. 5 tests, mutation-checked (write guard → 2 red; read guard → 7) | 2026-08-12 | [client.js:3156](packages/litestone/src/core/client.js#L3156) · [query.js:236](packages/litestone/src/core/query.js#L236) |
| FJS-203 | litestone — **`groupBy` and `aggregate` skipped hydration, so a column's TYPE depended on which method asked for it** **Fixed 2026-08-12.** The row path runs `deserializeRow` + `coerceBooleans`; the grouped and aggregated paths returned SQLite's own values, so one row read three ways gave `["x"]` / `"[\"x\"]"` / `"[\"x\"]"` for an array column, `true` / `1` / `1` for a Boolean, and an object / a string / a string for `Json`. `DateTime` agreed, because its storage and read forms are the same ISO string — which is why this first looked like an array problem. Hydration now runs wherever a returned value is still in the column's own domain: a groupBy's `by` keys, and `_min`/`_max`. **Deliberately not `_sum`/`_avg`** — the number there is no longer of the column's type, and the sum of a Boolean column is a count, so coercing it back would answer 3 as `false`; a test pins that. The semantic question behind it stays open in the matrix as `203:ref` on `aggMax`: `_max` over a JSON document is lexicographic over text and means nothing, and grouping by one is grouping by an exact serialized document. 2 tests, mutation-checked (7 red) | 2026-08-12 | [client.js:3170](packages/litestone/src/core/client.js#L3170) |
| FJS-208 | litestone — **an update with no columns left to set emitted `UPDATE "t" SET  WHERE …`** **Fixed 2026-08-12.** SQLite answered `near "WHERE": syntax error`. Reachable two ways, and the second is ordinary: `data: {}`, and `data: { bogus: 1 }` after the unknown-key strip — which is the **mass-assignment protection working as designed**, so a form post whose fields no longer match the model was turned into malformed SQL by the feature that exists to make it safe. `updateMany` now answers the count of rows the where matched, having written nothing, which is what `update` already did with the same input. SET and WHERE parameters are collected in separate arrays and joined at the end; sharing one array made the statement depend on which half was built first, which is why the case could not be handled where it belonged. 2 tests, mutation-checked (1 red) | 2026-08-12 | [client.js:4952](packages/litestone/src/core/client.js#L4952) |
| FJS-209 | litestone — **writing a `@generated` or `@computed` field was silently ignored** **Fixed 2026-08-12.** Both are absent from `_allowedWriteKeys`, so a write naming one left through the *unknown-key* strip — silent by design, because that strip is the mass-assignment protection. Declared-but-unwritable is a different thing wearing the same clothes: `update({ data: { upper: 'ZZZ' } })` returned the row with `upper` unchanged and threw nothing, and if it was the only key the emptied payload became FJS-208's syntax error, so the same mistake was silent or fatal depending on what else was in the object. A companion map now carries *why* each name cannot be written and the strip loop throws instead of deleting, naming the field and which kind it is. An **unknown** key is still stripped without a word — a test pins that, because the protection is the reason the silence existed. 3 tests, mutation-checked (6 red) | 2026-08-12 | [client.js:2359](packages/litestone/src/core/client.js#L2359) |
| FJS-212 | litestone — **`{ tags: { in: [...] } }` answered nothing where the bare array answered the row** **Fixed 2026-08-12.** `filtering.md` documents the bare array as meaning `in`, and after FJS-189 the shorthand read the column's elements while its own explicit spelling still compared the whole JSON document — so the more readable form was the one that silently matched nothing. The bare-array branch had `arrayFields` and the `in` branch was never given it. `in` and `notIn` now move inside `json_each` on an array column, exactly as the shorthand does, and a scalar column is untouched. 3 tests, mutation-checked (4 red) | 2026-08-12 | [query.js:373](packages/litestone/src/core/query.js#L373) |
| FJS-198 | sierra — **the example's production build shipped no JavaScript and no CSS, and had been doing so unnoticed** **Fixed 2026-08-12.** `packages/sierra/example/web/index.html` explained in a comment that "`app` + a theme go on `<body>`", and vite injects the built `<script>` at the first TEXTUAL match for the body tag without skipping comments — so both the module script and the stylesheet landed *inside* the comment in `dist/client/index.html`. The build succeeded, the file looked right, and the page was inert. The hazard is documented in the root `CLAUDE.md` and the repo's own example was the thing violating it, which is the argument for checking a rule rather than writing it down. Found by the first run of `fli check`; rebuilt and confirmed the tags now land outside the comment. Also split `resources/leads.mesa` — lowercase, three Resources in one file — into `Lead.mesa`, `Account.mesa` and `Tag.mesa` (Invariant 19) | 2026-08-12 | [index.html](packages/sierra/example/web/index.html) |
| FJS-197 | junction — **`ctx.id` was a string over HTTP and whatever JSON type the client sent over WebSocket** **Fixed 2026-08-12.** Over HTTP the id is a path segment and can only ever be a string; over the socket it rides a JSON frame, so `patch(42, …)` handed the service the number `42` while `PATCH /leads/42` handed it `'42'`. A handler comparing `ctx.id` to a row's own id, or using it as a Map key, answered differently depending on whether a socket happened to be connected — and the browser client silently prefers the socket when one is up, so the same code is correct in dev and wrong in production or the reverse. The WS handler now normalises to a string, matching the transport that has no choice. Found by `verifyTransportParity` on its first run against a custom action | 2026-08-12 | [channels.ts:625](packages/junction/src/transport/channels.ts#L625) |
| FJS-196 | junction — **a thrown status junction has no error class for arrived as a 500** **Fixed 2026-08-12.** `fromStatusCode` mapped fourteen codes to classes and everything else to `GeneralError`, whose `code` is 500 — so an app throwing `{ status: 423 }` or `{ status: 451 }` got a server error on the wire. Not a narrower answer but a different category: the client stops retrying and the 5xx pages someone, for a refusal the app stated deliberately. `err.name` and `registerErrorMapper` were unaffected, which is why it survived — every error junction itself throws has a class. Now an unmapped code in 400–599 keeps its status and only loses the class name, which is the part nothing on the wire reads. Found by `verifyTransportParity`, whose falsifiability test staged a hook throwing a 418 and got a 500 back from both transports | 2026-08-12 | [errors.ts:244](packages/junction/src/core/errors.ts#L244) |
| FJS-195 | litestone — **`field == null` in a row policy filtered every row out, silently, and disagreed with the same rule on create** **Fixed 2026-08-12.** `@@allow('read', ownerId == null)` compiled to `"ownerId" = NULL`, which SQLite answers NULL — never true — so an unowned row was invisible to everyone and nothing raised: an empty list with a 200, the failure mode `@@allow` has by design and the reason a wrong policy is so hard to see. Worse than half-wrong: `create` is evaluated by `evalJs`, which compares with `===` and had always been right, so a row could be created successfully by a caller who then could not see it. The `auth() == null` form was already special-cased in both paths; the `field == null` form was in neither. Now emits `IS NULL` / `IS NOT NULL`, resolving a `belongsTo` field to its FK the same way `field == auth()` does. Found while building `@frontierjs/testing`, whose fixture schema wrote the natural thing — the first vertical test through the new package failed on it. 3 tests, mutation-checked | 2026-08-12 | [policy.js:236](packages/litestone/src/core/policy.js#L236) |
| FJS-189 | litestone — **a bare array in a `where` on an array column matched nothing, and said nothing** **Fixed 2026-08-11.** `findMany({ where: { tags: ['x','y'] } })` against `tags String[]` returned `[]` for a row whose tags were exactly that. A bare array is litestone's shorthand for `IN`, and `"tags" IN ('x','y')` asks a JSON document whether it equals `'x'`. `$checkWhere` reported nothing — the key IS a real field — so `autoFilter` passed it through as a valid filter, and the one filter shape a caller reaches for first was the one shape that silently answered nothing. **Ruled 2026-08-11: the shorthand stays `IN` on both kinds of column** — *the column's value is in this list* — so an array column moves the `IN` inside `json_each` and the shorthand is `hasSome`, reached without a new word. Deliberately NOT Prisma's reading, which takes a bare array on a list field as `equals`: both readings are silently wrong for a caller who meant the other, but `equals` fails to ZERO rows and this fails to too many, and a wrong empty result is the failure this row exists to remove. Stated in `docs/filtering.md` for anyone arriving from Prisma. Four things came with it: **`equals`** (`json(col) = json(?)`, both sides wrapped so a row a JS migration wrote as `[ "x", "y" ]` still matches; ordered, as PostgreSQL array equality is), **`hasNone`**, **`not: [...]`** — which used to fall into `col != ?` with one placeholder and N bindings, so SQLite answered *expected 1 values, received 2*, about placeholder counts, naming neither the field nor the reason — and **an array operator on a scalar column**, which raised `malformed JSON` from `json_each` and now says which field and which operator. `{ tags: [] }` also emitted `IN ()`, which is not valid SQLite. The plumbing is the bulk of it: the two readings are the same SHAPE (`{ id: [1,2] }` vs `{ tags: ['x','y'] }` differ only in the column), so `buildWhere` now takes an `arrayFields` set built by `buildArrayMap`, threaded to every path that builds its own WHERE — `delete`, `deleteMany`, a relation filter (the TARGET model's set), an `include` filter and a `_count` include. Found probing enum arrays ([FJS-141](#)); not enum-specific, every array column had it. 15 tests, mutation-checked (4 red on revert: the column test, the guard, `equals`, `not`) | 2026-08-11 | [query.js:318](packages/litestone/src/core/query.js#L318) |
| FJS-141 | litestone — **a vocabulary of MANY values could not be a column, so a declared set had to leave the schema** **Fixed 2026-08-11.** `targets ReclaimTarget[]` was refused at parse time — *array [] is only supported for Text, Integer, File, or a model name for many-to-many* — so a set-valued enum had no home in the seed. An app either wrote `String[]` and validated the members somewhere else (basecamp's `cleanup` service), or declared the enum anyway and kept two homes with nothing joining them, which is the shape that let `AlertRule.severity` default to a value its own API refused. An enum array is now a column: JSON TEXT under the same `json_type = 'array'` CHECK every array carries, `Enum[]` in the generated types, `{ type: 'array', items: { $ref } }` in the JSON Schema — the `$ref` on the ITEMS, since a picker reading the field's own schema would otherwise offer one choice for a column holding several — and every member checked at the client boundary, naming all the bad ones rather than the first. **There is no membership CHECK and there cannot be**: reading the elements of a JSON array needs `json_each`, that needs a subquery, and SQLite forbids a subquery in a CHECK. So the boundary IS the client here, which is the tier `@minItems`, `@uniqueItems` and `Int[]` element typing already sit at; `enumCheck` returns null for an array rather than emitting `IN (...)`, which would compare the whole document against one value and fail every non-empty set. `@@transitions` already refused an array field and still does. 11 tests, mutation-checked (drop the CHECK guard → 5 red; drop the membership branch → 4; drop the items wrap → 1) | 2026-08-11 | [parser.js:2452](packages/litestone/src/core/parser.js#L2452) · [ddl.js:251](packages/litestone/src/core/ddl.js#L251) · [client.js:3062](packages/litestone/src/core/client.js#L3062) |
| FJS-190 | litestone — **`@default` on an array field parsed, migrated, then failed the first insert that relied on it** **Fixed 2026-08-11.** An array column is JSON TEXT under a `json_type = 'array'` CHECK, and a `@default` is emitted into the DDL verbatim — so `tags String[] @default("x")` produced `DEFAULT 'x' CHECK (json_valid(...) AND json_type(...) = 'array')`, a column whose own default violates its own constraint. Nothing complained until an insert omitted the field, and then it was `CHECK constraint failed`, naming the constraint rather than the schema line that broke it. Now refused at parse time unless the default is a string that parses as a JSON array, so `@default("[]")` — which is what every schema in this repo already writes, working by accident — still passes and `@default("x")`, `@default(a)`, `@default(1)` are schema errors. Found alongside [FJS-141](#), where `@default(logs)` on a set-valued enum is the natural first thing to write. 2 tests, mutation-checked | 2026-08-11 | [parser.js:2460](packages/litestone/src/core/parser.js#L2460) |
| FJS-175 | litestone — **`createMany` derived the INSERT column list from the first row, so a mixed batch lost data or crashed on order alone** **Fixed 2026-08-11.** `Object.keys(rows[0])` built one prepared statement for the whole batch, so row 0 decided what every other row was allowed to write. Narrow row first → the wide row's extra columns were dropped, `{ count: 2 }` returned, nothing said; wide row first → `NOT NULL constraint failed`, because a column absent from a later row was bound as an explicit NULL and that defeats the DDL `DEFAULT` which would have filled it. The same two rows in the same call either lost data or threw depending only on their order, and one outcome was silent. Now one prepared statement per row SHAPE, cached by column list — a uniform batch, which is the ordinary case, prepares exactly one and reports exactly the SQL it did before. Rows are still inserted in **caller order** rather than grouped by shape, because an `@id @default(autoincrement())` is assigned in insert order and grouping would renumber the caller's rows. **`upsertMany` had the identical defect** and is fixed the same way, with the `ON CONFLICT DO UPDATE SET` clause derived per shape so a row carrying a column row 0 omitted updates it rather than losing it. **A third case the filing did not name**: a key present with the value `undefined` was bound as NULL, so `{ views: form.views }` off a form with no views field defeated the column default and failed a NOT NULL column — on `create()` too, one row, no batch involved. `writeData` now drops an undefined-valued key with the unknown ones, so only `null` clears (Invariant 9), on create, createMany and update alike. 10 tests, mutation-checked (shape from row 0 → 6 red; keep the undefined key → 3) | 2026-08-11 | [client.js:4595](packages/litestone/src/core/client.js#L4595) · [client.js:2954](packages/litestone/src/core/client.js#L2954) |
| FJS-015 | litestone — **`createClient({ db })` was ignored, in silence, when the schema declared `database main`** **Fixed 2026-08-11.** `db` was only ever consulted to invent an implicit main when the schema declared none, so against a schema that declares one it did nothing and said nothing: `db: ':memory:'` wrote the declared file, and a test that believed it was in-memory accumulated state across runs. basecamp carried a nine-line comment warning about it rather than passing the option. `db` now names main's path whether or not a `database main` block exists — it is folded into the `databases` overrides as `main`, so one option means one thing everywhere. Precedence is most-specific-wins: `databases: ':memory:'` (every sqlite database) > `databases: { main: { path } }` > `db` > the declaration. It reaches **main only** — a second declared database keeps its declared path, which is the distinction between the two options and is now stated in the docs. 5 tests, mutation-checked | 2026-08-11 | [client.js:6222](packages/litestone/src/core/client.js#L6222) |
| FJS-064 | litestone — **the unscoped `litestone` npm name is blocked** **Closed 2026-08-11, not needed.** npm's similarity check refuses it and the support ticket was never chased. `@frontierjs/litestone` is published and is the name every consumer in this repo uses; the unscoped name buys nothing the scoped one does not already have. Reopen only if the package ships outside the `@frontierjs` scope | 2026-08-11 | ruling |
| FJS-188 | litestone — **a `view` over a model made that model impossible to migrate** **Fixed 2026-08-11.** A table rebuild ends in `ALTER TABLE "note__new" RENAME TO "note"`, and SQLite reparses every view in the schema on a rename. A view still pointing at the table the rebuild had just dropped is an error, so the statement threw `error in view NoteTitles: no such table: main.note` and the transaction rolled back. **This is litestone's own `view` declaration against litestone's own migrations**: declare `view NoteV { @@sql("SELECT title FROM note") }` and `model Note` can never drop a column, change a type, change a foreign key or change `@@strict` again — the migration fails identically every time and the message names the view rather than the cause. An app-created view did the same. Found probing [FJS-183](#), whose filing had listed views alongside triggers as *silently lost*; they are neither silent nor lost. **A view is not in the trigger's class and must not be treated as one**: it is a stored SELECT with no state and no side effects, so it can be dropped before the rebuild and put back verbatim afterwards, which is now what happens. A view the schema is redefining in the same migration is left to the changed-views block instead, since restating its old body would fail on exactly the change the new body was written for. Dependency is detected by naming the table in the view body and deliberately over-approximates — recreating a view that did not need it costs nothing, missing one costs the migration. **One sharp edge is closed with it**: SQLite does not resolve a view body at CREATE time, so a view over a column the rebuild dropped comes back without complaint and fails in whatever reads it, months later. Each restored view is now read (`SELECT 1 FROM "v" LIMIT 0`) inside the migration's transaction, so a view the schema change invalidated refuses the migration rather than surviving broken. That cannot catch a body written `SELECT "scratch"` with the column double-quoted, because SQLite resolves an unknown double-quoted identifier as a string literal and reports no error at all — the same trap `rebuildSQL` already carries a comment about. 6 tests, mutation-checked (drop the view first → 4 red; drop the validation read → 1) | 2026-08-11 | [migrate.js:457](packages/litestone/src/core/migrate.js#L457) |
| FJS-187 | litestone — **an index the app created was dropped by the next schema change of any kind** **Fixed 2026-08-11.** `diffIndexes` compared pristine against live by shape, so an index created outside the schema — in a JS migration, or straight against the database — was live-and-not-pristine and landed in `dropped`, which emits `DROP INDEX IF EXISTS`. Worse, `hasChanges` counts `indexes.dropped`, so **its mere presence made the table look changed**: `autoMigrate` on an unmoved schema reported `migrated` and the only thing it did was remove the index. In practice the DDL-hash fast path hid it until the next real change, which is what makes it nasty — measured as: add the index, restart twice (survives, `in-sync`), add one unrelated nullable column, index gone. No error and no rebuild; a query plan silently collapses and the app just gets slower. Litestone now drops only an index whose name it generates — every one it emits for a model table is `idx_<table>_<fields>` — so removing an `@@index` still drops `idx_note_title`, and `note_title_idx` is left alone. An index the app happened to name `idx_<table>_…` is still treated as litestone's, since that is the name litestone would have generated for the same declaration; stated in `docs/migrations.md`. Found probing [FJS-183](#), whose filing had this as a rebuild problem — it never was. A rebuild destroys it too, and that half stays under FJS-183. 4 tests, mutation-checked | 2026-08-11 | [migrate.js:296](packages/litestone/src/core/migrate.js#L296) |
| FJS-186 | litestone — **`search()` with a `select` that did not name the id returned no rows at all** **Fixed 2026-08-11.** `search()` runs two queries: one against the FTS5 table for `rowid` + `rank`, then one against the base table for the rows, rejoined by id to restore rank order. The second query took the caller's `select` verbatim, so `search('sqlite', { select: { title: true } })` fetched `SELECT "title"` — no id — and every row failed the rejoin. The result was `[]`: not an error, not a partial answer, just *no results* for a query that has them, and the more precisely a caller asked the more completely it failed. Pre-existing and independent of [FJS-185](#), but found by it — narrowing a computed field's fetch is exactly what makes an id-less `select` common rather than rare, and the first probe of `search` + `needs` returned nothing. The id is injected when a narrowed select omits it and dropped again by the trim, the same shape `findManyCursor` already uses for its `orderBy` columns. 3 tests, mutation-checked | 2026-08-11 | [client.js:5563](packages/litestone/src/core/client.js#L5563) |
| FJS-184 | litestone — **every `@computed` fn ran on every row of every read, including reads that had narrowed the row it was handed** **Fixed 2026-08-11.** `applyComputed` iterated the whole extension map and knew nothing about `select`, so `findMany({ select: { id: true, title: true } })` on a model with two computed fields ran both, then threw both results away in `trimToSelect`. The waste is the smaller half. The row those fns were handed was the row the select had **already narrowed**, so a fn reading a column the caller did not select saw `undefined` and answered something plausible — the exact failure [FJS-178](#) chased across `findManyCursor` and all three `include` shapes, and [FJS-173](#) across a relation `orderBy`, appearing a third time on the select path itself. Measured before the fix on `model Post { … shout @computed }`: `select: { id, title }` called the fn twice, over rows carrying only `id` and `title`. A computed field outside the caller's select is now not computed at all, which removes the waste and the partial row together; with no `select` every computed field still runs over the whole row, unchanged. The narrowing that makes a *selected* computed field cheap is [FJS-185](#) | 2026-08-11 | [client.js:1614](packages/litestone/src/core/client.js#L1614) · 15 tests |
| FJS-185 | litestone — **a computed field could not declare what it reads, so naming one in a `select` fetched every column** **Fixed 2026-08-11.** `parseSelectArg` set `needsAllDbCols` for any selected `@computed` field, and the package's own `docs/schema.md` stated the consequence as a fact of life: *there is no way to declare a narrower dependency set*. A computed field over one column widened the SELECT to `*`, which defeats a covering index (SQLite answers a narrow list from the index b-tree and must seek the table for `*`), decrypts `@encrypted` columns nobody asked for, and — the amplification that is easy to miss — emits **every** `@from` correlated subquery on the model, because the `sqlCols === '*'` branch is what appends them. Three `@from` fields and one selected computed field needing none meant three subqueries per row. A fn may now be written `{ needs: ['name', 'bookCount'], compute: fn }`; the SELECT carries exactly those (a `@from` name emits just that subquery), and they are trimmed from the result, so asking for a computed field does not smuggle its inputs back. A bare fn keeps the old behavior and the old `*` — undeclared has to mean *fetch everything*, since nothing can know what the fn will touch, and one bare fn in a select widens it for the declared ones too. **The declaration is enforced, not trusted**: the row a `needs` fn receives is a Proxy carrying exactly the declared names and reading anything else throws, naming the field and the list. Without that, adding a line to the fn and forgetting the list would answer `undefined` — strictly worse than fetching every column, and the same silence [FJS-184](#) exists to end. `in` is left alone so feature-detection still works. A `needs` naming something that is not a readable field of the model is refused at `createClient`, where the list is written rather than at the read that would answer nothing. Narrowing reaches `findManyCursor` and `search()`, which build their own SELECTs, and a nested `select` under an `include`. 15 tests, mutation-checked (3 red on revert: skip-unselected, narrow-the-select, the guard) | 2026-08-11 | [query.js:690](packages/litestone/src/core/query.js#L690) · [client.js:1470](packages/litestone/src/core/client.js#L1470) |
| FJS-179 | litestone — **`@@softDelete` and `@@fts` on one model corrupted the FTS index on every soft delete, and `remove()` reported it as `database disk image is malformed`** **Fixed 2026-08-11.** Two triggers fired on one soft delete: an unconditional `AFTER UPDATE` one that issued `'delete' old` then re-inserted `new`, and an `AFTER UPDATE OF "deletedAt"` one that issued `'delete' old` a second time. FTS5 answers a second delete of one docid with `SQLITE_CORRUPT_VTAB`, naming neither the model, the FTS table, nor the two attributes — so it read as a broken database file rather than an unsupported pair. **It only RAISES when the extra delete empties the structure**, which is why the original report said *every* remove and why nothing caught it: above one indexed row the second delete was swallowed and the row stayed in the index, so the triggers never achieved the live-only index they were written for and `search()` was correct only because it filters again in its own `WHERE`. That second filter is now the only one — the index mirrors the table, the two extra triggers are retired, and the trigger set no longer branches on `@@softDelete` at all. Consequences: `withDeleted`/`onlyDeleted` on `search()` were documented and could not work, and now do; a `rebuild` reindexes from the content table, so it agrees with the triggers instead of silently disagreeing; and `search()` narrows before the FTS `LIMIT` rather than after, so a search for 20 no longer answers 13 with nothing to say why. The fixture that found it now carries both attributes on one model, which is what separate fixtures hid | 2026-08-11 | [ddl.js:311](packages/litestone/src/core/ddl.js#L311) · [client.js:5401](packages/litestone/src/core/client.js#L5401) · 10 tests |
| FJS-181 | litestone — **`introspect()` recorded no triggers, so no change to a generated trigger could ever migrate** **Fixed 2026-08-11.** Tables, columns, indexes, foreign keys, STRICT and views were all read back from `sqlite_master`; triggers were not, so `diffSchemas` could not see one and `generateMigrationSQL` could not emit one. The cost was concrete rather than theoretical: [FJS-179](#)'s fix reaches a database built by the new DDL, and **every database that already existed kept the broken trigger pair** while the diff reported the schema in sync. Triggers now travel in `__triggers` beside `__views`, compare on normalized SQL, and are dropped-and-recreated when they differ. Only names litestone generates are ever dropped (`*_fts_*`, `*_updatedAt`) — a trigger the app wrote is not in pristine and survives, which a test pins | 2026-08-11 | [migrate.js:79](packages/litestone/src/core/migrate.js#L79) |
| FJS-182 | litestone — **a table rebuild dropped every trigger on the table and put none of them back** **Fixed 2026-08-11.** `rebuildSQL` is the standard SQLite table-rewrite: create `_tmp`, copy, `DROP TABLE`, rename. Dropping the table takes its triggers with it, and nothing recreated them — so a model came out of an ordinary column-drop or type-change migration with an FTS index that had **silently stopped updating** and an `updatedAt` that had stopped being stamped. Both keep working, and neither reports anything: writes succeed, searches return stale results forever. Found while testing [FJS-181](#), where a rebuild left a table with zero triggers. A rebuilt table now has all of its generated triggers restated after the rebuild, even where the bodies match. **A trigger the APP wrote on a rebuilt table is still lost** — it is not in pristine, so nothing can restate it; that half is [FJS-183](#) | 2026-08-11 | [migrate.js](packages/litestone/src/core/migrate.js) |
| FJS-180 | litestone — **`restore()` returned a shape neither of its two docs claimed, and its TypeScript declaration accepted a property the value never had** **Fixed 2026-08-11.** Three sources, three answers: `index.d.ts` said `Promise<TRow \| null>`, `CLAUDE.md` said `row[]`, the code returned `{ count }`. So `(await db.post.restore({ where })).id` typechecked and was `undefined` at runtime, and the array form the package's own map documented never existed. The rows were there the whole time — `restore` runs `UPDATE … RETURNING *` and threw them away to count them. It now answers the array, which is the honest shape (`where` can match many, an empty array means nothing was deleted to restore) and mirrors `remove`, which returns its row. They are also **shaped** now: the RETURNING rows had never been through `read()`, so had they been returned before they would have carried unparsed Json, `0`/`1` for booleans, no computed fields and no `@from` — a shape no other read in the package produces. Declaration, `CLAUDE.md` and `docs/soft-delete.md` all corrected. Breaking for any caller reading `.count`; one existed, in this package's own audit-trail suite. 5 tests | 2026-08-11 | [client.js:5134](packages/litestone/src/core/client.js#L5134) |
| FJS-178 | litestone — **`@from` existed on one read path only, so `findManyCursor` and every `include` silently dropped it — and any `@computed` field reading one computed from `undefined`** **Fixed 2026-08-11.** The subqueries are built in `makeTable`'s closure and appended by the query pipeline, which `findManyCursor` and `resolveIncludes` both bypass to build their own SQL. So the field was simply **absent**: `db.author.findManyCursor({})` answered `{ id, name }` where `findMany` answered `{ id, name, bookCount: 2 }`, and `include: { author: true }` did the same. **Absence is the dangerous half**, because `applyComputed` still runs — a `@computed` field over a `@from` field answered a plausible `0` (or `NaN`, or `''`) rather than throwing, so the same row read two ways gave two different numbers and neither said so. On a CRM that means a paginated client list, or a notes list with the client included, shows every account at a score of zero. Selecting the field explicitly answered `{}` on those paths: no throw, no column. Same root as [FJS-173](#) — one owner, several call sites that assemble SQL themselves — and closed the same way: `fromSelectExpr()` and `deserializeFromRow()` are module-level, `fromMap` is on `ctx`, and the four sites ask rather than growing a fourth copy. The **m2m** include needed the ALIASED variant, since it selects `t.*` beside the join table. Both halves of the shaping were missing and only one is visible: without the SELECT expression the field is absent, without the deserializer a `@from(X, last: true)` arrives as the JSON string SQLite returned and `exists:` as `0`/`1`. **Two more paths closed in the same pass**, found by walking every method that returns a row: `search()` builds its own step-2 SELECT and had the same hole, and **every write returned a row with no `@from` field at all** — `RETURNING` is table columns only, SQLite cannot put a correlated subquery there, and `applyComputed` runs regardless, so `create`/`update`/`upsert`/`remove`/`delete` handed back a row whose derived fields disagreed with the same row refetched. That one reached furthest: junction returns `table.update()` straight through, so the PATCH response **and** the `svc updated` broadcast built from it both carried the degraded row, and every open tab replaced a correct one with it. Writes now re-read the `@from` values before shaping — one extra SELECT, only for a model that declares `@from`, only on write paths that opt in (a read already carries them, and hydrating whenever a key was missing would fire a query per row for a `select` that legitimately excluded it). `delete` is the exception and needs no extra query: it already reads the row before the DELETE, where the values still correlate. 26 tests, mutation-checked (4 + 7 + 6 + 2 red on revert); two of them assert the property that makes this one bug rather than a dozen — an included row equals a directly-read row, and a just-written row equals the same row refetched | 2026-08-11 | [client.js:862](packages/litestone/src/core/client.js#L862) · [client.js:1849](packages/litestone/src/core/client.js#L1849) · [client.js:5177](packages/litestone/src/core/client.js#L5177) |
| FJS-174 | litestone — **`orderBy` validated nothing, so an unknown key and a `@computed` field were both a silent no-op** **Fixed 2026-08-11.** `orderBy: { bogusColumn: 'desc' }` returned the rows in insertion order and said nothing anywhere — not a throw, not the stderr warning that a bad `where` key prints. The same silence covered `@computed`, which cannot be sorted at all: it is a JS function applied to a row SQLite has already returned, so there is nothing to put in an `ORDER BY`, and a list "sorted by" a derived score was arbitrary rows that looked plausible — page 2 most of all. **This half deliberately does not inherit `checkWhereKeys`'s warn-on-read split**: a bad filter key returns fewer rows, which the caller can see, while a bad sort key returns the RIGHT rows in the wrong order, which nothing can see. Both now throw, naming what is sortable and separating the two refusals — an unknown field gets the typo suggestion litestone already computes, a `@computed` field is told why it cannot be sorted and what to do instead (`@from`, `@generated`, or store it). A `@from` field sorts, as it always did — it is a correlated subquery aliased into the SELECT list. `db.$checkOrderBy(accessor, orderBy)` is `$checkWhere`'s sibling with the identical contract — `[]` for an unknown accessor, on every flavor of client — and junction's new `autoSort` derived hook calls it on `find`, so `?$orderBy=-bogus` is a 400 naming the sortable fields rather than an unsorted 200. Two existing tests asserted `"relation 'x' not found"` for keys the model never declared; both now get the better message earlier. 12 tests here + 6 in junction over HTTP against a real client, mutation-checked | 2026-08-11 | [client.js:1174](packages/litestone/src/core/client.js#L1174) · [litestone.ts:1087](packages/junction/src/core/litestone.ts#L1087) |
| FJS-173 | litestone — **a `@from` field did not survive a relation `orderBy`, silently or loudly depending on the select** **Fixed 2026-08-11.** The correlated subquery a `@from` compiles to names the outer table, and a relation `orderBy` aliases that table to `t`, so the two disagreed and the query took one of two exits. With the default select the whole subquery was dropped — `SELECT t.*` — so **every `@from` field came back `undefined` and any `@computed` field reading one computed from `undefined` in silence** (a score of 0, a ratio of `NaN`, no error). With an explicit `select` naming the field, the raw `SQLiteError: no such column: <table>.<pk>` reached the caller. Both variants of every subquery are now built once at schema load and the query picks by whether it aliased. The WHERE clause needed the same choice and **the alias question is not the join question** — a relation *aggregate* orderBy adds an order part and no join, which is the case still wrong after the SELECT list was fixed. 5 tests, mutation-checked | 2026-08-11 | [client.js:765](packages/litestone/src/core/client.js#L765) · [client.js:3170](packages/litestone/src/core/client.js#L3170) |
| FJS-172 | litestone — **a `@from` ignored the target model's own `@@softDelete` and `@@hasTemplates`** **Fixed 2026-08-11.** `@from(Note, count: true)` counted soft-deleted notes and template notes, so every schema had to repeat `where: "deletedAt IS NULL"` by hand on every derived field — a default nobody remembers on the second model, failing quietly rather than loudly. **`include: { _count: true }` over the same relation had injected both filters since it was written**, so one relation had two counts that disagreed and neither said so. A `@from` now reads the target the way the target is read; `withDeleted: true` / `withTemplates: true` opt back in, named for the `findMany` args rather than a second vocabulary, and an explicit `where:` composes on top. An existing hand-written `deletedAt IS NULL` becomes redundant, not wrong. Found building a CRM chattiness score — counts per related model, which is the shape the package recommends. 7 tests, mutation-checked; one of them asserts the `@from` count and the `_count` agree | 2026-08-11 | [client.js:765](packages/litestone/src/core/client.js#L765) · [parser.js:1165](packages/litestone/src/core/parser.js#L1165) |
| FJS-171 | cli — **`fli scaffold` wrote resource files the repo had ruled against** | 2026-08-11 | `fli scaffold Note` emitted `web/src/resources/notes.mesa`, and Invariant 19 says a resource file is named for its noun — PascalCase, singular — with the lowercase accessor as the export inside, exactly the split `model Note` / `db.note` already makes. So the generator taught the shape ruled out on 2026-08-08, and `fli new`'s own example carried it too. Now `Note.mesa` exporting `notes`, with the header comment and all three route imports moved with it; where a plural is irregular the filename is where that becomes visible. Verified by scaffolding and reading the emitted import line |
| FJS-170 | auth — **`model User` was gated above every caller, so an app could not list its own people** | 2026-08-11 | `@@gate("8")` is SYSTEM — a level no request reaches — so every read of the identity table was refused for every principal including a sysadmin, and the one screen `fli new` ships rendered *"User.read" requires SYSTEM access (use asSystem())* to the account that had just registered. The number was never true: the three models that hold credential material (`Credential`, `Session`, `Verification`) are the ones with nothing outside auth to say to them, and `User` is a table an app's own screens legitimately list. Now `"4.4.4.5"` — read, create and update USER, delete ADMINISTRATOR: an app's screens list its people and a signed-in caller edits a profile, while removing a person is not self-service. **Registration is unaffected by any of it** because every write this package makes goes through `asSystem()`, above the ladder, and there is now a test that registers a user rather than assuming it. **A gate is per MODEL, not per row**, so update at USER means one caller may write another's row and its `role` column with it — measured, not inferred, and the scaffold's own `getLevel` reads `role`, so that is a self-promotion path. Row ownership is a `@@allow('update', id == auth().id)` and column ownership is a field `@allow('write', …)`; neither is declared here, and the test file states the consequence rather than implying otherwise. Changed in both copies — [auth/schema.ts](packages/auth/schema.ts) and the CLI's hand copy in [auth/install.md](packages/cli/commands/auth/install.md), which drift is the trap that file already carries a warning about. The two tests that pinned the old behavior asserted the right property with the wrong constant and are rewritten in both directions: anonymous is refused *by level 4* rather than by SYSTEM, a signed-in user reads, a plain user is refused a delete at level 5 while an ADMINISTRATOR performs one, and the credential tables still bounce for both. A sixth test states the gap on purpose — one user writing another user's row succeeds — so nobody reads the ladder as ownership. **Corrected 2026-08-14**: this row said `packages/basecamp` keeps `@@gate("8")` on its own `User`, and it does not — the hand copy moved to `"4.4.4.5"` with this change and nothing in that app's docs moved with it, which left the model an app's whole hub tier is written around updatable by any signed-in caller ([FJS-177](#)). **Closed properly 2026-08-14**: the fragment now ships what bounds the level rather than naming the gap in a comment — `@@allow('update', id == auth().id \| \| auth().isAdmin)` and `@allow('write', auth().isAdmin)` on `role` and `emailVerified`, with both CLI scaffolds grading the same standing and projecting `role === 'admin'` onto it in `sessionFields`. A test parses the CLI hand copy and compares what the two files declare. Ruled in `DECISIONS.md` § Access control. Verified on a clean scaffold in a browser: register, sign in, `/users/` lists the row; anonymous 401, non-admin create 403. [auth/schema.ts:36](packages/auth/schema.ts#L36) |
| FJS-169 | cli — **a freshly scaffolded app could not start, could not render, and could not add a model** | 2026-08-11 | Fourteen defects between `fli new my-app` and a working screen, found by scaffolding into a temp directory and running what came out — the method the CLI's own file prescribes and which nothing had done end to end. **The app never started**: the scaffolded `schema.lite` declared no `database` blocks while auth's injected fragments name `main` and `audit` in every model, so the parse failed at `createClient` listing six errors about databases the author never wrote — and `auth:install`'s own guard exempted `main` by name, on the assumption it is implicit. **The schema never reached the database**: `auth:install` shelled to `litestone push`, which is not a command (`litestone db push` is), and `fli db:push` ran `litestone migrate apply`, which replays migration FILES — on a project that has none it prints *Schema applied* having created no table, so `fli scaffold Note && fli db:push` left a service reading a table that was not there. **The key was generated after the push that needs it**, the presence test was a substring so a scaffolded `ENCRYPTION_KEY=` read as already set, and — the one that took longest — `bootstrap.js` loads the project `.env` at startup, so that empty value was on `process.env` and every child inherited it, where Bun's own `.env` loading will not override an existing var: the push reported no key while the file beside it held a good one. **The UI rendered nothing, with no error**: there was no entry point at all. `index.html` pointed at `App.mesa`, and importing a `.mesa` module mounts nothing — nobody imported `virtual:sierra` (so no router, no client, no `registerSchemas`), nobody imported `@frontierjs/css`, nobody called `mount()`. `web/src/main.js` is now emitted. **Vite never started either**: `sierra.config.js` read `location.protocol` at module scope, and Vite loads that file in Node. **Nothing was proxied**, so every API path hit the SPA fallback — 200, an HTML body, and a client reporting nonsense; the config now declares the proxy and states the web port, because Sierra's default is 3000, the API's number. **The shell imported `connected` from `@frontierjs/sierra/junction`, which exports `status`** — a Sierra error overlay on first paint — and read it with no `$:` line, which is the subscription, so it would have rendered once at the initial false and never changed. **`/ws` was never registered**, since `channels()` is what mounts it; the client had nothing to upgrade to and reported itself offline forever. `@frontierjs/css` was imported but neither a dependency nor in the local link list. Finally `deploy:doctor` grepped for the literal strings `"/health"` and `"/ws"`, which a plugin-wired app never writes, so a correct app drew two warnings. Verified end to end in a real browser against a clean scaffold: register → sign in → `fli scaffold Note` → `fli db:push` → the row on screen at `/notes/`, WebSocket connected, `bun run build` green, `deploy:doctor` clean but for the deliberate placeholder server. Two more found here and closed the same day — the `/users/` example was gated above every caller ([FJS-170](#)) and the generated resource filename broke Invariant 19 ([FJS-171](#)) |
| FJS-167 | cli — **Tab completion had never worked, in any shell** | 2026-08-10 | `completion/_module.md` referenced `join`, `homedir`, `existsSync`, `statSync`, `mkdirSync`, `writeFileSync` and `createHash` and imported none of them — the first is evaluated at module top level (`const CACHE_DIR = join(homedir(), '.fli')`), so **all five completion commands threw `ReferenceError: join is not defined`**, and the one that runs on every press of Tab throws where a shell discards stderr. The shell scripts, the install command and the query parser were all correct; nothing behind them ever ran. Invariant 15's parse sweep cannot see it by construction — it compiles each command with NO module script — which is the hazard the CLI's own file already states, and this is its first named instance. The fix deletes rather than repairs: the module carried its own scanner, its own mtime fingerprint and its own cache file, which is the registry's job, so `loadCompletions()` asks `buildRegistry()` and gets the shared cache with it; `completion:refresh` drops that cache through `clearRegistryCache()` rather than a file that no longer exists. Verified by running all three paths — command names, per-command flags, refresh |
| FJS-166 | cli — **a global install could not run a single command** | 2026-08-10 | A compiled command is written as a `.mjs` shim and imported, because a Node ESM loader resolves real files and not strings. The shim went to `<fliRoot>/.fli-tmp/<pid>/` unconditionally — right for a checkout, and `npm i -g @frontierjs/cli` puts fliRoot under a root-owned prefix where the first `mkdir` is `EACCES`. Every command died there, before running, on the one install shape a published CLI actually has; nothing in the repo could see it because the workspace copy is always writable. `fliTmpRoot()` in `core/utils.js` is now the one owner of the location and it decides by *attempting* the write rather than probing a mode bit — a root-owned prefix, a read-only mount and a squashed container image fail differently and only the write tells the truth. The fallback is `$TMPDIR/fli-<digest of fliRoot>/` with `node_modules` symlinked back at the install's, which is what keeps `import 'zx/globals'` resolving: Node resolves a bare specifier from the importing file's own directory, so the link has to sit on that path, and the digest keeps two installs from sharing a link into the wrong tree. Proven by copying the package to a `chmod 555` root and running `crypto:keygen` through it — `EACCES` before, the key after. It also collapsed a duplicate: the stale-session sweep existed twice, in `bin/fli.js` and in `runtime.js`, and both parsed a directory name as an int — so the suites' own `test-<pid>` dirs were invisible to it and **47 had accumulated in the checkout**. One `sweepStaleTmp()`, one name pattern, 3 tests in `tests/project-root.test.js` |
| FJS-165 | css — **the guide carried a hand copy of every theme's tokens, and it made the guide unreadable in `theme-basecamp`** | 2026-08-10 | `applyTheme()` wrote a copy of the theme's custom properties inline onto `.sg-app` from a `THEMES` map in `guide.js`, rather than applying the package's own `.theme-*` class — and the copy had drifted the way a second copy always does. `basecamp`'s entry carried the surfaces and the whole ink ramp but **not `--paper`**, the guide's private page-background token, which sat at `:root`: so the ground stayed cream while `--ink` went light, and every heading and paragraph in the reference rendered pale-on-pale while the theme's own stylesheet was correct. `dark`'s entry looked right only because it happened to override `--paper`, `--code-bg` and `--code-text` by hand — one theme patched, the next one not, which is what a copy buys. `notebook`'s entry was one colour and the word `Testing`, so its swatch showed basecamp's blue. **This is the same rule the package already states twice** — a guide page that documents source must READ the source (`ruleText()`), and a token alias at `:root` resolves once and inherits past every `.theme-*` (`tokens.css` § `--ring`) — applied to the one file that was exempt from both. Now: `applyTheme()` swaps the class, `themeToken(key, prop)` reads a swatch out of the CSSOM, the Themes page prints `ruleText('.theme-' + active)`, and `THEMES` holds only what a stylesheet cannot say — a display name and a one-line description. The guide's warm skin moved from `:root` to `.sg-app.theme-default`, and its five use sites take the package's surface as a use-site fallback, so a theme that says nothing about paper now gets the ground its own `--surface-sunken` describes. `--accent` follows `--color-primary`, resolved on `.sg-app` so it reads the theme class on that same element. Found by a reader switching to Basecamp; verified in a real browser under `basecamp`, `press` and `default` · [css/guide/guide.js](packages/css/guide/guide.js) · [css/guide/guide.css](packages/css/guide/guide.css) |
| FJS-156 | vscode — **two Mesa snippets expanded with the call removed** | 2026-08-10 | `$onCleanup` and `$class` sat unescaped in a snippet BODY, where `$name` is a VS Code snippet *variable*: an unknown one expands to nothing, so `$onCleanup(() => { })` inserted `(() => { })` and the `$class` prop snippet inserted `export let  = ''`. Both are the reverse of a typo — the text was right and the syntax swallowed it. The only thing that ever said so was one line in the extension host log at startup (*"very likely confuse snippet-variables and snippet-placeholders"*), naming neither snippet nor file, and it takes an installed extension to see. `\\$` in both, and `test/snippets.test.js` now walks every body of both languages: a `$` must be escaped, a tabstop (`$1`, `${1:x}`) or one of the 33 real VS Code variables. Prefixes are exempt — `"prefix": "$watch"` is typed, not expanded, and a rule reaching into it would push the vocabulary out of the snippet list. Found by opening the extension in a dev host, which is also the only place it was visible |
| FJS-152 | litestone — **Implicit many-to-many was unusable on any schema whose models are not `Int @id` named `id`** **Fixed 2026-08-10.** The join table hardcoded `INTEGER … REFERENCES "<table>"("id")` and every runtime path read the target's key as the literal `.id`, so the feature was off for the two commonest real shapes and failed differently in each. A uuid key died loudly — `cannot store TEXT value in INTEGER column _post_tag.postId` on the first `connect`, naming a table the author never wrote. **A key named anything else failed silently**: join rows are written `INSERT OR IGNORE` so connecting twice is idempotent, and OR IGNORE swallows a NOT NULL as happily as a duplicate — so `connect` returned the created row, wrote nothing, and the relation read back empty forever. `detectM2MPairs` now carries each side's `@id` name and SQL type onto the pair (the `@edge` side table, which had copied the same two lines, takes them too), and the relation map carries `selfPk`/`targetPk` through the six runtime sites that had their own `t."id"`: the include join, its policy subquery, `_count` both ways, the relation-filter correlation, the aggregate `orderBy`, and the connect/disconnect/set/delete writes. A target row with no key now throws by name rather than being ignored. **Nothing in the repo caught it because nothing in the repo uses the feature** — `basecamp` reaches for an explicit join model all 3 times (`AppServer`, `ServerNetwork`, `AlertRuleChannel`) and no note said why, while `sierra/example` is the only implicit m2m and its ids are `Int`. 4 tests (DDL, round-trip on `slug`/`code` keys, mixed `String`↔`Int`, uuid self-relation), mutation-checked in both halves. **One thing the fix does not do**: join tables are invisible to introspection, so a migration emits them `IF NOT EXISTS` and never alters one — a database created before this keeps the broken table. Drop it and re-migrate; it is provably empty, because no insert into it could ever have succeeded | 2026-08-10 | [ddl.js:536](packages/litestone/src/core/ddl.js#L536) · [client.js:3268](packages/litestone/src/core/client.js#L3268) |
| FJS-151 | litestone — **A post-update `@@allow` denial did not roll back on any model carrying a Json or Boolean column** — the write stood and the caller got a SQLite binding error **Fixed 2026-08-10.** The revert wrote the before-snapshot back column by column, and that snapshot had been through `read()`: a Json column is an object there and a SQLite parameter cannot be one, so the revert threw `Binding expected string, TypedArray, boolean, number, bigint or null` on its way out. Two failures from one line — the `AccessDeniedError` never reached the caller (a `TypeError` about bindings did, naming nothing), and **the update the policy had just refused was left applied**, since the throw happened before the revert ran. `read()` is also the wrong shape for the job in two more ways: it adds computed and `@from` fields no `UPDATE` can name, and it strips `@guarded` ones. The rollback now reverts from the raw SQLite row, whose keys are the table's columns exactly; `beforeRow` stays read-shaped for the audit snapshot. Found the first time a model with Json columns declared a policy — basecamp's `Server`, where "move this server to another workspace" is precisely what post-update exists to refuse. 1 test on a model with Json + Boolean, red on revert | 2026-08-10 | [client.js:4342](packages/litestone/src/core/client.js#L4342) |
| FJS-150 | litestone — **`include:` applied no access rule of the model it reached** — not `@@gate`, not `@@allow`/`@@deny`, not `@guarded`, not a field `@allow` **Fixed 2026-08-10.** Every documented guarantee held on a direct read and none of them survived being reached as somebody's child, because `resolveIncludes()` builds its own SQL below the query pipeline: `@@gate("7")` on `Vault` refused `db.vault.findMany()` and handed the whole table to `db.team.findMany({ include: { secrets: true } })`, `@guarded(all)` came back in plaintext, `@encrypted` came back as raw ciphertext (and did NOT decrypt for `asSystem()`), and a tenant filtered out of a row by `@@allow('read', workspaceId == auth().workspaceId)` still received it as `appServer.server`. The docs promise the opposite in as many words — *there is no path to unfiltered data except `asSystem()`* — and no test in 1462 covered a policy under an include. Three owners, three fixes: the gate is a **preflight** in `GatePlugin.onBeforeRead` (a mirror of the nested-WRITE preflight that was already there), walking `include:`, `select:` and `_count` and refusing by name, because `getLevel` is async and the include resolver is not — and because a gate is per model, so an empty list would read as *no rows*; the row policy is compiled into each of the three relation SQL shapes plus both `_count` shapes, subqueried in the m2m branch where the target is aliased beside the join table; and the field rules moved out of `makeTable`'s closure into `applyFieldPolicyTo(row, modelName, …)` so the include path can ask for a model that is not its own. 13 tests, mutation-checked — 10 fail on revert, the other 3 are controls | 2026-08-10 | [client.js:1446](packages/litestone/src/core/client.js#L1446) · [gate.js:174](packages/litestone/src/plugins/gate.js#L174) |
| FJS-149 | litestone — **`$transaction` on a scoped client handed the callback the ROOT client** | 2026-08-10 | `asSystem()`, `$setAuth(u)` and `$scopedBy()` each exposed the root `$transaction`, which calls `fn(clientProxy)`. So the body of a transaction ran unscoped, and it fails in opposite directions on the two flavors: `db.asSystem().$transaction(tx => tx.account.create(…))` was refused by the `@@gate` it was meant to bypass, while `db.$setAuth(u).$transaction(…)` ran with `auth()` null — every `@@allow` matching nothing and every `@createdBy` stamping nobody, silently. Nothing had noticed because no schema in the repo carried both a transaction and a gate until basecamp's did: the first-run `POST /setup`, whose whole point is writing four models in one transaction as system, failed on the first request with `"Account.create" requires SYSTEM access (use asSystem())` — an error naming the exact call that WAS using asSystem. The `query()` batcher on both proxies already went to trouble to keep its scope alive through `$transaction` and says so in a comment; `$transaction` itself was the one that did not. Each proxy now passes ITSELF, and the root is unchanged. 3 tests, including the quieter policy half where an unscoped tx does not throw at all — it matches nothing, which reads as a bug in the transaction body. 1461 litestone pass; junction 919, sierra 810 + 5 safety, basecamp 270 |
| FJS-148 | mesa — **a `style:` directive resolving to `null` wrote the string `null` for some properties** | 2026-08-10 | `bindStyle` passed the value straight to `setProperty`, which leaves `null` to the DOM implementation: happy-dom wrote `position: null` and `z-index: null` and dropped `color` and `top` — same element, same render, different answers, so the rule was per-property and invisible. Every conditional style in the repo is written `style:x={cond ? 'v' : null}`, and `@frontierjs/ui`'s `Badge` shipped `<span class="badge success" style="position: null; z-index: null;">`. Invalid CSS is ignored by a browser, so the cost was never the paint: the SERVER's attribute and the CLIENT's disagreed, which is what hydration compares, and any snapshot of rendered HTML pinned the word `null`. Now `null`/`undefined`/`''` removes the property instead. 2 tests; 16 fail on revert, which is the measure of how much of the suite was rendering that attribute |
| FJS-147 | mesa — **`{#each}` over anything that was not a real array died as `array.map is not a function`** | 2026-08-10 | The block called `.map()` on whatever it was handed, so the failure named no block, no expression and nothing to search for. The case that bit is the array-LIKE: `{#each { length: 6 } as _, i}` is how a fixed-size grid gets written, and `@frontierjs/ui`'s `DatePicker` built **both** calendar panes that way — so the component threw on first render and **had never rendered at all, in any environment**, while compiling perfectly. It is one of the 35 the kit has never opened in a browser (`FJS-028`), which is the only reason that survived. `eachItems()` is now the one definition of what an `{#each}` may iterate: an array as-is, anything iterable (Set, Map, NodeList, a string) or array-like via `Array.from`, `null`/`undefined` as empty — and a number or a plain object **refused by name**, because both are typos with an obvious intent and converting one produces an empty list where the author expected rows. The number's message says what to write instead. Shared with `{#virtual each}`, which read the same getter. 8 tests; mutation-checked, 4 fail on revert |
| FJS-146 | mesa — **an `{@attach}` ran during SSR, where `el.animate` does not exist** | 2026-08-10 | `renderToHTML` is a real render against happy-dom, and happy-dom implements no Web Animations API — so an attachment that animates threw `el.animate is not a function` and took the **whole** render with it, not just the animation. `@frontierjs/ui`'s `Toast` was one; the kit's other overlays escaped only by sitting behind an `{#if open}` or a portal that renders nothing server-side. The cost lands where SSR is the product: a Sierra `static` route carrying one could not be prerendered. The rule was already written and simply not applied here — an attachment runs when the element MOUNTS (VISION §10.6), and a server render has no mount, which is why `$onMount`, `watchProxy` and path watches are already no-ops under `setRenderEnvironment(true, false)`. `attach()` and `applyAttachments()` now return early on `!_isClient`, next to the six existing guards that say the same thing. `Toast` is a render case in `@frontierjs/ui` for the first time. 1 test, red on revert |
| FJS-145 | junction — **a WebSocket frame could be DROPPED and nobody was told** | 2026-08-10 | Bun's `ws.send()` returns the bytes written, `-1` when the frame was buffered under backpressure, and **`0` when it was discarded** — the socket's buffer is full. Junction ignored that number at all five send sites (`ctx.send`, the `connected` ack, the connection ack, `Channel.sendRaw`, `sendToConn`), and the `BunWS` type even declared `send` as returning `void`, so no call site could have read it. A dropped `service_result` left the caller's promise pending until its own 30s timeout — no error, no close, nothing logged, and a screen sitting on "Loading…" while the server believed it had answered. A dropped `event` frame is quieter still: nothing is waiting on it, so the row is never updated and every open tab is silently stale. **Measured, headless**: one socket, 200 concurrent reads of a ~1MB payload — 193 never settled, and a call issued afterwards answered in 34ms, which is exactly what makes it read as *the socket is fine*. Closed by `transport/send-queue.ts`, one owner for *put this frame on that socket*: try to send, hold a dropped frame, flush on `drain` — and once anything is held everything queues behind it, because a frame that jumps the queue arrives before one sent earlier. The queue is bounded (`http.wsMaxQueued`, default 8MB): past the cap the socket is closed with 1013, the client rejects every in-flight call and reconnects, so a consumer that cannot keep up is TOLD rather than quietly served nothing. **There is deliberately no knob for Bun's own buffer** — `maxBackpressureLimit` is accepted and ignored (measured on 1.3.11: 64KB, 1MB and 16MB all start dropping at the same ~16.9MB), and a control that changes nothing is worse than none. 8 tests, both halves mutation-verified: the integration case delivers 200 of 200 frames with the send queue and 98 of 200 without it |
| FJS-144 | junction — **a `find` that answered one object reached the browser as an EMPTY list** | 2026-08-10 | `ServiceProxy.find()` normalises three shapes into a `ListResult` — a real envelope, a bare array, and `{ total, data: [...] }` — and everything else fell through to `return list(this.name, [])`. So a service whose `find` answered ONE thing (a runtime rollup, a settings object, a health snapshot) arrived as `{ kind: 'list', data: [], total: 0 }`: 200, no warning, and the screen rendered nothing while the API was answering correctly throughout — visible only from a browser, which is what made it cost two sessions. The normalization is right (`FJS-C08` makes the envelope a contract); what was wrong is that an empty list and *not a list at all* were the same answer. The fall-through now throws `ResultShapeError`, naming the service, what arrived (`typeof`, or the object's keys) and the three shapes that are accepted — and it throws on **both** transports, since the WS and HTTP paths share the one normaliser. Not dev-only: the browser client has no reliable dev flag, and a dev-only throw would leave production with exactly the silent failure being fixed. Throwing costs an app that was already broken a loud error and costs a correct app nothing. `find` still means a list — a service answering one thing gives it a name and is called as an action, which is the workaround basecamp's `GET /hub` already took. Checked against every custom `find` in basecamp's 20 services: all answer `{ total, data }` or a `findScoped` page, so none is affected. 6 tests in `tests/client.test.ts` — three accepted shapes, a single object, `null`, and the WS path |
| FJS-140 | junction — **a custom action answering `data` plus anything else lost the anything else** | 2026-08-10 | `wrapResult` decided list-vs-single by SHAPE alone: any `{ data, total }` (or `{ data, errors }`) was read as a paginated list and rebuilt from `total`/`limit`/`offset`/`data`/`errors`, whatever method produced it, with every sibling key dropped — no warning, no error, a 200. Basecamp's `dashboards.kinds` returned `{ total, data: WIDGET_KINDS, statSources, portalServices }` and the browser received the nine kinds and neither of the two vocabularies needed to configure them, so the picker offered widgets it could not fill in. The envelope is a deliberate contract (`FJS-C08`), so the fix is not *keep the extra keys*: **the METHOD decides the kind**, and `find` is the only method that promises a list. `wrapResult(raw, service, method)` now reads: `find` → a list or it throws; an array → a list; the bulk `{data,errors}` partial-write protocol → a list on any method; **everything else → a single, which unwraps whole and loses nothing**. So an action answering rows plus a summary keeps both, and the guess that dropped them is gone rather than made quieter. Same ruling closes the other end — a `find` answering a non-list is `ResultShapeError` naming the service, what arrived and any keys a list envelope cannot carry, which is `FJS-144` moved to the server side where the app's own logs can see it. `data` must now be an ARRAY to be a list: `{ data: {…}, total: 3 }` used to pass `isListResult()` and reach the browser as a list nothing could iterate. The browser client no longer carries its own copy of the rule — it calls this same function as `find`. Blast radius, checked: ~15 list-shaped ACTIONS in basecamp become singles, which reach the browser as the same `{ total, limit, offset, data }` object minus the wrapper, and every consumer in the repo reads `.data`/`.total` or `Array.isArray(raw) ? raw : raw?.data`; nothing reads `envelope.kind`. Every model `find` (Litestone's base, basecamp's `findScoped`) already answers exactly the allowed keys. 13 tests across `envelope.test.ts` and `client.test.ts`; 897 pass |
| FJS-139 | junction · sierra — **a service call issued from a channel subscriber could hang until some other call was made** | 2026-08-10 | **Closed as not reproducible on the current tree, with the reproduction now permanently asserted rather than stepped around.** Basecamp's `/volumes/` reloads on push (`service.on('*') → load()`), and after a burst of agent reports the reload did not settle: the screen sat on "Loading…" with no error and no rejection for the 15s the drive polled. The only reproduction of it was in `basecamp/web/test/verify.mjs` § 13c-ter — and it was MASKED there, by an `await goto('/volumes/')` that navigated before the last check, so the check passed whether or not the live reload ever settled. That goto is now gone: the check reads whatever the push-driven reload produced, and `bun run verify --reset` passes 262/262 three times consecutively in that state. **The cause is not confidently identified**, and this is recorded rather than dressed up: the only change in that seam since the report is [FJS-145](#closed), the silently dropped WebSocket frame — but that drop needs ~16.9MB of unacknowledged data on one socket, and three small agent reports are nowhere near it, so the honest reading is *it no longer happens and we cannot say which change stopped it*. What makes that acceptable is the unmasking: if it returns, the drive fails on the live path instead of navigating past it. If it does return, the suspects that were never cleared are in the per-call path rather than the socket — `ensureDeps()` in the WS `service_call` branch, and the litestone client held on `ctx.locals` across a burst |
| FJS-087 | mesa — **a component could not expose a method, and neither documented way worked** | 2026-08-10 | Two halves, both silent. `export function submit() {…}` was **deleted from the output** — the emitter skipped every `ExportNamedDeclaration` and only `export let` had been handled before it — while every reference to the name survived, so a component calling its own exported function from its template threw `ReferenceError` on the first interaction. **No render test can see that**: SSR dispatches no events, so the component renders perfectly and fails when a user touches it. It is now emitted as the plain declaration it is, with assignments rewritten through the signal setters like any other function body. The other half was `bind:this` on a component, which handed over the ANCHOR — a comment node — where VISION §10.2 and RULE 36 promise the exported interface: `ref.focus()` was a TypeError and `ref.count` was `undefined`. It is now `$runtime.componentApi(anchor)`, built from the same registration `pushProps` already used: `export function` methods, plus props as accessors onto the child's own signals, so `ref.count` is the current value rather than a snapshot taken at mount and `ref.count = 2` writes it. `bind:this` on a DOM element is untouched. Every other export form in an instance script is now refused by name — `export { x }` and `export class` were dropped in the same silence. **Five kit components had documented a method they did not have**: `Input`, `Select`, `Textarea` and `NumberInput` (`focus()`, `select()`) work with no change, and `Form.mesa` dropped the comment explaining why its api had to travel through an `onready` callback (`onready` stays for callers already using it). 14 assertions in [mesa/test/component-api.test.js](packages/mesa/test/component-api.test.js), every one of which either calls a method or reads a prop back after a mutation; proven in a browser by `example`'s `verify` (37), `verify:ui` (27) and `verify:public` (21) |
| FJS-082 | junction · sierra — **an overlapping `load()` landed in arrival order, so an older response overwrote a newer one** | 2026-08-10 | `resource().load()` was `store.set((await svc.find(q)).data)` — unconditional. A search-as-you-type box typing `ac` then `acme` showed the results for `ac` whenever the first request was the slower one, and stayed wrong until the next keystroke, with nothing thrown and nothing logged. A load is now STAMPED when it is issued and only the newest stamp may write the store; the superseded request is not cancelled and its own caller still receives its rows, because the ordering problem belongs to the SHARED store, not to whoever awaited a call. `createStore().find()` in sierra carried the same defect and takes the same guard. It also moved the second staleness path in that seam: sierra's `@version` map was filled from `load()`'s return value, so a load the store refused as stale left its versions behind anyway, and a WS push — which reaches the store as an upsert and passes through no call result — never updated them at all, so the row a second tab patched 409'd on the next patch from this one. Versions now follow the STORE, which is the one thing that knows which data is current; `get`/`create`/`patch`/action results still record on the way out, since a form reads a single record that never enters the list store. **Deliberately NOT closed here**: a superseded request is still sent and still answered ([FJS-011](#) is the related filter leak, and cancellation belongs with the owner that file argues for). junction 919 pass, sierra 810; `example` `verify` 37/37 and `verify:build` 37/37 |
| FJS-071 | litestone — **contested: "the audit logger writes 0 rows"** | 2026-08-10 | Settled in favor of *not a bug*, and **the explanation that won was itself wrong**. There is no ~1s buffer and nothing flushes on exit: `fireLog()` defers one event-loop tick via `setImmediate` and the jsonl driver then does a synchronous `appendFileSync` ([client.js:5666](packages/litestone/src/core/client.js#L5666), [jsonl.js:69](packages/litestone/src/drivers/jsonl.js#L69)). So a read in the SAME tick sees nothing and a read after any `await` sees the row — which is why litestone's own examples report 0, and why waiting a second on the strength of the old note would have taught the wrong lesson. The separately-real half is fixed: `resolveDbPath` now calls `resolve()` on every branch, so `path` is no longer CWD-relative. Both `CLAUDE.md` files corrected |
| FJS-065 | cli — **`admin:generate` emitted `.svelte`, wrong paths and lowercase-plural model names** | 2026-08-10 | All three gone. Output is `.mesa` throughout, citing Invariant 18 as the reason; paths go through a depth-aware `pathsAt(depth)` resolver rather than being assembled per call site; and a model is PascalCase singular with the plural belonging to the SERVICE, `pluralLabel` lowercased only for prose. One self-documented limitation remains and is not a defect: an irregular plural is guessed wrong, which is the same English-plural assumption `modelNameFor()` makes at the other end of the framework |
| FJS-062 | conduit — **doc/version skew, conduit a version behind its consumers' docs** | 2026-08-10 | Not reproducible: `package.json`, the package's `CLAUDE.md`, its `docs/AUDIT.md` and the root `CLAUDE.md` all say 0.1.0, and the only consumer (basecamp) depends on `workspace:*`, which cannot skew. Inherited from `drift-report.md` (2026-07-31); either it was repaired in passing or the original reading was wrong |
| FJS-060 | sierra/mesa — **`externalSignals` was a hand-maintained cross-package registry** | 2026-08-10 | Closed by having nothing to register. A module-level signal read bare in a template is only reactive if the CONSUMING build names it — in another package, by hand — and omitting a name fails in the worst possible way: the expression reads nothing reactive, so it is hoisted out of the render block and assigned once at mount. `{connected ? 'ws connected' : 'ws offline'}` read *ws connected* with the API stopped and across a reload. **Two thirds of the retirement had already happened** — the router's eight signals became the plain object `page`, junction's two became `status` — and the map was down to one entry, `theme`, in two spellings, which nothing in the repo read. `theme` is now `{ value }`, written through `watchProxy` like the other two, and the plugin passes **no map at all**. `externalSignals` survives in mesa as an app-facing escape hatch for a third-party package that does export a signal. **The finding that changed the shape of this row**: the plain-object replacement has the IDENTICAL silent failure — `{page.path}` with no `$:` watch is hoisted static exactly as a missed rewrite was — and by default it was **quieter than what it replaced**, because mesa's path tier reports an uncovered read only when the file already watches some other path on the same import, which says nothing about a component that watches nothing, and that is the shape the original bug had. `externalReactivityHints: 'strict'` covers it, existed, was opt-in, and **nothing enabled it**; sierra's plugin now does. Measured before finishing: 4 warnings over 97 app components, all on `resource.gate.<method>`, a level number the schema fixes — now `var` snapshots, which is what RULE 13 exists to say. After: **0 warnings over all 218 `.mesa` in the repo**, so strict costs nothing. The drift test that guarded the map is replaced by [sierra/tests/no-module-signals.test.js](packages/sierra/tests/no-module-signals.test.js), asserting the stronger property in both directions: `src/` exports no module-level signal, and the plugin declares none. `signal()` itself stays for `presence(channelId)`, which returns one from a call — no map could ever have described that. Breaking: `theme.get()` → `theme.value`, README §Theme rewritten; zero consumers in this repo. Verified by `example`: `verify` 37, `verify:build` 37, `verify:public` 21 |
| FJS-043 | junction — **`/metrics` reported `actions: []` for every service** | 2026-08-10 | `health.ts` read `svc.actions`, a key no service has. It now uses the shared predicate — `customMethodNames(svc).filter(m => isMethodAllowed(svc, m))` at [health.ts:260](packages/junction/src/transport/health.ts#L260) — the same one the manifest and OpenAPI plugins ask, so the three surfaces give one answer, and an action withheld by a `methods:` allow-list is not reported as available. **Found by this audit, already fixed in the tree**: the code carries a comment describing the defect in the past tense, so the work was done and the row was never closed — which is the failure mode this register exists to prevent, pointing the other way |
| FJS-040 | conduit — **secrets unredacted in logged/persisted target config** | 2026-08-10 | Closed by redesign rather than by adding redaction: a `TargetDescriptor.auth` is a `TargetAuth` carrying `{ type, ref }` — a REFERENCE, never material ([types.ts:32](packages/conduit/src/types.ts#L32)) — and the value is resolved at send time through a `CredentialResolver`. So the `JSON.stringify(descriptor.auth)` the sqlite store persists ([stores/sqlite.ts:65](packages/conduit/src/stores/sqlite.ts#L65)) writes a ref, and `CredentialError` omits the value by construction. There are no redaction helpers in the package because a secret can no longer reach the config object at all. Inherited from `drift-report.md` (2026-07-31) and never re-verified until now |
| FJS-029 · FJS-137 | ui — **55 of 64 components dropped every attribute their caller wrote on them, and all 22 display components dropped `id`** | 2026-08-10 | One defect, not two: `{...$attributes}` compiles to `restProps($option.props, declared)`, so an undeclared `id` passes through the spread and needs no prop of its own — `FJS-137`'s stated diagnosis (*`id` has to be declared as a prop*) was wrong, and adding 22 props would have been the wrong fix. Every component now forwards. **Where it lands is deliberately not uniform**: display, layout, feedback and overlay put it on the element `{class}` already lands on; a form control puts it on the CONTROL rather than the `.field-group` wrapper, because a `<label for>` and an `aria-describedby` have to reach that element. Two departures, each stated in the file: `FileUpload` uses the visible dropzone rather than the visually-hidden `<input type="file">`, and `DatePicker` has no control of its own — its trigger is the caller's slot content. Where `id` IS declared it means something else (`Toast`'s identity, `Tab`/`TabPanel`/`AccordionItem`'s pairing, the control a `Label`/`Field` points at, `Tooltip`'s generated id) and those are named in the suite with their reasons. `test/attributes.mjs` renders all 64 and asserts three things — an undeclared attribute lands, `id` lands, and **the caller's value REPLACES the component's own rather than duplicating it** — with the six it cannot render named rather than filtered, so nothing goes quiet. Mutation-checked: removing one spread turns it red. Proven in a browser: `example` `verify:ui` 27/27 and `verify` 37/37. **Writing the sweep found three defects nothing else could see**, because it is the first thing in the repo that renders every component: `FJS-146`, `FJS-147` (`DatePicker` has never been able to render at all) and `FJS-148` |
| FJS-023 | mesa — **`<mesa:element this={…}>` was not a feature and compiled without complaint** | 2026-08-10 | It exists. A tag cannot be interpolated into a template string — the string is parsed once, and the parse is what decides the element — so the element is compiled under the placeholder tag `mesa-dynamic-element`, and `$runtime.dynamicElement` transplants it onto an element built from the live expression: attributes copied, children moved. The whole ordinary element path runs over the placeholder first, so `class`, `on:`, `style:`, `bind:` and `{@attach}` need no special case. Wrapped in a `keyBlock` on the tag, because an element's tag is not writable and the alternative is an `<h2>` that answers to `h3` for every selector on the page. One documented limit: a **tag selector** in a scoped `<style>` cannot match it, since the scoper runs on the parsed template. **The silence was the larger half of this bug and it covered every unknown `mesa:*` name** — the element and all its children were dropped from the output, so a typo, a misremembered feature and a working element were the same event, and the build stayed green. Unknown names now error listing the eight that exist; a nested `<mesa:mounted>`, which gates nothing anywhere but the top level, says so. `@frontierjs/ui`'s `SectionHeader` replaced its explicit `h1`–`h6` `{#if}` ladder with one line, and `test/render.mjs` asserts the heading TAG for `level: 3` — a level that stopped reaching the DOM renders something that looks identical and carries a different outline. 12 assertions in [mesa/test/dynamic-element.test.js](packages/mesa/test/dynamic-element.test.js) |
| FJS-022 | mesa — **`{@const}` inside `{#each}` calling the loop index** | 2026-08-10 | **Closed as no longer real.** It was a defect while the index was a plain number — `{@const last = idx === list.length - 1}` emits `idx()` — and the index has been a signal in its own right since 2026-08-04, the fix for stale indices after a keyed move, which makes the call correct. Probed plain, keyed, destructured, nested inside an `{#if}` and inside a second `{#each}`, after a mutation, after a reorder, and through SSR: all correct, nothing thrown. The kit was already relying on it in `Combobox` and `MultiSelect`, both of which read the index in a `{@const}` and work. Pinned in `emission.test.js` rather than simply deleted, because the two halves are owned in different files — the compiler decides to CALL it, the runtime decides to hand over a getter — so either one moving alone brings back `idx is not a function`. `Breadcrumbs` keeps its precomputed flag: truncation is what decides which item is last, and its comment blaming the compiler is gone |
| FJS-021 | mesa — **a destructuring assignment to reactive lets emitted invalid JS** | 2026-08-10 | `[a, b] = [b, a]` compiled to `[$runtime.get($$sig_a), …] = …` — an assignment to a call. Clean compile, empty `analysis.errors`, module dead on load, and Vite reporting only *contains invalid JS syntax*; the kit's `DatePicker` used the swap idiom and threw before it rendered anything. Both rewriters recognized only a bare `Identifier` on the left, so a pattern fell through to the generic descent and every target was rewritten as a READ. The pattern is now mirrored into temps and each target written back through whatever it is — a setter for a reactive let, an ordinary assignment for a plain local or a member expression, so a pattern may mix them. Reproducing the pattern rather than rebuilding it is what keeps holes, rest elements and nesting working without the rewriter understanding them: only the leaves move. Two exceptions it does have to know — a shorthand property is expanded (`{a}` → `{a: $$d0}`), since the identifier there is both key and target, and a default is an ordinary expression that may read a signal, so it is rewritten in place. The IIFE answers the right-hand value, so `x = ([a, b] = pair)` keeps its meaning, and a pattern naming nothing reactive is left byte-identical. **Both rewriters needed it and neither could borrow the other's**: a handler in the script goes through `rewriteAssignments`, the same handler inline on the element goes through `rewriteExpr`, they index into different strings and disagree about what is reactive (only `rewriteExpr` knows the local scope) — so the shared shape is passed in and the walk is written once. Fixing one path alone passes a compile test and leaves every inline handler broken. 9 tests replace the pinned-gap test, driven through both paths; mesa 1044 pass, `example` `verify` 37/37 and `verify:public` 21/21 |
| FJS-020 | junction — **a custom method's return shape was load-bearing and nothing said so** | 2026-08-10 | A method's return value is also its broadcast payload, and those two jobs want different things. The browser store upserts BY ID and replaces wholesale, so an id-less payload was appended as a PHANTOM ROW and a partial one replaced the record and lost every omitted field — in every open tab, silently. Basecamp shipped four: `setVariable` → `{ id, variables }`, the deployment engine's five-field projection, `servers.heartbeat` → `{ ok, server_id, status }` with no id at all, and `jobs.trigger` → `{ id, queued: true }`. All four were found by looking at a SCREENSHOT — a page doing the obvious thing rendered `undefined` as its heading with every other assertion passing. Ruled by [FJS-D08](DECISIONS.md): **a method may answer what it likes; an announcement is about a row, so it carries one.** `announcementPayload()` in `core/litestone.ts`, called from the one announcement point: the payload when it already is a row (extra keys are fine — `{ ...job, queued: true }` is a row and a flag; only an omission makes it a projection), otherwise the row re-read by `payload[idField] ?? ctx.id`, otherwise the payload as the SIGNAL it is. Dropping that last case was the first design and was wrong: `volumes.report` changes many rows, has none to carry, and its subscribers use the event as a trigger to re-read — suppressing would have stopped a live screen updating with only a server-side line to say why, which is the same silent failure being fixed. Caught by reading what basecamp returns before running its drive. Warns once per `service.method`, naming the missing columns and both ways out; per-call would be a log nobody reads. `ctx.dispatch` and model-less services are left alone by design. The client's `Store.upsert` now refuses an id-less record as a last line, since a payload from a hand-rolled `channel.send()` never reaches the server-side check. **The response half is not fixed and cannot be** — a projection is still a projection to its caller, which is what the warning is for. 11 tests against a real Litestone client, mutation-verified: 5 of 10 fail with the resolver disabled |
| FJS-019 | junction — **junction pinned litestone `"latest"` while the workspace shipped a different version** | 2026-08-10 | Closed by re-probing: `packages/junction/package.json` now declares `"@frontierjs/litestone": "^1.1.0"` as a peer dep and `workspace:*` as the dev dep, so the range the package publishes against and the copy it is tested against are both stated rather than resolved. The recorded failure — a floating `"latest"` agreeing with the workspace by luck — cannot recur while the range is explicit |
| FJS-008c | vscode — **the extension could not be packaged** | 2026-08-10 | `icons/` now exists — `frontierjs.png` (128×128, generated from `icons/frontierjs.svg`) plus `litestone-light.svg` / `litestone-dark.svg`, one family with `example/`'s favicon: three bars, the middle one `--color-primary` `#0d83dd`. **The icon was not the only blocker, only the one that spoke.** With it in place `vsce package` still died with `invalid relative path: extension/../../.claude/settings.local.json`: bun installs the three runtime deps as SYMLINKS into the workspace root's `.bun` store, and vsce's dependency walk follows them above the extension root. `--no-dependencies` packs cleanly and ships an extension whose first `require('vscode-languageclient/node')` throws — in the marketplace, where nothing tests it. So `vscode:prepublish` now bundles: `scripts/bundle.js` (esbuild, already a dep and already bundling the parser) rewrites `out/extension.js` and `out/litestone/server.js` as self-contained CJS with `vscode` external and the computed `require('parser-bundle')` left alone. Proven against the ARTEFACT rather than the tree — `npm run verify:package` packs, unpacks the `.vsix` where no `node_modules` sits above it, asserts every icon `package.json` names is inside, asserts neither bundle bare-requires anything unshipped, and runs all 34 LSP assertions against the unpacked server (`FJS_LSP_SERVER` points the suite at any copy). A `LICENSE` and a `.vscodeignore` came with it; the vsix is 20 files, 292 KB. Still unpublished — `vsce publish` needs a marketplace publisher account, which is the only step left |
| FJS-008b | vscode — **Mesa support was written but switched off** | 2026-08-10 | `startMesaClient` is called from `extension.ts` and all three blockers are gone, each of which would have shipped an extension that throws on activation with nothing to catch it. **`allowJs`** — `mesa/{hover,completions,symbols}.js` are plain JS, tsc emitted none of them, and `require('./hover')` threw; they are also `import`ed rather than `require`d now, because esbuild leaves a computed require alone and the bundled `.vsix` would have shipped without them. **The compiler** is `@frontierjs/mesa` at `src/compiler.js`, not the pre-rename `@mesa/compiler/compiler.js`, and it is resolved from the workspace, from a `packages/mesa` above the edited file, from `mesa.compilerPath` (a file or the package directory) or from a sibling of `context.extensionPath` — never `__dirname`, which is `out/` bundled and `out/mesa/` from tsc. It is loaded through `new Function('specifier', 'return import(specifier)')`: tsc under `module: commonjs` rewrites a plain `await import(p)` into a `require()`, and `require()` of mesa's ESM throws in the extension host, which is a *silent* loss of diagnostics. **The duplicate grammar** was two copies of an older `mesa-language-support` in `~/.vscode/extensions` (publishers `frontierjs` and `your-publisher-name`) both claiming the `mesa` language id, plus a stale `undefined_publisher` build of this extension; all three removed by hand, since nothing in this repo can. A dev host shows what is INSTALLED. Proven by a second suite, `test/mesa.test.js`: **36 assertions** over the built `out/mesa/`, with a stubbed editor (`test/vscode-stub.js`) and the REAL compiler, covering activation, all five resolution routes plus the none-found prompt, diagnostics from an analysis error and from a `compile()` that throws, the debounce, and the three providers. `npm run verify:package` now runs it against the unpacked `.vsix` too, and asserts the providers and the opaque dynamic import survived bundling. Writing it found one more defect: a diagnostic's range came from matching a declared variable on a WORD BOUNDARY anywhere in the message, so `bind:group={missing} — 'missing' must be a top-level let variable` underlined `let a = 1` — "must be a top-level" contains a standalone `a`. Only a quoted name names a variable now |
| FJS-007 | basecamp — **no `@@gate` in the schema; access control was service hooks** | 2026-08-10 | All 37 models declare a level, and the resolver they are graded by is `api/src/core/gate.ts`. **What made this app different from `example/` is that its level is not a fact about the user**: the same person is `owner` in one workspace and `viewer` in the next, so `sessionGateLevel()` — which grades standing that travels with the row in `user` — could only ever answer USER(4) everywhere, including workspaces the caller is not in. The level is per REQUEST, off the `WorkspaceMember` row for the workspace being addressed: viewer/billing READER(2), developer USER(4), admin ADMINISTRATOR(5), owner OWNER(6), `isSystemAdmin` SYSADMIN(7) above all of them, an authenticated caller with no membership VISITOR(1) — which reads `Workspace` and nothing else, because that is the screen a fresh login needs. `applyStanding()` in `core/hooks.ts` puts `memberRole` on the PRINCIPAL and re-scopes the client, and it has to be the principal: junction's `getTable()` re-derives its own scoped copy from `ctx.auth.user`, so a standing living only on `ctx.locals.db` is dropped the moment a service touches a model. It is a fresh object, never a mutation — over WebSocket the session is resolved once at upgrade, shared by every frame, and frozen. It re-resolves when the workspace changes mid-request (the workspaces service addresses `ctx.id`, not the header), without which an admin of the workspace they are LOOKING AT carried level 5 into a patch of any other workspace they could name. **The levels are not a design drawn on paper** — they are the `requireWorkspaceRole` calls the services were already making, moved to the one place that also covers an engine calling a service in-process, a custom action nobody wired a hook onto, and a where-clause built by hand. The hooks stay: a gate refuses with a level and a person needs the sentence. Two departures from the levels `db/README.md` had recorded since Phase 1, both because a path reads what it needs: `Recipe` reads at 4, not 5 (running one is a developer's act and running it means reading the script), and the machine-written models read at 2 rather than being SYSTEM throughout (a person's action writes the `ServerEvent` beside it). `AuditEvent` is `"5.8.9.9"` — LOCKED on update and delete, which `asSystem()` does not pass either, so `db:seed --force` cannot clear the table and lets the workspace FK cascade do it. Also fixed on the way: `runSeeder` ran on the root client, which grades STRANGER(0) — the seed's own header said everything ran as system and that was the one line that did not. **Proven by refusal, not by absence**: 8 new tests in `db/test/schema.test.ts` against a real client and the app's own resolver, plus 8 checks in the browser drive asked as a second human — a viewer reads the fleet and creates nothing, a developer writes projects and is still refused `GET /secrets` **with the level named in the message**, which is the check that fails if `memberRole` never reaches the principal, since nothing else refuses that read. `verify` 270/270 (was 262). Ruled in `DECISIONS.md` § Basecamp's gate ladder. Still open beside it: `@@allow` for row-level tenancy, which the gates deliberately do not express |
| FJS-003 | auth — **the package could not be published** | 2026-08-10 | All three causes gone and proven the way the row asked for — `npm pack`, then install the tarball into an empty project and import it. The 8 `../junction/index.ts` imports across 5 shipped files are now `@frontierjs/junction`, which is what conduit, caravan, notifications, basecamp and `sierra/example` were already writing — auth was the only package reaching out by path. The peer range is `^0.1.0` rather than `"*"`, and `files` is `["*.ts", "README.md"]`, so the tarball is 10 files: the sources, the manifest and the README, with `tests/`, `tsconfig.json` and `node_modules/` gone. The install probe resolves the three RUNTIME value imports (`parseTtl`, `createScheduler`, `Unauthorized`/`BadRequest`/`TooManyRequests`) through the specifier and builds a plugin. **The peer landed the same day** — `@frontierjs/junction@0.1.0` is on npm, so `bun add` of the auth tarball into an empty project now resolves junction from the registry and imports. Before that it 404'd, and it 404'd from a sibling `file:` tarball too, with `"*"` as the range as well — bun declines to satisfy a semver peer from a `file:` dep, so that failure was never about the range |
| FJS-001 | repo — **nothing was committed** | 2026-08-10 | `git status --porcelain` was ~465 paths with `example/` untracked and `build/`'s 20 source files un-ignored but unstaged, so the repo's entire working state was one `rm -rf` from gone and no change could be bisected, reverted or attributed. Now 9 modified files, `example/` tracked, `build/` un-ignored and committed. **The `.gitignore` half is the part worth keeping**: `build/` matched `packages/*/src/build/`, which is SOURCE — sierra's whole build pipeline and jetty's — so the ignore rule hid 20 files that no one had deleted and no one could see. It is now negated explicitly, and it takes **two** lines — `!packages/*/src/build/` and `!packages/*/src/build/**` — because git cannot re-include a file whose parent directory is excluded, so negating the contents alone silently does nothing. Check any new ignore rule against `git status --porcelain` |
| FJS-138 | css — **The Surface lineage had no notion of a link — `<a class="card">` rendered underlined and did nothing on hover.** Both halves fixed 2026-08-09. (1) `src/foundation/reset.css`, one rule in a new first layer: `a:where([class])` loses the UA underline while a bare `<a>` in prose keeps it, because colour alone is not an accessible link affordance and a wider selector would strip it silently. (2) `surface.css` gives `:where(a, button):where(.surface, .card, .tile)` a cursor, a 1px lift and a tone-following border. It keys on the ELEMENT rather than a `.hover` modifier — the opt-in pattern elsewhere (`.rows.hover`, `.items.menu`) exists because an `<li>` or `<tr>` is not focusable and an unconditional hover would advertise an interaction the markup cannot deliver, and an `<a href>` has no such problem. So the affordance cannot be attached to a static card or forgotten on a link. **Three near-misses, each of which rendered plausibly and is now pinned by a test**: moving `--surface-bg` to `--surface-raised` did nothing in the default theme (the two tokens are both `#ffffff`) and erased `.outlined`, whose whole variant is a transparent background; writing the border as `var(--surface-tint-border, <brand>)` fired the fallback only when the tint was invalid — never on a toned card — so `.card.danger` hovered to exactly the red it already had; and without `(hover: hover)` a tap left the card lifted, reading as a stuck selection. The border is now one `color-mix` toward `--bg-mix`, so untoned goes brand-blue and danger goes a deeper red. Alert, Toast, Popover, Dialog and Drawer are deliberately excluded: a notification is not a navigation target. Found by asking what in the guide's next-page footer could not be replaced by a shipped term; that footer now declares nothing. 7 tests in `core-gaps.spec.js`, all four mutation-verified | 2026-08-09 | [css/src/foundation/surface.css](packages/css/src/foundation/surface.css) · [css/src/foundation/reset.css](packages/css/src/foundation/reset.css) |
| FJS-136 | auth — **`revokeApiKey` silently revoked nothing on any app whose `Credential.id` is not an `Int`** | 2026-08-09 | It ran `delete({ where: { id: Number(keyId) } })`, because this package's own `schema.ts` fragment declares `Credential.id Int @id`. But the fragments are a starting point apps edit — basecamp declares `String @default(uuid())`, like every other id in that schema — and `Number('a1b2-…')` is `NaN`, which matched no row **and threw nothing** (verified: `delete where id=NaN → no throw`). Revoke returned success and the key kept working, indefinitely, which is the worst outcome available to this function. The coercion is gone — Litestone coerces a where-value to the column type either way, verified against both shapes — the delete is filtered on `type: 'apiKey'` as well as the id so a wrong id cannot take somebody's password with it, and a revoke that matches nothing now throws instead of lying. One behavior change came with it: **verifying is quiet, issuing is loud.** `verifyApiKey` used to throw `AuthConfigError` with no `encryptionKey`; it answers `null` now, because it runs on attacker-supplied input on every request and `verifySession` falls through to it — a 500 anyone could trigger with a Bearer header. `createApiKey` still throws, which is where a developer is. 4 tests |
| FJS-135 | auth — **the scopes a key was issued with were stored and then dropped, so every key had its owner's full standing** | 2026-08-09 | `createApiKey(userId, { scopes })` writes them space-joined into `Credential.scope`, and `verifyApiKey` returned `toContext(user, 'apiKey')` — built from the USER row, which has no idea a key exists. `SessionContext.scopes` was therefore always `undefined`: a key issued `servers:read` authenticated with exactly the permissions of the person who made it, and nothing downstream could tell the difference between that and an unrestricted key. A scope checkbox that changes nothing is worse than no scopes at all, because it reads as a control. The session now carries `scopes` (absent, not `[]`, when a key was issued without any — absence means *no opinion*, the same rule `verifiedAt` follows) and a new `credentialId`, without which an app cannot say WHICH of a user's keys made a call: two keys on one user produce sessions that are otherwise identical, so per-key usage and per-key revocation have nothing to key on. 4 tests |
| FJS-134 | auth — **an API key could be issued and could never authenticate anything** | 2026-08-09 | `createApiKey()` worked, stored an HMAC, returned a token — and every request carrying that token was anonymous. Junction's HTTP transport resolves a Bearer token through **`auth.verifySession()` and calls `verifyApiKey` nowhere** (`transport/http.ts:385`); the better-auth provider knows this and falls through, the native one did not. So the whole API-key half of `IAuth` was reachable only by calling it yourself, which nothing does. The failure is silent in the worst direction: minting looks like it worked, and the key fails as *unauthenticated* rather than as *not a key*, so it reads as a bad token. `verifySession` now routes on the `fjs_` prefix — the format is this package's own, so a machine request costs no wasted session lookup — and falls back to `verifyApiKey` for any token that missed, which covers a key issued before the prefix existed. Found building basecamp's `/api-keys/`. Verified in a real browser against a running app: an issued key returns 200 from `GET /servers`. 3 tests |
| FJS-133 | css — **the demo's pagination was unstyled, because it wrote a class the package refused to ship** | 2026-08-09 | Every control carried `class="page"` and the gap `class="page-gap"`. Neither is shipped, and `nav.css` documents by name why not: *"the control is a link, not a page. Shortening the class to `.page` puts one word on two subjects"* — Page is also a vocabulary TIER, and Previous/Next are neither a page nor numbered. So the demo rendered raw UA links: measured `rgb(0,0,238)`, `padding: 0`, `border-radius: 0`, no fill on the current page. The demo is the package's flagship consumer and its README calls the markup *"the structural half of the system, followed strictly"*. Third instance in three days of the same hole — markup that no spec owns can name anything at all (`FJS-130` the wizard, `FJS-131` the demo's Feeds). Closed by `test/specs/demo.spec.js`, two checks: every class in `index.html` is shipped or is demo-owned (`demo-` prefix), and every class-carried vocabulary term appears at least once. `run.js` now inlines the demo markup — and it must be `escapeForInlineScript`'d even though `JSON.stringify` quoted it, because the demo is an HTML document carrying its own `</script>` tags, which end the inline block whatever the JavaScript around them means. That cost one debugging round: the spec read `undefined` and reported all 54 terms absent. Proven to bite: reverting the rename turns it red. The kit's `Pagination.mesa` was already correct; the one other hit in the repo is a stale `example/web/dist/` artefact predating that fix |
| FJS-131 | css — **the demo's two Feeds rendered with no timeline, the same defect as `FJS-130`** | 2026-08-09 | Both wrote `<li class="feed-item">`, collapsing the two elements ANATOMY keeps apart. The connecting line is `.feed > li:not(:last-child) .feed-item::after` — a DESCENDANT selector — so the collapsed form matches nothing: measured `content: none`, zero height, a column of dots with no line between them. Found by covering the whole vocabulary in the demo, days after the identical bug was fixed in the guide's wizard, which is the argument for the check in `FJS-130` being data rather than review: three separate authors wrote this same shape and no test in the package could see any of them, because every check asks whether a class is PRESENT. The `<article>` inside the `<li>` is load-bearing and now says so in a comment at both sites |
| FJS-130 | css — **the Learn wizard taught markup that does not render, for four terms** | 2026-08-09 | The wizard's sample is the end of every path and the only markup in the guide no reference page owns. Four of the 52 omitted parts their own term is built from: **Feed** wrote `<ol class="feed"><li><article>` with no `.feed-item`, `.feed-dot` or `.feed-content` — a plain list, no dot column and no connecting line, which is exactly the distinction the term exists to make; **Steps** wrote no `.step-marker`, and the numbered disc AND the connector between discs are both drawn off it; **Disclosure** and **Empty** wrote bare elements where class-styled parts belong. Invisible to every existing check: `anatomy.spec.js` renders ANATOMY's own block, which is correct, and `decisions.spec.js` asked only whether the classes a sample DID write are shipped — a class you never write is never wrong. Found while fixing the Feed blurb, which described a "chronological stream" and never mentioned the timeline the CSS draws (VOCAB, the wizard option and its lead all now say so). Closed by `decisions: a wizard sample writes the parts its term cannot render without`, which resolves a borrowed part's optionality **through the term that owns it** — `uses` is a bare class list and the `'optional'` flag lives on the owner, so reading it as required reported all three Surface sub-regions against Card, Dialog, Drawer and Popover, four terms that are right. That distinction is the design: a wizard sample is the smallest thing that renders, not the exhaustive reference. 19 reported → 7 real. Proven to bite: breaking both `.feed-dot`s turns it red |
| FJS-127 | ui — **`Drawer` styled `.drawer[open]` locally, in the kit, for the package's own term** | 2026-08-08 | Four lines making the drawer a flex column so a `.surface-body` between a header and a footer takes the remaining height. It is now in `drawers.css`, the file that owns `.drawer`. The `[open]` scoping is the whole subtlety and it survives the move with a comment: an author `display: flex` beats the UA's `display: none`, so declared on `.drawer` the drawer renders on screen while shut — the exact trap `overlay: a closed <dialog> stays closed` already records. Pinned by a new test that asserts both halves, and proven to fail: dropping `[open]` turns two tests red |
| FJS-126 | ui — **`DropdownItem` carried a control reset `@frontierjs/css` ships, and had drifted the row** | 2026-08-08 | The local `<style>` is deleted; `lists.css`'s `.items :is(button, a).item` owns the reset and `.item` owns the layout. Four of the eight local declarations restated `.item`, and one of those — `gap: 0.625rem` — disagreed with the `var(--space-sm)` every other Item reads, so a menu row's icon sat a rung further from its label than an identical row outside a menu **and did not move inside a `.dense` region**, which the literal made impossible rather than merely wrong. `DropdownMenu` renders `<div class="items menu">`, so the descendant selector matches and the disabled pair keeps its `.items` scoping, which is what lets it outrank the `(0,3,0)` hover. Verified in a real browser: `example`: `verify:ui` 27/27 including `menu.items` and `menu.escapeCloses`, plus `verify` 37/37 |
| FJS-122 | junction · sierra — **a collection-level custom action was unreachable from the browser** | 2026-08-08 | The bridge dispatches on the `X-Service-Method` header BEFORE it looks at `params.id`, so the server has always accepted an action about a whole service rather than one row. Both client layers interpolated the id unconditionally — `${base}/${id}` — so the only way to reach one was to invent a throwaway id and post to `/{service}/null`. `action(name, id?, data?, query?)` now posts to the service root when `id` is null or omitted, and carries a plain filter query (`?limit=50`, not the `$`-prefixed directive syntax `buildQueryString` emits — an action declares its own query vocabulary). Found writing basecamp's fleet-wide event feed and its flag `resolve`, neither of which has a subject row. 4 junction tests; 884 junction, 809 sierra, 5/5 `test:safety` |
| FJS-121 | conduit — **any non-JSON response was a `server_error`, so a Slack webhook could never succeed** | 2026-08-08 | The HTTP transport refused every content type that was not JSON. The check was written for one real case — a 200 carrying HTML is a captive portal, a proxy interstitial or a provider error page — but spelled as "not JSON", which is a far wider net: **a Slack incoming webhook answers `200 text/plain: ok`**, so `app.conduit.send()` reported a failure for a notification that had already been delivered. Now only markup fails (`text/html`, `application/xhtml+xml`); anything else non-JSON comes back as the text it is. Found wiring basecamp's notification channels through the outbound boundary, where the test delivery arrived at the sink and was reported as an error. 1 test; 193 pass |
| FJS-120 | litestone — **a JSON Schema `default` was emitted as a value of the WRONG TYPE, and the boundary then refused its own default** | 2026-08-08 | An array or `Json` field spells its default as a STRING in the `.lite` source, because that is how it is stored: `tags String[] @default("[]")`. `generateJsonSchema` passed it through verbatim, producing `{"type":"array","default":"[]"}` — a schema whose own default fails its own type check. Junction's `autoValidate` fills the default in and then validates it, so **every create that OMITTED the field 400'd with `tags must be an array`, naming a field the caller never sent**; sending `tags: []` explicitly worked, which is the opposite of what a default is for. Structured defaults are now parsed before emission, keyed on the FIELD's type rather than on the literal looking like JSON — a `String` column defaulting to the two characters `[]` still means those two characters — and a literal that does not parse is dropped rather than emitted wrong. Found declaring basecamp's `FeatureFlag`. 4 tests |
| FJS-118 | litestone — **raw SQL always ran on the READONLY connection, so the documented escape hatch could not write** | 2026-08-08 | `_runRawSql` sent every statement to `readDb`, which is opened `readonly` with `query_only = ON`. Every raw write through every surface failed with `SQLITE_READONLY: attempt to write a readonly database` — a message about a connection, naming nothing the caller wrote. It hit `db.sql`, `db.$setAuth(u).sql`, `db.asSystem().sql` and **the system client a JS migration is handed**, which is the caller most likely to need a raw `ALTER`/`UPDATE` and least able to route around it. On a schema declaring access rules `asSystem().sql` is the ONLY way to run a raw statement, so raw writes were unreachable outright. Statements now route by kind: `SELECT`/`EXPLAIN`/`VALUES` stay on the reader, everything else goes to the writer, which reads perfectly well. `WITH` counts as "everything else" on purpose — `WITH x AS (…) DELETE FROM …` is legal SQLite, so a CTE cannot be assumed to be a read; leading comments are stripped before the test so `-- why\nDELETE …` is not misread. Found in `basecamp`, hard-deleting a row to prove an FK cascade fires — the one thing `.remove()` cannot do on a `@@softDelete` model. 3 tests; 1458 pass |
| FJS-104 | basecamp — **no workspace-wide read for `ServerEvent`** | 2026-08-08 | Server events were reachable only through `servers.events`, per server, so any screen wanting the fleet's stream needed one request per server merged in the browser — and `/activity/` therefore covered `AuditEvent` alone, a record of what PEOPLE did, while the mock's feed is mostly what the machines did. `servers.feed` is the collection-level read: the server ids in this workspace, then the events on them, two indexed queries instead of N+1 over the network. `ServerEvent` still carries no `workspaceId` of its own — an event is meaningless without its server, and denormalising the tenancy fact onto it would be a second owner — so the scope is the join. The server's NAME is resolved server-side rather than shipping the whole row per event. `/activity/` now merges both sources by time, because an operator reading "what happened" does not care which table it came from, and a drain followed by the agent confirming it only reads as one story if they sit together. Reaching it needed `FJS-122` first: there is no subject row, so it is `POST /servers` with the header |
| FJS-085 | repo — **no suite probed a production build, so a blank page shipped green** | 2026-08-08 | Basecamp's `web/index.html` mentioned the body tag inside a comment; Vite injects the built `<script>`/`<link>` at the first textual match without skipping comments, so both landed *inside* the comment and `dist/` loaded no JS and no CSS — white screen, empty console, `bun run build` exit 0. Fixed in basecamp 2026-08-06 and in `example/` on 08-04: twice, found twice by eye. `example` gained `verify:build` (the same 37 assertions against the built output); **basecamp had nothing**, which is where the trap actually landed. It now has `bun run verify:build` — `web/test/preview.mjs` serving `dist/client` with the API proxied (basecamp mounts services at the ROOT, so the proxy matches a route list rather than an `/api` prefix), and `verify-build.mjs` checking two layers. The file: comments STRIPPED FIRST, then require a `<script>` and a stylesheet `<link>` to survive — a regex over the raw file passes on exactly the broken output, since the tags are there and inert. The page: loaded in a real browser, required to render something, to have fetched its own JS and CSS, and to log no errors — a file can be well-formed and still throw on first execution. Deliberately not all 150 assertions against the build: those need an empty database and a full setup run, and the unasked question was only *does the built page come up at all* |
| FJS-110 | mesa — **two components separated only by whitespace shared one compiled anchor** | 2026-08-07 | A pending label request is satisfied by the next text node, and the compiler's `tpl` keeps those as separate entries while the emitted template is one **string** — where adjacent text parses as a single DOM `Text` node. So `<Button>Attach</Button>` and `<Button>Cancel</Button>` on consecutive lines inside an `{#if}` both resolved their anchor to the one whitespace node between and after them. **The DOM was correct** — each component inserts before the same node, in source order, so it rendered, laid out and clicked exactly as written — but `_componentRegistry` is keyed BY ANCHOR, so the second `registerComponentAnchor` replaced the first and the first component **never received another prop push for the life of the page**. That is why a plain `<button>` two lines up, carrying the identical expression, kept following it: an attribute binding writes the node directly and needs no registry. Fixed by giving every component invocation an anchor comment of its own instead of letting it adopt the text beside it — which also made several templates *smaller*, since a component no longer needs a filler comment plus a separate label. 8 mesa tests over the shapes (`{#if}`, `{:else}`, `{#each}`, inside an element, at component root, adjacent with and without whitespace); 4 of them fail if reverted, checked. `basecamp`'s `verify` now asserts the button follows the picker (135), `example` 37 dev + 37 built + 27 ui + 21 public. **The lesson is the eight probes that missed it**: every one put a static element between the two components, which is exactly the thing that made the anchors distinct. A reproduction that "simplifies" the layout can delete the bug |
| FJS-155 | A `.mesa` import by package specifier could not be resolved by the SSR renderer | 2026-08-06 | `import Email from '@frontierjs/email-kit/components/Email.mesa'` — the usage that package's own README documents — was `path.resolve`d against the importing file and produced `…/api/emails/@frontierjs/email-kit/components/Email.mesa`. It worked inside the kit only because its own templates import `../components/…` relatively; the first consumer outside it hit the wall. Two halves: mesa's `compileTree` now resolves a bare specifier with `import.meta.resolve`, and the kit's `exports` map (`"./components/*" → "./components/*.mesa"`, which turned `Email.mesa` into `Email.mesa.mesa`) gained the extension-carrying pattern. Same family as [FJS-100](#) — correct from within, broken from without |
| FJS-117 | litestone — **`$checkWhere` existed only on the ROOT client, so every list read by a signed-in caller 500'd** | 2026-08-06 | `FJS-109` added `db.$checkWhere` inline in the top-level proxy's `get` trap. The three derived clients — `$setAuth`, `asSystem`, `$scopedBy` — never got it, and a Litestone proxy **throws** on an unknown property rather than answering `undefined`, deliberately, so a typo'd accessor is loud. Junction hands a service `$setAuth`'s client on `ctx.locals.db`, so `autoFilter`'s capability probe `typeof client?.$checkWhere !== 'function'` was itself the throwing expression — and it sat *outside* the try/catch guarding the call. Every list read in **both** apps died with `"$checkWhere" is not a table in this schema`, naming a table nobody had written. Which keys are filterable is a question about the SCHEMA; auth and scope have no bearing on the answer, so it is now defined **once** in `createClient` scope and handed to all four proxies, which give identical answers (pinned per flavor). Junction's probe now reads inside a `try` as well — belt and braces, and independently correct: `db` is app-supplied and a client that cannot answer must no-op, not 500. **The lesson is what it looked like**: the browser symptom was navigation, not data — basecamp's setup and login redirects never committed and `example/`'s `<Select>` options never populated, because the page each landed on did a list read that threw. It was blamed on a concurrent edit to `mesa/src/runtime.js` and recorded in HANDOFF as such; that was wrong. mesa's 1027 and sierra's 809 unit tests were green throughout, as were litestone's and junction's — **no suite reached the seam, because every test client was the root one**. `example`: 37/37, `basecamp`: 134/134, `sierra`: `test:safety` 5/5 |
| FJS-116 | ui — **`<Textarea>` silently ignored `oninput`, while `<Input>` and `<Select>` both honored it** | 2026-08-06 | Textarea declared `onchange`/`onblur`/`onfocus` and kept its own `handleInput` for the internal value and autogrow, so a caller writing `oninput={…}` got no handler and no warning — an undeclared prop is just ignored. Found filing a TLS certificate through `<Form>` in basecamp: the two PEM fields stayed empty and the service answered `certPem and keyPem are both required`, with nothing on either side saying why. `handleInput` now calls `oninput?.(e)` after the internal write and the autogrow, so a handler that re-renders cannot cancel either. 64/64 compile, 25/25 render, 7/7 form |
| FJS-114 | mesa — **`{@attach}` ran on a DETACHED element, so every overlay in the kit was invisible** | 2026-08-06 | VISION §10.6 says the attachment runs when the element *mounts*; it ran when the element was BUILT, before anything inserts it, so every attachment saw `isConnected === false` and `parentNode === null`. `focus()` is a no-op there and a rect is all zeros — but the killer is `el.animate([{opacity:0},{opacity:1}], { fill: 'forwards' })`, which on a disconnected node returns an animation with `startTime: null` that **never starts even after the element is connected**, and is not listed in `getAnimations()`. The element paints at keyframe 0 for good. `@frontierjs/ui`'s CommandPalette was therefore a `position: fixed; inset: 0; z-index: 9000` backdrop that was **completely invisible and swallowed every click on the page** — reported as "clicking Search ⌘K does nothing and the whole app freezes". Toast, Popover, DropdownMenu, ConfirmationPopover and AlertProvider use the same pattern. Fixed by deferring the attachment to a microtask — the queue `$onMount` already uses — when the element is not yet connected; an already-connected element still runs synchronously, so nothing correct is reordered. 3 mesa tests + `example`'s `verify:ui` now opens the palette by CLICKING the header button (only ⌘K was ever driven) and asserts computed opacity, hit-testing and size, not presence. Both fail if reverted, checked. **The lesson is the assertions**: 26/26 passed against an invisible overlay for as long as it existed, because every check asked whether the DOM was there |
| FJS-113 | litestone — **`@from` against a model with no relation parses, then silently omits the field** **Fixed 2026-08-06.** The field was absent from every row — not `0`, not `null`, no key — because the SQL builder skipped it on `if (!fkCol) continue  // validation catches this` and validation did not. **Root cause was two owners of one rule**: FK inference lived inline in `buildFromMap()` while `validate()` checked only that the target model and types existed. Extracted to `inferFromFk(model, targetModel)` in the parser, exported, called by both — validate rejects what the builder could not have built, so the `continue` is now unreachable. Error names the relation to declare. Preserved: the `<Model>Id` fallback, and following `references:` rather than assuming the PK (an FK onto a non-PK `@unique` column still correlates — verified end to end). 1443 litestone tests green | 2026-08-06 | [parser.js:2331](packages/litestone/src/core/parser.js#L2331) · [client.js:760](packages/litestone/src/core/client.js#L760) |
| FJS-111 | basecamp — **the schema's own default was a value the API refused** | 2026-08-06 | `AlertRule.severity` was `String @default("medium")`; the alerts service accepted only `info \| warning \| critical`. Two owners of one vocabulary, disagreeing — and unnoticed for as long as it was, because `AlertRule` had no service at all, so nothing could reach the model to try it. Found the moment the screen submitted a form without touching the severity control. Fixed by making the schema the seed: `enum AlertSeverity { info warning critical }`, `@default(warning)`, migration regenerated, and the service's hand-written list **deleted** — the column now carries a CHECK, `autoValidate` refuses a bad value before the method runs, and the UI's `<Select>` reads `alerts.fields.severity.enum`. One declaration, three consumers. The verify check leaves severity untouched on purpose, so the default has to arrive from the schema for it to pass |
| FJS-109 | junction — **an unknown query key silently filtered to nothing** | 2026-08-06 | `?bogusColumn=7`, `?limit=100` (a directive in the filter slot) and a genuinely empty table all answered `200 {"data":[],"total":0}`. **Litestone knew the whole time** — it validates where-keys, rejects them on writes, and on reads prints *Unknown field 'bogusColumn' … Valid fields: id, name* to the SERVER'S stderr, invisible to whoever typed it. That warn-on-read / throw-on-write split is right for the ORM (a typo'd filter on a write is a mis-scoped destructive operation) and wrong at a boundary that can answer 400. So the boundary asks: litestone gained `db.$checkWhere(accessor, where)` — the same rule, the same Levenshtein typo hint, the same AND/OR/NOT descent, no warning and no query — and junction's `autoFilter` derived hook turns a non-empty answer into a `BadRequest` naming **every** bad key, its suggestion, the filterable fields, and the `$limit`/`$offset`/`$orderBy`/`$select` directives it was probably meant to be. One definition, junction contributing only the status — same division as `gateAuth`. A client without `$checkWhere` no-ops: *I cannot judge this* is not *this is wrong*. 8 junction tests over real HTTP + 7 litestone. Two existing tests asserted on the LENGTH of a compiled `before` list and broke on the second derived hook; both now filter the exported `DERIVED_HOOKS`, which is what their own comments already said they wanted |
| FJS-108 | sierra — **a prerendered page linked no stylesheet and carried no body class** | 2026-08-06 | `wrapDocument` emitted the page's own scoped `<style>` blocks and nothing else, because a prerendered document is assembled by Sierra rather than by Vite's HTML transform — so a `target: 'static'` page shipped every `@frontierjs/css` class name in the app and not one rule behind them, and no theme, while the SPA built from the same source looked right. The build already emits the stylesheet; nothing linked it. `wrapDocument` now takes `stylesheets` (the CSS assets of that build, linked BEFORE the page's own scoped rules, which are the more specific statement) and `bodyClass` from a new `document: { bodyClass, lang }` config key. Read in `writeBundle`, not `generateBundle` — Vite's CSS plugin emits the asset in its own `generateBundle`, which runs after Sierra's, so a hook earlier saw an empty asset list and linked nothing silently. 5 sierra tests, and `example/`'s `verify:public` asserts the link, the class and a theme token resolving in a real browser |
| FJS-107 | mesa — **every prerendered `<input>` carried `formaction="http://localhost/"`** | 2026-08-06 | happy-dom's `cloneNode` re-derives an input's attributes from its default PROPERTIES, so each clone of a template gained `formaction` (the build machine's own URL) and `formmethod=""`, and an authored relative `formaction="/search"` came back absolutised. Not cosmetic: `formaction` overrides its form's action, so a prerendered form posted to whatever machine built the site, and that machine's localhost URL shipped in a public file. `template()` now parses per instance on the server instead of cloning a cached parse — the same path that produced the original, so what ships is what was written; the client still clones. 3 tests, all three fail if reverted |
| FJS-106 | mesa — **a `.mesa` route with frontmatter was compiled as MARKDOWN** | 2026-08-06 | `compileSource` routed any source beginning with `---` to the Markdown compiler whatever its extension, and a `---` block is how every Sierra route states its title and render mode. Markdown escapes what it does not recognize, so `<CatalogList client:load products={data?.products ?? []} />` came out as a paragraph of ESCAPED TEXT with the props stringified into it — while `<LiveStock />` beside it, having no attributes, compiled as a component. It only bit callers handing the compiler a raw file: Sierra's Vite path strips frontmatter first, so dev and the SPA build were right and the PRERENDERER was wrong, for the same file. The extension decides the language now (VISION always said `.md` is the Markdown one), and `compile()` strips a leading block, exposes it as `ctx.frontmatter`, and replaces it with blank lines so warnings keep their line numbers. Mesa's three Markdown REPL examples are named `.md`. 6 tests |
| FJS-105 | sierra — **`@version` reached the browser and stopped there** | 2026-08-06 | `createResource` now keeps the version of every record it reads — from `get`, `find`, `load()`, `create` and each patch response — and puts it on the next patch. Kept per record rather than read off `store`, because a form usually loads one record with `get()`, which does not populate the list store at all; `load()` needed its own call, since it bypasses `_call` and a list whose rows are unpatchable is the same bug. A caller-supplied version still wins, and with nothing remembered the patch goes up without one so the server refuses — better than inventing a number that would silently win a race. Also `resource.version(id)` and `.versionField`. **The conflict needed one thing junction was not sending**: a 409 alone cannot separate a race (`VersionConflictError`, `TransitionConflictError` — re-read and retry) from a domain refusal (`TransitionViolationError` — whose own message is the right thing to show), so `retryable` is now adopted in `toFrameworkError` and serialized by `FrameworkError.toJSON`. `isStaleWrite()` reads it and `toFieldErrors` returns a sentence instead of a column name and two integers. Verified end to end through a real litestone update and a real HTTP round-trip: 409 retryable → the sentence, 409 non-retryable → its own message, 400 → the required-version explanation, success → version 1 → 2. 16 sierra tests + 3 junction |
| FJS-103 | A template's `subject` export could not depend on the render data | 2026-08-06 | `<script module>` runs once at import, before props exist, so `export const subject` could only be a constant — and a receipt's subject names the order. Module exports already came back as values, so a function export needed one change to work: the email document wrapper called `subject.replace()` to build its `<title>` and threw `subject.replace is not a function` out of the renderer. It now titles the document only when the export is a string and hands the value back as exported |
| FJS-102 | mesa — **`prop=""` on a component compiled to `prop={true}`** | 2026-08-06 | `inspectProp` decided the valueless-boolean form (`<Table striped>` → `striped={true}`) by testing the value for falsiness, and an explicit empty string is falsy. So the one spelling every `@frontierjs/ui` component documents for *suppress this* produced its opposite. The parser had never lost the distinction — valueless is `{ value: undefined }`, `prop=""` is `{ value: '' }` — so the fix is `value === undefined`. Found by wiring `<Select placeholder="">` into basecamp's workspace switcher: the placeholder rendered as an `<option>` whose visible text was the word **`true`**, which basecamp's browser suite caught as an unexpected third workspace. Silent by construction — a valid prop of the wrong type, a clean compile, a component that rendered. Element attributes take a different path and were never affected. 2 tests; mesa 1015, sierra 789, ui 64/25/7 all green |
| FJS-101 | A notification's email body could only be a list of lines | 2026-08-06 | `MailMessage` was `{ subject, lines, to }` and the driver rendered `lines` and nothing else — so `@frontierjs/notifications` and `@frontierjs/email-kit`, in the same repo, could not be used together at all. A receipt with a table of items was unexpressible. `mail()` gains `.html()` / `.text()`, and the driver prefers a rendered body **per field**, so a template supplies the HTML while the builder still writes the plain-text alternative. 5 tests |
| FJS-100 | Every test in `@frontierjs/email-kit` had been failing, and the package's own state file said "34 tests green" | 2026-08-06 | Two causes, found by sweeping the rename. (1) The kit probes for mesa's `render-component.js` at `../mesa/render-component.js` — the pre-`src/` layout — so both sibling probes missed, and its not-found test read only Node's phrasing while Vite says `Failed to load url … Does the file exist?`, turning a missed candidate into a rethrow before the candidate that works. (2) The real one, in mesa: `findMesaDir()` tested `dirname(import.meta.url)` for a `package.json`, and that file moved into `src/`, which has none — so temp render modules landed in the OS temp dir, where `import '@frontierjs/mesa/runtime.js'` cannot resolve. It now walks up to the nearest `package.json`. **Guarded 2026-08-06**: mesa's suite could not see this — every test in it runs from the mesa package root, which is the one cwd where the broken lookup worked — so `test/render-component.test.js` gained a case that renders in a CHILD PROCESS with its cwd outside the repo. Reverting the fix fails it with `Cannot find package '@frontierjs/mesa' imported from /tmp/__mesa_render_…`. The last-resort branch also warns now instead of silently writing somewhere nothing resolves. email-kit 34/34, mesa **1013**, sierra 789 |
| FJS-099 | `methods:` was silently ignored by `createBaseService` | 2026-08-06 | The allow-list added for [FJS-004](#) was read by `createService` and neither read nor forwarded by the other factory — the one the loader is built around (`export function createPostsService() { return createBaseService({}) }`). So the same `methods: 'readOnly'` that makes an audit trail append-only through one factory did nothing at all through the other, and the only symptom was a write that succeeded. Carried through now, resolved in the one place that validates it; 4 tests. Found declaring `methods: ['find','get','patch']` on `example`'s notifications service and getting a create that reached the model's gate |
| FJS-098 | The audit log could not record a String actor id | 2026-08-06 | Litestone's synthetic audit model declared `actorId Int`, and the jsonl driver's index is a STRICT table — so the first audited write with a known actor threw `cannot store TEXT value in INTEGER column auditLogs_idx.actorId` and took the request with it. Every FrontierJS app is exposed: `@frontierjs/auth` issues `id String @id @default(uuid())`. **It was masked by [FJS-097](#)** — with no `id` on the principal, `actorId` was always null, and NULL fits an INTEGER column; fixing one uncovered the other in the same afternoon. `actorId` is now `Any` (a real SQLite STRICT type), and an index whose columns no longer match the schema is dropped **and refilled from the `.jsonl`**, so history stays queryable rather than looking truncated. 3 tests |
| FJS-097 | A Junction session reached the Data boundary with no `id`, so every row policy matched nothing | 2026-08-06 | Junction's `SessionContext` names the caller `userId`; Litestone's policy language reads `auth().id` — its documented spelling. `$setAuth(ctx.auth.user)` therefore handed the Data boundary a principal with no id, and `@@allow('read', userId == auth().id)` compared a column to `undefined`: an empty list, no error. Gates were unaffected, because `sessionGateLevel()` was written against Junction's shape — so the half that was translated worked and the half that was not failed silently. Fixed with `toDataPrincipal()`, the one owner of that translation, applied at both `$setAuth` call sites; 9 tests. Found by `example/`, where a signed-in user's own notifications were invisible to them and plainly there under `asSystem()` |
| FJS-092 | litestone — **the bulk write paths skipped every `ctx.auth` stamp** | 2026-08-06 | Wider than filed. `upsertMany` stamped nothing — not `@default(auth().id)`, not `@createdBy`, not `@updatedBy` — and probing the whole table turned up a second hole: **`updateMany` never stamped `@updatedBy` either**, so `$setAuth(ann).updateMany(…)` left the column naming whoever last wrote through `update()`. That one was the worse of the pair: `@updatedAt` is a SQL trigger ([ddl.js:379](packages/litestone/src/core/ddl.js#L379)) so the timestamp kept moving, and a row read *just edited by Bob* when Ann edited it — a wrong name, not a missing one. Both paths now run the stamps, and in `upsertMany` a create-time column is held out of the `ON CONFLICT … SET` list so a conflict cannot rewrite the author — but only when *we* filled it, since a caller who names the column meant it. The four inline copies of the stamp collapsed to two functions that state the distinction: `stampFromAuth` (principal wins) and `applyAuthDefaults` (payload wins). 8 tests; **4 fail if the stamps are removed** |
| FJS-086 | `verify-ui.mjs` reported a rate-limited sign-in as a timeout | 2026-08-06 | `verify.mjs`'s `signIn()` copied across: wait for the badge **or** `.alert.danger`, then read the alert and name the 429. Proved by injecting a `.alert.danger` reading `sign-in failed: HTTP 429` before the click — the drive printed the limiter explanation and exited 1 instead of `waitFor timed out`. 26/26 still pass with the injection removed. The limiter is an in-process per-IP `Map` in `auth/plugin.ts`, so an API restart resets the window, which is what the message now says |
| FJS-081 | A `render: static` route could publish gated data as a public HTML file | 2026-08-06 | The prerenderer now collects each route's read set and refuses to emit a page whose data outranks what the route declares. Read set comes from litestone's `$tapQuery` around `load()` — **not** the render, which is where `IDEAS/static-safety.md` proposed watching and where nothing is read (static data arrives from the `.meta.js` companion as a prop, so a render-watcher would have observed an empty set and passed everything). Fail-closed: a route that pulls data the build cannot observe is refused, and the only escape is a per-route `publishes: N` written in frontmatter. 39 tests + a 5-check bun script against a REAL Litestone client (`bun run test:safety`); both halves fail when the check is removed. Exercised in `example/` — `bun run build:public` prints `/catalog/ 0 Product(0)` and exits 1 with the route, model and fix named when pointed at a gated model |
| FJS-080 | `bun run verify` could only be run once per database | 2026-08-06 | `seed()` guards per table and restores the three seeded orders by reference, resetting a status the drive moved. Demonstrated: a restart against a database with **zero** orders brought all three back in their expected states, and the drive then passed three times consecutively |
| FJS-076 | `<Form>`'s state machine had never run in a browser | 2026-08-06 | `example/web/src/routes/orders/create.mesa` rewritten onto it — ~90 lines of hand-rolled state machine deleted. `bun run verify` is **37/37 in dev and in the production build**, including all six live-validation assertions, which now pass against the rule as `<Form>` implements it rather than a per-app copy |
| FJS-075 | An unpicked relation picker silently selected the first row | 2026-08-06 | The placeholder `<option>` was `disabled`, and a disabled option cannot hold the selection — so a select whose options arrive late (every relation picker) lost the placeholder when the list repopulated. An unpicked "customer" became customer #1 and the order was filed against them. `example`'s `form.customerStartsEmpty` had been failing since before 2026-08-05 |
| FJS-051 | The email kit's package name did not match its directory | 2026-08-06 | Ruled [FJS-D15](#fjs-d15): it is `@frontierjs/email-kit`. `package.json` already said so; the old name survived in prose, comments, a user-facing error string and one filename, all swept. `drift-report.md` keeps it — a dated audit's findings are not rewritten |
| FJS-033 | Live updates were untested against a second client — and a custom action announced nothing at all | 2026-08-06 | Tested, and it found the gap. A watcher tab (signed out, never acts) sees `orders created` and `orders removed` cross the socket and apply with no reload, so channels DO reach a stranger — but the `pay` between them was never published: `callService` gated announcements on `AUTO_EVENT_MAP`, five CRUD methods, and an action is in none of them. The browser client had listened for action events since it was written. Fixed in junction (`?? (isAction ? method : undefined)`, 6 tests), ruled as [FJS-D21](#); the example's orders table dropped its `find()` refetch — that refetch was the mask. Pinned by `bun run verify:live`, 12 assertions, of which 4 fail if the one line is reverted |
| FJS-014 | litestone — **`Object.keys()` on a client threw, and `asSystem()` answered wrongly** | 2026-08-06 | Two strings. `$setAuth` and `$db` were on the proxy target AND in the `ownKeys` trap's hand-written list, and a duplicate makes the *engine* throw — so `Object.keys`, `getOwnPropertyNames`, `{...db}`, `for…in` and **`JSON.stringify(db)`** all failed on the top-level client and on a transaction proxy, with a message about proxy internals rather than either name. All five traps now go through one `dedupeKeys()` rather than having the two names deleted: the literal lists have grown before. **Probing turned up the quieter half** — `asSystem()`'s proxy had a `get` trap and nothing else, so it did not throw, it lied: `'user' in db.asSystem()` was `false` while `db.asSystem().user` worked, and it enumerated no tables at all, so a guard reading `if ('user' in db)` silently skipped the table under a system client. It now carries the same three traps. Junction's `try/catch` around `Object.keys(scopedDb)` stays — `db` is app-supplied and a stand-in must not turn *your model name is wrong* into a stack trace — but its comment no longer describes a live bug, and the list it produces was itself wrong the moment it started working: it offered `asSystem`, `sql` and `query` as model names. It now filters against `$schema.models`. 6 litestone tests + 2 junction, the latter against a REAL client — every existing test in that file used a plain object, which is exactly why the list went unverified |
| FJS-008g | vscode — **Hover synthesised attribute names from bare words** **Fixed 2026-08-06.** Candidates were `[word, '@'+word, '@@'+word]`, and the attribute table was consulted before the schema — so hovering the declaration `function slug(text: String): String` returned the docs for the `@slug` transform, and any model, field or enum named `trim`, `lower`, `upper`, `email` or `url` would have done the same. `wordAt()` already scans back through `@`, so an attribute always arrives prefixed and the extra candidates could only ever mis-fire. Found by the new suite on its first run | 2026-08-06 | [server.ts:559](packages/frontierjs-vscode/src/litestone/server.ts#L559) |
| FJS-008f | litestone — **`@from` docs teach a form that cannot parse** **Fixed 2026-08-06.** Every `@from` example in the docs was wrong **twice**: the first argument is the target **model** name (`@from(Order, …)`, not `@from(orders, …)` — *"unknown model"*), and `first:`/`last:` return the **whole related row**, so the field is typed as the model (`Order?`) — `lastOrder DateTime @from(orders, last: true)` failed on the type as well as the name, while its own comment said "last related object". `orderBy:` was undocumented everywhere. Rewritten in `relations.md` (+ an operation/type table and real output), `README.md` ×2, `schema.md`, litestone's `CLAUDE.md`. Verified: both schema examples parse straight out of the markdown, and the documented result rows are the actual output of running that schema | 2026-08-06 | [relations.md:133](packages/litestone/docs/relations.md#L133) · [parser.js:1107](packages/litestone/src/core/parser.js#L1107) |
| FJS-008e | vscode — **No test suite** **Landed 2026-08-06.** `npm test` = **34 assertions** over real LSP/stdio: `test/lsp-client.js` (Content-Length framing, id-correlated requests, `openDoc()` resolving on the diagnostics notification — no sleeps, one server process) and `test/lsp.test.js`. Covers diagnostics, the null-schema mid-keystroke state, block detection, caret-aware attributes, hover, formatting, and both real app schemas. **Builds before running** — a stale `out/` silently tests the previous fix. Plain node, no new deps, matching jetty's convention. **Mutation-checked**: reverting `isInsideBlock`, the caret-aware attribute detection, or the null guards each turns it red, the last reproducing the original `Cannot read properties of null (reading 'models')`. Writing it found one more defect (FJS-008g) | 2026-08-06 | [test/lsp.test.js](packages/frontierjs-vscode/test/lsp.test.js) |
| FJS-008d | vscode — **Completion offers attributes instead of types on any line that already has one.** **Fixed 2026-08-06.** `atTrigger` was `trimmed.includes('@')` over the WHOLE line, ignoring the caret, so editing the type in `status OrderStatus @default(pending)` — the shape of most real field lines — offered `@id`/`@unique` and never `OrderStatus`. Now read off `line.slice(0, character)`: `attrToken` (`/(@@?)(\w*)$/`) means "typing an attribute name", `inArgsOf` (`/@@?(\w+)\([^)]*$/`) means "inside that attribute's arguments". The five branches that keyed off `line.includes('@@gate')` and friends switched to `inArgsOf` — `@relation` had to, since it rode on `atTrigger` and the caret is past the `(` by then — and the type list is now suppressed inside an argument list, where a gate level is not a field type. 7 new caret assertions, incl. two caret columns on ONE line giving two different answers | 2026-08-06 | [server.ts:238](packages/frontierjs-vscode/src/litestone/server.ts#L238) |
| FJS-008 | vscode — **`npm run build` fails** — stale parser path in `scripts/build-parser.js`. Plus a null-schema crash **Both fixed 2026-08-06.** Parser had moved to `litestone/src/core/parser.js`; the resolver now probes `core/parser.js` then `parser.js` under each base. `parse()` returns **`schema: null` on any syntax error** — the normal mid-keystroke state — and the server typed it non-nullable, guarding `result?.schema.enums` (outer `?.` only, 5 sites) and not at all in hover (3 sites). Verified by driving the server over stdio with a broken schema: initialize + completion + hover all answer, the right diagnostic publishes, process stays alive. **A third bug fell out of that probe and is also fixed**: `isInsideBlock` incremented only on its own keyword but decremented on every `}`, so depth went negative after any earlier block and `inModel` was false for the rest of the file — an `enum` above the models (i.e. every real schema) silently killed type, enum and attribute completion. Now a stack of all block kinds; 9 stdio assertions cover enum-first, one-line enum, model-first, commented-out block, database driver, top level, and `example/db/schema.lite` | 2026-08-06 | [server.ts:51](packages/frontierjs-vscode/src/litestone/server.ts#L51) · [server.ts:744](packages/frontierjs-vscode/src/litestone/server.ts#L744) · [build-parser.js:26](packages/frontierjs-vscode/scripts/build-parser.js#L26) |
| FJS-006 | `@encrypted` on a `Json` field silently destroyed the value | 2026-08-06 | `encryptField` does `String(plaintext)`, so an object became `'[object Object]'` and what got stored was a faithful ciphertext of that literal — unrecoverable, nothing thrown. A Json field is now serialized before encryption and parsed after decryption, mirroring the pipeline either side (write encrypts then serializes; read parses then decrypts), keyed on the DECLARED type captured in `buildFieldPolicyMap`. Objects, nested structures, arrays and JSON scalars all round-trip; the column stays ciphertext and no plaintext reaches the file. `@secret`, `$rotateKey` and `searchable` verified too. 9 tests, 4 fail if reverted. The `CLAUDE.md` hazard telling people to use `String @encrypted` instead is removed |
| FJS-005 | `$setAuth(user).sql` was byte-identical to unscoped `sql` — raw SQL bypassed every access rule | 2026-08-06 | Raw SQL is now available through `asSystem()` only, on any schema declaring `@@gate`/`@@allow`/`@guarded`/`@encrypted`/`@scoped`. Measured before: `$setAuth({id:1})` gave 1 row via the ORM and 3 via `.sql`, with a `@guarded` column in plaintext — and the UNSCOPED client was wider still (ORM 0 rows vs `.sql` 3), which is why the rule covers `db.sql` too and overturns the idea file's "unchanged". Coarse per schema rather than per statement on purpose: a hand-written SQL validator that is subtly wrong grants a false guarantee, and `bun:sqlite` exposes no authorizer (verified). Three byte-identical copies of `sql` collapsed to one runner. 9 tests, 5 fail if reverted. Ruled in `DECISIONS.md` § Access control. **Scoped raw SQL (the view set in `IDEAS/scoped-sql.md`) is deliberately NOT built** — revisit with `herald` |
| FJS-004 | A model service had no way to say "read only" — an admin could forge a row into an append-only audit trail | 2026-08-06 | `methods: ['find','get']` \| `'readOnly'`, enforced in `callService` so an in-process caller is refused exactly as the wire is. CRUD and actions share the list; unknown names throw at construction; `/manifest`, `/metrics` and OpenAPI filter by the same predicate. 21 tests, 7 fail if the check is removed. Basecamp's `/audit` dropped four hand-written stubs for one line and was re-probed live: `405 {"message":"Service 'audit' does not offer 'create' (allowed: find, get)"}`. Ruled as [FJS-D07](#fjs-d07) |
| FJS-002 | `cookieAuth: true` could not authenticate a request | 2026-08-06 | Junction's `extractToken()` read only `authorization: Bearer` and `x-api-key`, so the cookie the auth plugin set was never read back and the documented mode gave you a session you could not use. `extractToken` now takes an optional cookie name and @frontierjs/auth calls `app.http.setAuthCookie('session')` from its own `register()`, so cookie mode is ONE switch — requiring the app to also write `config.auth.cookie` would have left a half-configured state reproducing the original bug. Cookies stay **off by default** as a CSRF decision (a Bearer token cannot be forged cross-site, a cookie travels automatically; `SameSite=Lax` is what makes it safe when on), and an explicit credential always beats the ambient one. Covers the WebSocket upgrade too — otherwise a cookie-authed app connected its socket anonymously, silently. Marker test flipped from 401 to 200; 12 junction tests + 5 auth tests, 7 fail if reverted |
| <a id="fjs-d15"></a>FJS-D15 | email-kit — **`@frontierjs/mesa-email` or `@frontierjs/email-kit`?** | 2026-08-06 | **Ruled 2026-08-06: `@frontierjs/email-kit`** — the directory was already right and every other package's name matches its own. Fixed [FJS-051](#). `DECISIONS.md` § Naming — [DECISIONS.md](DECISIONS.md) |
| <a id="fjs-d21"></a>FJS-D21 | junction — **Does a custom action announce?** | 2026-08-06 | **Ruled 2026-08-06: yes, under its own name** (`orders pay`); reads opt out with `ctx.dispatch = false`. Fixed [FJS-033](#). `DECISIONS.md` § API design — [DECISIONS.md](DECISIONS.md) |
| FJS-C06 | Sierra's `mesa-plugin` ignored `analysis.errors` | 2026-08-05 | The transform throws; ruled in `DECISIONS.md` |
| FJS-C05 | All four `@frontierjs/ui` stores were inert | 2026-08-05 | Writes go through the `watchProxy`, not a plain object |
| FJS-C04 | A click inside `<mesa:portal>` never reached its handler | 2026-08-05 | Portals register their target, reference-counted |
| FJS-074 | `updateMany` / `deleteMany` wrote no audit entry at all on an `@@log` model | 2026-08-05 | All five uninstrumented paths log (`updateMany`, `deleteMany`, `removeMany`, `restore`, `upsertMany`); bulk ops take `RETURNING` on a logged model so the entry names its rows, which also fixed `createMany`'s empty `records`. 8 tests, `docs/audit-logging.md` corrected |
| FJS-073 | `<mesa:boundary>` watched every async value in the component | 2026-08-05 | The watch set is the async values the body reads; the whole-component union is kept when the body reads none (that is how you gate a region on everything) or renders a snippet defined elsewhere. 9 tests |
| FJS-C10 | `example/`'s production build loaded no JavaScript | 2026-08-04 | Vite injects at the first body-tag match without skipping comments |
| FJS-C09 | `packages/basecamp` invisible to the workspace glob | 2026-08-04 | Has a `package.json`, installs |
| FJS-C07 | Gate resolver graded every session `VISITOR(1)` | 2026-08-04 | Absent ≠ null; standing checked before lifecycle |
| FJS-C03 | A handler ran once per ancestor delegation root (Mesa double-dispatch) | 2026-08-03 | Nearest delegation root owns an event — now Invariant 11 |
| FJS-C01 | `css` is not a package | 2026-08-03 | v0.10.0 — real package, tested, with a demo app (was ledger 23) |
| FJS-068 | mesa — **`renderComponent` resolved bare imports from Mesa's own package root (SSR_SPEC W1)** | 2026-08-03 | `renderComponent` / `renderFile` take `options.tmpDir`, threaded through the recursive `compileTree` path so an import graph cannot be split across two directories, and the directory is created if missing. The renderer compiles each module to a temp `.mjs` and imports it, and Node resolves a bare specifier relative to the *importing* file — so where those temp modules live decides which `node_modules` a rendered tree can reach; it was a module-level `const` resolved at import time, making the directory a property of the package rather than of the call. **The default deliberately stays Mesa's own package root**, because a temp module carries `import '@frontierjs/mesa/runtime.js'` and that has to resolve from wherever it is written; pointing them at the app's tree is the caller's statement, and `sierra/src/build/index.js` makes it (`node_modules/.sierra/render`), which is what makes a prerendered layout containing `import { page } from '@frontierjs/sierra/router'` render at all. Four cases in `render-component.test.js` including the negative one — the same import must still fail under the default, or the test proves nothing. **Closed in code 2026-08-03; the row and three doc lines said "open" until 2026-08-16**, which is the only thing that was wrong with it since |
| FJS-C08 | The result envelope · one event origin · the gate seam · internal pagination · `publish` reserved | 2026-08-02 | Ledger 3, 6, 11, 13 — all ruled in `DECISIONS.md` |
| FJS-C02 | Sierra's `static` target is not wired to the renderer | 2026-08-02 | `prerender.js` composes route + layout chain through `renderComponent` |
