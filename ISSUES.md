# Issues

**The register of everything open in this repo.** Defects, gaps, and questions
awaiting a ruling. One id per item, stable forever.

**This file is the source of truth for what is open.** Nothing is open unless it
is here. Other documents no longer carry their own open-item lists — they link
here. Long analysis may stay where it was argued (`packages/*/PROJECT_STATE.md`,
`IDEAS/*.md`, `drift-report.md`); the **Detail** column points at it. What must
never happen again is an item existing in exactly one document that nobody reads.

**What belongs here:** shipped behaviour that is wrong, a gap that blocks
something, a question that needs a decision. **What does not:** features and
roadmap (`IDEAS/`), settled rulings (`DECISIONS.md`), traps that are correct
behaviour you must know about (`CLAUDE.md` § Live hazards), history
(`packages/*/CHANGES.md`).

## Conventions

| | |
| --- | --- |
| **Id** | `FJS-###` for a defect or gap, `FJS-D##` for a decision. **Never reused, never renumbered.** Closing an item moves it to § Closed with its id. |
| **Sev** | `S1` blocker — data loss, a security hole, or cannot ship · `S2` high — silently wrong, costs a day to find · `S3` medium · `S4` low or cosmetic |
| **Status** | `open` — verified open on the date shown · `stale?` — inherited from an older audit and **not re-verified**; probe before acting · `contested` — the recorded diagnosis is doubted, see the note |
| **Verified** | The date somebody last ran it. A `stale?` row's date is when it was *written*, not confirmed. |

`stale?` is load-bearing. Of the items inherited from `drift-report.md`
(2026-07-31) at least two proved wrong later — the notifications "empty email
body" diagnosis did not match the tree, and `css is not a package` had already
been fixed. **Treat a `stale?` row as a lead, not a fact** (`VERIFYING.md`).

---

## S1 — blockers

| Id | Pkg | Title | Status | Verified | Detail |
| --- | --- | --- | --- | --- | --- |
| FJS-001 | repo | **Nothing is committed** — `git status --porcelain` is ~465 paths, `example/` untracked, `build/`'s 20 source files un-ignored but unstaged | open | 2026-08-05 | [HANDOFF.md:22](HANDOFF.md#L22) |
| FJS-003 | auth | **The package cannot be published.** 5 shipped files import `../junction/index.ts` 8× (3 runtime value imports); peer dep is a bare `"*"`; no `files` field. Proven by `npm pack` + install | open | 2026-08-02 | [auth/PROJECT_STATE.md:185](packages/auth/PROJECT_STATE.md#L185) |
| FJS-007 | basecamp | **No `@@gate` in the schema** — access control is service hooks, the outstanding gap against repo Invariant 6. Blocker is a per-workspace `getLevel` mapping `WorkspaceMember.role` onto 0–7. **Sequencing decided 2026-08-06: deferred until every screen is built** — it would be in the way while the app is still being assembled, and this is a long way from live. The cost is accepted and known: screens written before it need revisiting. **Every screen is now built (2026-08-10), so nothing is in the way any longer** — this is the next piece of work on the app. Two things Phase 10 put in place for it: `User.isSystemAdmin` is a column and reaches the session, which is exactly the field `sessionGateLevel()` grades SYSADMIN(7) on, and the hub's reads are already written against `asSystem()` because `User` is meant to be `@@gate("8")` — a level even SYSADMIN does not pass — so the screen that breaks first will not need rewriting. Still S1 — it is a hole until it lands, not a nice-to-have | open | 2026-08-10 | [basecamp/PROJECT_STATE.md:211](packages/basecamp/PROJECT_STATE.md#L211) · pattern is `example/api/gate.ts` |
| FJS-008 | vscode | ~~**`npm run build` fails** — stale parser path in `scripts/build-parser.js`. Plus a null-schema crash~~ **Both fixed 2026-08-06.** Parser had moved to `litestone/src/core/parser.js`; the resolver now probes `core/parser.js` then `parser.js` under each base. `parse()` returns **`schema: null` on any syntax error** — the normal mid-keystroke state — and the server typed it non-nullable, guarding `result?.schema.enums` (outer `?.` only, 5 sites) and not at all in hover (3 sites). Verified by driving the server over stdio with a broken schema: initialize + completion + hover all answer, the right diagnostic publishes, process stays alive. **A third bug fell out of that probe and is also fixed**: `isInsideBlock` incremented only on its own keyword but decremented on every `}`, so depth went negative after any earlier block and `inModel` was false for the rest of the file — an `enum` above the models (i.e. every real schema) silently killed type, enum and attribute completion. Now a stack of all block kinds; 9 stdio assertions cover enum-first, one-line enum, model-first, commented-out block, database driver, top level, and `example/db/schema.lite` | fixed | 2026-08-06 | [server.ts:51](packages/frontierjs-vscode/src/litestone/server.ts#L51) · [server.ts:744](packages/frontierjs-vscode/src/litestone/server.ts#L744) · [build-parser.js:26](packages/frontierjs-vscode/scripts/build-parser.js#L26) |
| FJS-008d | vscode | ~~**Completion offers attributes instead of types on any line that already has one.**~~ **Fixed 2026-08-06.** `atTrigger` was `trimmed.includes('@')` over the WHOLE line, ignoring the caret, so editing the type in `status OrderStatus @default(pending)` — the shape of most real field lines — offered `@id`/`@unique` and never `OrderStatus`. Now read off `line.slice(0, character)`: `attrToken` (`/(@@?)(\w*)$/`) means "typing an attribute name", `inArgsOf` (`/@@?(\w+)\([^)]*$/`) means "inside that attribute's arguments". The five branches that keyed off `line.includes('@@gate')` and friends switched to `inArgsOf` — `@relation` had to, since it rode on `atTrigger` and the caret is past the `(` by then — and the type list is now suppressed inside an argument list, where a gate level is not a field type. 7 new caret assertions, incl. two caret columns on ONE line giving two different answers | fixed | 2026-08-06 | [server.ts:238](packages/frontierjs-vscode/src/litestone/server.ts#L238) |
| FJS-008f | litestone | ~~**`@from` docs teach a form that cannot parse**~~ **Fixed 2026-08-06.** Every `@from` example in the docs was wrong **twice**: the first argument is the target **model** name (`@from(Order, …)`, not `@from(orders, …)` — *"unknown model"*), and `first:`/`last:` return the **whole related row**, so the field is typed as the model (`Order?`) — `lastOrder DateTime @from(orders, last: true)` failed on the type as well as the name, while its own comment said "last related object". `orderBy:` was undocumented everywhere. Rewritten in `relations.md` (+ an operation/type table and real output), `README.md` ×2, `schema.md`, litestone's `CLAUDE.md`. Verified: both schema examples parse straight out of the markdown, and the documented result rows are the actual output of running that schema | fixed | 2026-08-06 | [relations.md:133](packages/litestone/docs/relations.md#L133) · [parser.js:1107](packages/litestone/src/core/parser.js#L1107) |
| FJS-113 | litestone | ~~**`@from` against a model with no relation parses, then silently omits the field**~~ **Fixed 2026-08-06.** The field was absent from every row — not `0`, not `null`, no key — because the SQL builder skipped it on `if (!fkCol) continue  // validation catches this` and validation did not. **Root cause was two owners of one rule**: FK inference lived inline in `buildFromMap()` while `validate()` checked only that the target model and types existed. Extracted to `inferFromFk(model, targetModel)` in the parser, exported, called by both — validate rejects what the builder could not have built, so the `continue` is now unreachable. Error names the relation to declare. Preserved: the `<Model>Id` fallback, and following `references:` rather than assuming the PK (an FK onto a non-PK `@unique` column still correlates — verified end to end). 1443 litestone tests green | fixed | 2026-08-06 | [parser.js:2331](packages/litestone/src/core/parser.js#L2331) · [client.js:760](packages/litestone/src/core/client.js#L760) |
| FJS-008e | vscode | ~~**No test suite**~~ **Landed 2026-08-06.** `npm test` = **34 assertions** over real LSP/stdio: `test/lsp-client.js` (Content-Length framing, id-correlated requests, `openDoc()` resolving on the diagnostics notification — no sleeps, one server process) and `test/lsp.test.js`. Covers diagnostics, the null-schema mid-keystroke state, block detection, caret-aware attributes, hover, formatting, and both real app schemas. **Builds before running** — a stale `out/` silently tests the previous fix. Plain node, no new deps, matching jetty's convention. **Mutation-checked**: reverting `isInsideBlock`, the caret-aware attribute detection, or the null guards each turns it red, the last reproducing the original `Cannot read properties of null (reading 'models')`. Writing it found one more defect (FJS-008g) | fixed | 2026-08-06 | [test/lsp.test.js](packages/frontierjs-vscode/test/lsp.test.js) |
| FJS-008g | vscode | ~~**Hover synthesised attribute names from bare words**~~ **Fixed 2026-08-06.** Candidates were `[word, '@'+word, '@@'+word]`, and the attribute table was consulted before the schema — so hovering the declaration `function slug(text: String): String` returned the docs for the `@slug` transform, and any model, field or enum named `trim`, `lower`, `upper`, `email` or `url` would have done the same. `wordAt()` already scans back through `@`, so an attribute always arrives prefixed and the extra candidates could only ever mis-fire. Found by the new suite on its first run | fixed | 2026-08-06 | [server.ts:559](packages/frontierjs-vscode/src/litestone/server.ts#L559) |
| FJS-008b | vscode | **Mesa support is written but switched off.** `startMesaClient` is commented out at [extension.ts:3](packages/frontierjs-vscode/src/extension.ts#L3), and three things block uncommenting it: `mesa/{hover,completions,symbols}.js` are plain JS with no `allowJs` in tsconfig, so tsc emits none of them and `require('./hover')` throws at activation; the compiler resolver hunts the pre-rename `@mesa/compiler/compiler.js` (real name `@frontierjs/mesa`, entry `src/compiler.js`, `compile` exported async); and `frontierjs.mesa-language-support-0.1.0` is separately installed in `~/.vscode/extensions` claiming the same `.mesa` grammar | open | 2026-08-06 | [mesa/client.ts:131](packages/frontierjs-vscode/src/mesa/client.ts#L131) |
| FJS-008c | vscode | **`icons/` does not exist** — `package.json` references `icons/frontierjs.png` plus two litestone SVGs. `tsc` does not care, `vsce package` refuses. Blocks packaging only | open | 2026-08-06 | [package.json:28](packages/frontierjs-vscode/package.json#L28) |
| FJS-009 | repo | **No CI.** No `.github/` exists; every landmine in `CLAUDE.md` was found by hand, and will be found by hand again by everyone who builds on this | open | 2026-08-05 | [IDEAS/testing-and-ci.md](IDEAS/testing-and-ci.md) · wave 0.1 |

---

## S2 — high

| Id | Pkg | Title | Status | Verified | Detail |
| --- | --- | --- | --- | --- | --- |
| FJS-139 | junction · sierra | **A service call issued from a channel subscriber can hang until some other call is made.** Basecamp's `/volumes/` reloads on push (`service.on('*') → load()`), and after a burst of agent reports the reload does not settle: the screen sits on "Loading…" with no error and no rejection for the 15s the drive polls, showing the fleet as it was one report ago. It is not the client generally — at that exact moment, `action('usage', null)` and `find()` typed into the page's console both answer immediately, **and making those two calls is what releases the stuck one**, after which the screen renders. So a pending request appears to need a subsequent send before its response is dispatched. **Ruled out**: concurrency (two parallel calls from the console settle fine, action+find and find+find alike); the server (the same calls over HTTP answer in milliseconds, and the row the screen is waiting on is already written); and an overlapping-load race in the screen (a sequence token now guards the write, and the hang happens with exactly one load in flight). Not worked around in the app — every attempt (a `setTimeout` hop out of the subscriber, a debounce) moved the symptom without removing it, and a screen that is only nearly live is better read as a bug. Reproduce: `packages/basecamp` `bun run verify`, third `REPORT` in § 13c-ter, with the `await goto('/volumes/')` above the last check removed | open | 2026-08-10 | [basecamp/web/src/routes/volumes/index.mesa](packages/basecamp/web/src/routes/volumes/index.mesa) · [basecamp/web/test/verify.mjs](packages/basecamp/web/test/verify.mjs) |
| FJS-140 | junction | **A custom action that answers `data` plus anything else loses the anything else, silently.** `wrapResult` recognises `{ data, total }` (or `{ data, errors }`) as a paginated list and rebuilds the envelope from exactly `total`/`limit`/`offset`/`data`/`errors` — every sibling key is dropped with no warning, no error and a 200. Basecamp's `dashboards.kinds` returned `{ total, data: WIDGET_KINDS, statSources, portalServices }`; the browser received the nine kinds and neither of the two vocabularies needed to configure them, so the picker offered widgets it could not fill in. Worked around in the app by answering three named keys instead — a shape wrapped as `single`, which unwraps whole — and `volumes.usage` already documents the same trap from the other side, which is what makes this the second time it has cost a session. The envelope is a deliberate contract (`FJS-C08`), so the fix is not "keep extra keys": it is that dropping them should say so, or that a list envelope should carry a `meta` slot for the summary a list read genuinely has. Reproduce: return `{ total: 1, data: [], extra: 1 }` from any service method and read the response | open | 2026-08-10 | [junction/src/core/envelope.ts:115](packages/junction/src/core/envelope.ts#L115) · [basecamp/api/src/services/dashboards/dashboards.service.ts](packages/basecamp/api/src/services/dashboards/dashboards.service.ts) |
| FJS-144 | junction | **A `find` that answers one object is silently turned into an EMPTY list by the browser client.** `ServiceProxy.find()` normalises three shapes into a `ListResult` — a real envelope, a bare array, and `{ total, data: [...] }` — and every other shape falls through to `return list(this.name, [])`. So a service whose `find` legitimately answers ONE thing (a runtime rollup, a settings object, a health snapshot) reaches the browser as `{ kind: 'list', data: [], total: 0 }`: 200, no warning, and the caller's own `info.runtime` then throws inside a template where a render just stops. Found building basecamp's `GET /hub` — the whole screen rendered nothing, and the API answered correctly throughout, so only the browser could see it. The normalisation itself is right (`FJS-C08` makes the envelope a contract); what is wrong is that the fall-through cannot tell "an empty list" from "not a list at all", and it is the same class as [FJS-140](#) from the other end. Fix options: warn on the fall-through, or make it throw in dev. Worked around in the app by making the read an ACTION — `find` means a list. Reproduce: `async find() { return { a: 1 } }` on any service, then `client.service('x').find()` | open | 2026-08-10 | [junction/src/client/index.ts:185](packages/junction/src/client/index.ts#L185) · [basecamp/api/src/services/hub/hub.service.ts](packages/basecamp/api/src/services/hub/hub.service.ts) |
| FJS-143 | junction | **`IEventBus` can say whether anything is listening, never how much.** `hasListeners(event?)` is the only introspection there is: no subscriber count, no list of subscribed event names, and the `handlers` Map is closure-private. So an operations screen can show *the bus is idle* and not *four things are subscribed to three events*, which is the question anyone debugging a missing announcement actually has — and `publish`/`onAny` fan-out is exactly where a silently-absent subscriber costs a session (`FJS-010` is that failure in Litestone's direction). Basecamp's hub overview states the gap on the card rather than printing a number it cannot measure. Fix is small and additive: a `stats()` returning `{ events: Record<string, number>, total }`, alongside `hasListeners` rather than replacing it. Reproduce: `createEventBus()`, subscribe twice, and try to ask how many | open | 2026-08-10 | [junction/src/events/index.ts:11](packages/junction/src/events/index.ts#L11) · [basecamp/web/src/routes/hub/index.mesa](packages/basecamp/web/src/routes/hub/index.mesa) |
| FJS-142 | basecamp | **No impersonation, and it is offered twice in the mock.** Both `WorkspacesView` and `UsersView` carry an *Impersonate* button; neither is built, and both screens say so instead. The button is not the work — minting a session as another principal needs three things this app has none of: an audit trail that keeps recording who is REALLY acting (today `basecampAuditLog` writes `ctx.auth.user.userId` and would file every impersonated action under the impersonated person), a way back that cannot be forgotten or lost with the tab, and a rule for what an impersonator may not do — change a password, issue an API key, grant the hub tier. Without all three it is credential sharing with a nicer name. Wants a `SessionContext` that can carry both principals, which is a framework question, not only an app one | open | 2026-08-10 | [basecamp/api/src/services/hub/hub.service.ts](packages/basecamp/api/src/services/hub/hub.service.ts) · [basecamp/docs/SCREENS.md](packages/basecamp/docs/SCREENS.md) |
| FJS-010 | junction | **Litestone `onEvent` has zero Junction subscribers.** An `asSystem()` write in a Caravan job announces nothing. Junction cannot fix it alone — see [FJS-D04](#fjs-d04) | open | 2026-08-02 | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) |
| FJS-011 | sierra | **Live-store filter leak** — `resource()` upserts every event regardless of the query, so a patched row that leaves the filter stays in the store and goes silently wrong. **Also blocks optimistic updates** ([FJS-082](#)): a store that never removes a row on a filter miss cannot roll one back either, so this has to be settled before an optimistic write is safe to offer | stale? | 2026-08-02 | [IDEAS/live-queries.md](IDEAS/live-queries.md) · wave 0.2 · [IDEAS/client-data-lifecycle.md](IDEAS/client-data-lifecycle.md) |
| FJS-012 | junction | **No `apiPrefix` on plugin routes.** `registerServiceRoutes` applies it; a plugin calling `app.post()` does not. `@frontierjs/auth` defaults to `/auth`, Junction's browser client hardcodes `/api/auth/login` — with defaults on both sides they never meet | open | 2026-08-04 | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) |
| FJS-013 | junction | **`createLitestoneService` conflates the accessor with the model name** — `scopedDb[model]` wants `lead`, `generateJsonSchema` keys `$defs` by `Lead`. The `$defs` miss warns rather than throws, so a service can silently accept unvalidated input | open | 2026-08-04 | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) |
| FJS-015 | litestone | **`createClient({ db })` is silently ignored when the schema declares `database main`** — `db: ':memory:'` writes the declared file and says nothing, so a test that believes it is in-memory accumulates state across runs | open | 2026-08-04 | [CLAUDE.md](CLAUDE.md) § Live hazards |
| FJS-016 | junction | **Service conflates definition with runtime.** The index signature `[method: string]: unknown` poisons every `def.*` read — 14 of `service.ts`'s 17 typecheck errors. Three-way pipeline ladder, `register()` monkey-patching `service.hooks`, `_compiledPipelines` invalidation | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 1 · go/no-go is [FJS-D01](#fjs-d01) |
| FJS-017 | junction | **Two pipeline systems, one vocabulary** — middleware vs hooks. `rateLimit`/`rateLimitHook`; auth re-implements a limiter because `ServiceContext.params.ip` ≠ `TransportContext.ip`; `apiPrefix` hand-resolved in 4 files with 2 different defaults | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 5 |
| FJS-018 | junction | **Types stop at the server.** Litestone's typegen emits `Post`/`PostCreate`/`PostWhere`; nothing carries them to the browser. The hand-written `Seeder` type in `example/fullstack/app.ts` is the receipt | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 7 · relates to [IDEAS/framework-shape.md](IDEAS/framework-shape.md) 5 |
| FJS-019 | junction | **Dialect trap** — junction pins litestone `"latest"` → npm **1.0.3** for its own internals while the workspace ships **1.1.0**. They agree today by luck, not design | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 9 |
| FJS-020 | junction | **A custom method's return shape is load-bearing and nothing says so.** Four Basecamp methods answered a partial row and each broke a client doing the obvious thing. The payload is also what the channel publishes, so a projection with no `id` cannot be matched to its row | open | 2026-08-04 | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) · ruling is [FJS-D08](#fjs-d08) |
| FJS-021 | mesa | **A destructuring assignment to reactive lets is not rewritten.** `[a, b] = [b, a]` emits `[$runtime.get(…), …] = …`, which does not parse. `rewriteAssignments` only recognises a bare identifier on the left | open | 2026-08-04 | [ui/PROJECT_STATE.md:96](packages/ui/PROJECT_STATE.md#L96) — `DatePicker` threw on load; pinned in `emission.test.js` |
| FJS-022 | mesa | **`{@const}` inside `{#each}` treats the index as a getter** — `{@const isLast = idx === list.length - 1}` compiles to `idx()` and throws `idx is not a function` | open | 2026-08-04 | [ui/PROJECT_STATE.md:96](packages/ui/PROJECT_STATE.md#L96) |
| FJS-023 | mesa | **`<mesa:element this={…}>` is not a feature and compiles without complaint** | open | 2026-08-04 | [ui/PROJECT_STATE.md:96](packages/ui/PROJECT_STATE.md#L96) |
| FJS-024 | mesa | **`mesa-vite` has no tests at all**, and its HMR id-normalisation fix has never been confirmed in a browser. The dev loop is the highest-value untested surface in the package | open | 2026-08-02 | [mesa/PROJECT_STATE.md:282](packages/mesa/PROJECT_STATE.md#L282) |
| FJS-025 | mesa | **Nothing in mesa is verified in a real browser** — every suite is happy-dom. The REPL (codemirror, importmap, drawer) was repaired against happy-dom only | open | 2026-08-02 | [mesa/PROJECT_STATE.md:282](packages/mesa/PROJECT_STATE.md#L282) |
| FJS-026 | repo | **Nested directories are invisible to the `packages/*` glob** — `mesa-vite/` and `mesa-bench/` inside `packages/mesa`. Invisible means uninstalled, untested, unrunnable | open | 2026-08-04 | [CLAUDE.md](CLAUDE.md) § Live hazards |
| FJS-027 | css | **`.btn.outlined` fails WCAG AA in all six themes** — measured 1.99–4.40:1 against a 4.5 threshold. `.ghost` already applies the fix (`--ink-soft`); `.outlined` inherited the failure | open | 2026-08-03 | [css/PROJECT_STATE.md:1226](packages/css/PROJECT_STATE.md#L1226) |
| FJS-028 | ui | **35 of 63 components are compile-only** — never opened in a browser. `DatePicker` (1200 lines) is the biggest unknown, then `Drawer`/`Popover`/`ConfirmationPopover`/`FileUpload` | open | 2026-08-05 | [ui/PROJECT_STATE.md:132](packages/ui/PROJECT_STATE.md#L132) |
| FJS-029 | ui | **`{...$attributes}` is applied to 8 of 63 components** — the other 55 cannot take an `id` or an `aria-label`. One line each | open | 2026-08-05 | [HANDOFF.md:119](HANDOFF.md#L119) |
| FJS-030 | jetty | **Built `islands/demo.js` contains `import.meta`** and MV3 content scripts are classic scripts — the package's one failing test | open | 2026-08-05 | [CLAUDE.md](CLAUDE.md) § Packages |
| FJS-031 | basecamp | **Every service publishes after every method, reads included** — a `GET` broadcasts the row to everyone connected to the workspace. Noise rather than a leak, but the wrong default | open | 2026-08-04 | [basecamp/PROJECT_STATE.md:211](packages/basecamp/PROJECT_STATE.md#L211) |
| FJS-101 | basecamp | **The UI covers 31 of the mock's 41 screens.** Inventoried 2026-08-06 into [basecamp/docs/SCREENS.md](packages/basecamp/docs/SCREENS.md), which stays the map — it groups every unbuilt screen by what actually blocks it. Was 12 at the first count; ten phases since have built shell chrome, Portal + Activity, services over `AlertRule`/`Network`/`Secret`, `AppDetailView` (with `Domain` replacing `App.domain`), Channels + Flags, `ApiKey`, `Volume`, `Dashboard` + `DashboardWidget`, `Recipe` + `DiskUsage`, and the four sysadmin screens behind `/hub/`. **The dead-weight half is gone** — no model in `db/schema.lite` is now without an API, and **no screen is blocked on an API any longer**: every one of the 10 left needs either a new model or a real outbound adapter. 4 need models (`Blueprint`, `Backup`, hub settings, onboarding, user settings, `RegistryImage`), 6 need an adapter | open | 2026-08-10 | [basecamp/docs/SCREENS.md](packages/basecamp/docs/SCREENS.md) |
| FJS-119 | ui | **The kit's toasts are fire-and-forget — there is no updatable handle.** `toasts` exposes `add/remove/clear/success/error/warning/info` and each returns an id, but nothing takes that id and changes the toast; the only use of the return value is `remove(id)` on a timer. Every long action in basecamp — testing a channel, deploying, running a job — wants the mock's `toast.loading('Sending…').update('success', …)` shape, and the mock has it only because it is React state in a 12,557-line file. **It has already cost once**: `channels/index.mesa` called `toasts.loading(...)` from a click handler, outside the try, so a FAILED delivery reported nothing at all — caught by `verify` asserting the error text, not by any render test, because SSR dispatches no events. Worked around with `busy` + a terminal toast. Wants `loading(message)` returning `{ update(type, message), dismiss() }`, and `example`'s `verify:ui` driving it | open | 2026-08-08 | [basecamp/web/src/routes/channels/index.mesa](packages/basecamp/web/src/routes/channels/index.mesa) · [ui/stores/toastStore.js](packages/ui/stores/toastStore.js) |
| FJS-123 | basecamp | **Nothing evaluates an alert rule, and nothing delivers to a channel.** Both halves now exist and are proven — `AlertRule` declares a metric and a condition, `NotificationChannel` really sends (`channels.test` goes out through conduit and fails loudly if nothing answers), and `AlertRuleChannel` joins them. What is missing is the thing between: no evaluator reads a metric, compares it to a rule and writes an `AlertEvent`, and nothing fans a fired event out to the rule's channels. Deliberately not invented — a rule that fires from the browser's idea of the truth is theatre, and the metric source is an observability adapter this app does not have. The screens say so rather than rendering an empty box. Closing it needs a metric source first | open | 2026-08-08 | [basecamp/api/src/services/alerts/alerts.service.ts](packages/basecamp/api/src/services/alerts/alerts.service.ts) |
| FJS-124 | basecamp | **A flag's rollout percentage is stored and never applied.** `FeatureFlag.rollout` and `FlagOverride.rollout` are `Int @gte(0) @lte(100)` and `flags.resolve` returns them, but nothing buckets a user into or out of a percentage — deliberately: that decision has to happen where the user is, per request, and computing it server-side would produce a number an SDK could not reproduce. So today a 10% rollout behaves as on-or-off. Closing it means agreeing the hashing rule (which key, which algorithm) so the server and every SDK land on the same answer, and publishing it beside `resolveIn()` | open | 2026-08-08 | [basecamp/api/src/services/flags/flags.service.ts](packages/basecamp/api/src/services/flags/flags.service.ts) |
| FJS-103 | basecamp | **The audit trail records that something happened, never what changed.** `AuditEvent.diff` is `Json?` and nothing writes it, so `servers.drain` is recorded with no before/after — the trail can say a server was drained and not what state it was in. Litestone's row-level `@@log(audit)` JSONL *does* carry before/after snapshots with protected fields redacted, so the data exists on the host; the application trail, which is the one the UI reads, does not. Surfaced building `/activity/` 2026-08-06 | open | 2026-08-06 | [basecamp/docs/SCREENS.md § Phase 2](packages/basecamp/docs/SCREENS.md) |
| FJS-032 | basecamp | **No invitation flow.** `/auth/register` creates a user with no account and no workspace, so every scoped request 400s and they cannot create a workspace either. The setup wizard is the only way in for a HUMAN. Narrowed 2026-08-10: the hub's Users screen now creates **bot** accounts, which is the half that needed no invitation — a bot has no password and no inbox, so nothing is being handed to anyone, and it closes the separate gap that an API key could only ever belong to whoever pressed the button. What is still missing is invite → accept: a token model, mail through conduit, and a signed-out acceptance route the navigation guard lets through. The Users screen ships without the mock's Invite button rather than with one that cannot work | open | 2026-08-10 | [basecamp/PROJECT_STATE.md:211](packages/basecamp/PROJECT_STATE.md#L211) |
| FJS-034 | junction | **224 typecheck errors** (~76 in `src/`); baseline ratchets down only (Invariant 14) | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 16 |
| FJS-035 | repo | **No tsconfig in 11 of the packages** | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 17 |
| FJS-036 | cli | **Scaffolds unverified end to end** — templates updated twice, never run through `fli make:model` | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 15 |
| FJS-037 | cli | **A sixth reserved-key list** — `commands/project/_module.md:65`, a source-parsing heuristic that cannot import the shared set | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 14 |
| FJS-038 | cli/auth | **The auth schema is hand-copied** into `cli/commands/auth/install.md` — change one, change both, and nothing enforces it. Killed for free by [FJS-D16](#fjs-d16) / bare-specifier `.lite` imports | open | 2026-08-04 | [CLAUDE.md](CLAUDE.md) § Bridge index · [IDEAS/slices.md](IDEAS/slices.md) |
| FJS-039 | caravan | **`autoloadJobs` scoping bug** · **admin guard ctx shape always 401** · **`cancel()` revert race** | stale? | 2026-07-31 | [drift-report.md:246](drift-report.md#L246) — the raw-route `ctx` shape was fixed repo-wide since; re-probe all three |
| FJS-040 | conduit | **Secrets unredacted** in logged/persisted target config | stale? | 2026-07-31 | [drift-report.md:263](drift-report.md#L263) |
| FJS-041 | sierra | **Prefetch auth bypass** — a prefetched route fetches without the session check | stale? | 2026-07-31 | [drift-report.md:186](drift-report.md#L186) |
| FJS-042 | auth | **API keys are never accepted** · **auth emits no events** | stale? | 2026-07-31 | [drift-report.md:225](drift-report.md#L225) |
| FJS-089 | junction | **A side effect in an `after` hook cannot be tied to the call succeeding.** Hooks run in sequence, so an `after` hook that sends an email runs, and then a later `after` hook throwing makes the whole call report failure — the client sees a rejected create whose email has already gone. There is no commit-scoped phase to put the effect in: the announcement point ([service.ts:411](packages/junction/src/core/service.ts#L411)) fires on `!ctx.error && !pipelineError`, meaning *the call succeeded*, and nothing in Junction is aware of a transaction at all — `$transaction` exists on the litestone client ([core/litestone.ts:74](packages/junction/src/core/litestone.ts#L74)) and no hook phase relates to its outcome. Rails states the choice as `after_save` vs `after_commit`; FJS offers only the first and does not say so, which is the part that makes it a defect rather than a gap | open | 2026-08-06 | Glossary item 36 (*synchronous side-effect escape hatch*) · relates to [FJS-010](#) |
| FJS-088 | junction | **The inbound idempotency key is plumbed and inert, and FJS relies on the opposite outbound.** `ctx.idempotencyKey` is read off the `idempotency-key` header ([app.ts:1154](packages/junction/src/core/app.ts#L1154)) and declared on the request-metadata type ([context.ts:208](packages/junction/src/core/context.ts#L208)) — and **nothing anywhere consumes it**; the only other reference in the repo copies it in the test kit. So a double-submitted `create` executes twice while the request carried the key that says not to. Meanwhile Conduit *sends* `Idempotency-Key` outbound and uses its presence to decide a non-idempotent POST is safe to retry ([conduit http.ts:79](packages/conduit/src/transports/http.ts#L79), [:159](packages/conduit/src/transports/http.ts#L159)) — FJS asks other people's APIs for a guarantee its own API accepts the header for and does not provide. Same class as [FJS-043](#) and [FJS-057](#): a declared capability that reads as handled and does nothing | open | 2026-08-06 | [IDEAS/declared-semantics.md](IDEAS/declared-semantics.md) |
| FJS-087 | mesa | **A component cannot expose a method, and both documented ways fail silently-ish.** `export function submit() {…}` is **dropped from the output entirely** — `compiler.js:5998` skips every `ExportNamedDeclaration` in the instance script and only `export let` is handled before it — so VISION §10.2 and RULE 36 (`counterRef.reset()`) describe an API that does not exist, and a component referencing its own exported function from its template throws `ReferenceError` on first interaction. The obvious workaround, `export let submit = async (e) => {…}`, emits `$runtime.get($$sig_x) = true` for every assignment in the body: reads inside a prop initialiser are rewritten, assignments are not, so the module is unparseable. Found 2026-08-06 building `<Form>`; **no render test can catch the first half**, because SSR never dispatches an event. `@frontierjs/ui` documents `bind:this` methods on `Input`, `Select` and others that are all absent from the compiled output | open | 2026-08-06 | [ui/components/forms/Form.mesa](packages/ui/components/forms/Form.mesa) § The imperative API — works around it with an `onready` callback |
| FJS-082 | junction | **`resource().load()` has no staleness discipline.** [client/index.ts:519](packages/junction/src/client/index.ts#L519) is `const rows = (await svc.find(query, params)).data; store.set(rows)` — unconditional. Two overlapping loads resolve in arrival order, so a search-as-you-type box whose slower earlier request lands second overwrites the newer results with older ones and stays wrong until the next keystroke. The only `AbortController` in the file is the request timeout. The fix is to key a load by its query identity and drop a result that is no longer the latest — which is the same "staleness is dependency identity, not arrival time" rule the Mesa compiler already applies to `<mesa:boundary>` watch sets ([FJS-073](#)). `service.find()` is already the correctly-narrow escape hatch: it returns rows and never touches the store | open | 2026-08-06 | [IDEAS/client-data-lifecycle.md](IDEAS/client-data-lifecycle.md) |

---

## S3 — medium

| Id | Pkg | Title | Status | Verified | Detail |
| --- | --- | --- | --- | --- | --- |
| FJS-141 | litestone | **A vocabulary of MANY values cannot be a column, so a declared set has to leave the schema.** `targets ReclaimTarget[]` is refused at parse time — *array [] is only supported for Text, Integer, File, or a model name for many-to-many* — and the parser is right that SQLite has no array type; it stores `String[]` as a JSON TEXT column with a `json_valid` CHECK. But the consequence is that a set-valued enum has **no home in the seed**: an app either writes `String[]` and validates the members somewhere else (basecamp's `cleanup` service, `targets.ts`), or declares the enum anyway and gets two homes with nothing joining them — the shape that let `AlertRule.severity` default to a value its own API refused. A single-valued enum gets a CHECK, a `$def` on the JSON Schema, and one declaration feeding column, API and picker; the set-valued case gets none of that. Fix options, none free: a `json_each`-based CHECK on the column, or an enum-array keyword that emits validation at the boundary only and says so. Found ruling basecamp's reclaim targets (`DECISIONS.md` 2026-08-10). Reproduce: `parse('enum T { a b }\nmodel M { id String @id\n t T[] }')` | open | 2026-08-10 | [litestone/src/core/parser.js](packages/litestone/src/core/parser.js) · [basecamp/api/src/services/cleanup/targets.ts](packages/basecamp/api/src/services/cleanup/targets.ts) |
| FJS-137 | ui | **Every one of the kit's 22 display components drops `id`.** `{class}` is a Mesa passthrough and goes through; `id` has to be declared as a prop, and none of `Badge` `Bar` `Breadcrumbs` `Callout` `CopyButton` `Divider` `Dot` `EmptyState` `Kbd` `Mono` `Pagination` `Pill` `SectionHeader` `Sparkline` `Stat` `StatCard` `Steps` `Table` `Tag` `Avatar` `AvatarGroup` `AccountStatus` does. So `<Mono id="x">` renders `<code>` with no id, silently — the caller cannot address the element they just wrote, `aria-describedby` and `aria-labelledby` cannot point at one, and a browser drive's selector finds nothing rather than erroring. The forms components mostly do declare it (`export var id`), which is why this went unnoticed: the half of the kit people write tests against is the half that works. Found needing to assert on a one-time API token in basecamp, worked around there with a wrapper `<span>`. The fix is mechanical and the cost is the render tests that should come with it | open | 2026-08-09 | [ui/components/display/Mono.mesa](packages/ui/components/display/Mono.mesa) · [basecamp/web/src/routes/api-keys/index.mesa](packages/basecamp/web/src/routes/api-keys/index.mesa) |
| FJS-128 | ui | **`DatePicker` is a second design system inside the first.** 468 lines of local `<style>` — 69% of all the CSS in the kit sits in this file and `CommandPalette` — declaring **107 custom properties, 47 of them defined without reading a single design token**: its own six-rung radius scale, its own font-size scale, `font-family: sans-serif`, and 8 hardcoded colours. `@frontierjs/css` already ships radii, `--font-mono`/`--font-primary`, `--shadow-*`, the `--text-*` ladder, the `--space-*` ladder and the density axis, so a theme switch reaches this component partially at best and `.dense` not at all. It also styles `.disabled`, `.start` and `.end` — three real modifier names from the package. **It is one of the 35 kit components never opened in a browser drive**, so the rewrite has no safety net until `verify:ui` covers it; that ordering is the actual blocker | open | 2026-08-08 | [ui/components/forms/DatePicker.mesa](packages/ui/components/forms/DatePicker.mesa) |
| FJS-129 | ui | **`CommandPalette` declares its own `--cp-*` token namespace.** 276 lines, 21 custom properties, 5 defined without reading a design token — `--cp-radius: 12px`, `--cp-font: 'SF Mono', …`, `--cp-shadow: 0 24px 80px rgba(0,0,0,0.72)` — plus 5 hardcoded colours. Smaller and more tractable than `FJS-128`: the palette IS driven in a browser (`example`: `verify:ui`, 4 palette assertions), so the rewrite against `--radius-*`, `--font-mono`, `--shadow-*` and the space ladder has a safety net. The `.cp-*` prefix at least claims no vocabulary — this is token divergence, not name collision. **Partly reduced 2026-08-10**: the row's text block is now `.item-text`/`.item-title`/`.item-sub` from `lists.css` (`.cp-row-text` and `.cp-row-sub` deleted), which is the slice that was duplicating shipped CSS rather than merely diverging from tokens — it was the second of two copies of that shape, the guide's ⌘K being the first, and shipping the parts is what closed both. The `--cp-*` namespace itself, the literals in every padding and font-size, and the `-webkit-box`-free row geometry are untouched | open | 2026-08-08 | [ui/components/overlay/CommandPalette.mesa](packages/ui/components/overlay/CommandPalette.mesa) |
| FJS-132 | css | **A Popover needs two rules before the term is usable, and the package ships neither.** `.popover` is `position: absolute` with no inset. Without a positioned ancestor it anchors to the page; without an offset it resolves to its static position, centred ON its own trigger — measured in the demo at 265px tall, `y = -39`, hanging off the top of the viewport. `.tooltip-anchor` ships in the package and solves exactly this for Tooltip (`position: relative; display: inline-flex`), and `.tooltip` carries its own placement; Popover has no counterpart for either half, so every consumer of a term the vocabulary NAMES writes these two rules before it can be used at all. `demo/demo.css` — the file whose whole purpose is measuring what the package makes a consumer write — went from 1 rule to 3 because of this. Fix is a `.popover-anchor` mirroring `.tooltip-anchor`; the open question is whether a default offset belongs on the term, since a popover has four plausible edges and a tooltip effectively one. `packages/css/CLAUDE.md` records the top-layer half of this trap (`[popover]` escapes a relative parent) — this is the plain-element half | open | 2026-08-09 | [demo/demo.css](packages/css/demo/demo.css) · [src/components/popovers.css](packages/css/src/components/popovers.css) |
| FJS-125 | css | **`theme-notebook`'s `--ink-mute` is 2.67:1 and fails AA wherever it is text.** Measured against `--surface-sunken`; 3.06:1 against `--surface`. Nine files use it as muted text — table meta, field hints, step labels, facts, disclosure summaries, feedback, tiles, bars, dialogs — so every one of them is under AA in that theme, and it is not a typo to nudge: the theme's whole ink ramp is compressed (`--ink` `#202522`, `--ink-soft` `#59615b`, `--ink-mute` `#858c83`), and the value that clears 4.5:1 on the sunken surface is about `#5f665c`, which is `--ink-soft` again. Closing it means either widening the ramp or accepting that notebook has two ink tiers rather than three. Found deriving the syntax-highlighting palette, which reads `--ink-mute` for comments and is excluded from `code.spec.js`'s AA pass for exactly this reason — code.css invents no grey of its own, which the suite does pin | open | 2026-08-08 | [css/src/themes/notebook.css](packages/css/src/themes/notebook.css) |
| FJS-096 | notifications | **`notify()` addresses a `User`, and a customer is not one.** The unit of address is `{ id, email }` structurally, so a non-user recipient (a shop customer, a mailing-list address) is passed as a `User` with a made-up id — `example/api/notifications/OrderConfirmation.ts` namespaces it `customer:<id>` so it cannot collide with an auth uuid. That works for `email` and is a TRAP for `inApp`, which writes a row keyed by that id: a notification nobody can ever read, with no error. Either the email channel should take an address rather than a user, or `via()` returning `inApp` for an unrecognised id should say so | open | 2026-08-06 | [example/api/notifications/OrderConfirmation.ts](example/api/notifications/OrderConfirmation.ts) |
| FJS-093 | caravan | **A job handler cannot reach the app.** `defineJob('x', async (job) => …)` is called with one argument — there is no `job.app`, no second parameter, and `autoload.ts` imports the file without passing anything — so an autoloaded `*.job.ts` cannot call `app.service(…)`. In this framework that is what a job mostly wants: the service layer is the one place a write is announced ([FJS-D21](#fjs-d21)), and a job that reaches for `db.asSystem()` instead writes silently ([FJS-010](#)). Caravan is handed `app` in its own `register(app)` and keeps it. `example/api/app-ref.ts` is the workaround — a module-level back-reference `app.ts` sets and job files read — and it says so in its own header | open | 2026-08-06 | [caravan/CHANGES.md](packages/caravan/CHANGES.md) 2026-08-06 · [example/api/app-ref.ts](example/api/app-ref.ts) |
| FJS-094 | caravan | **A job file cannot declare its schedule.** `defineJob`'s options are `{ queue, maxAttempts, retryDelay }` — no `cron` — and `queue.schedule(name, expr, handler)` is the only way to register a recurring job. So a job in `jobsDir` can declare everything about itself except when it runs, and a recurring job ends up half in its own file and half in the file that calls `schedule()`. `example/api/jobs/sweep-abandoned.ts` is deliberately NOT `*.job.ts` for this reason, with the reason in its header. Adjacent to [FJS-090](#) — both are the job's declaration being split across places | open | 2026-08-06 | [caravan/src/index.ts](packages/caravan/src/index.ts) `schedule()` · [caravan/CHANGES.md](packages/caravan/CHANGES.md) |
| FJS-095 | repo | **Nothing can say "this column is written by the system, not by a user".** `example`'s `Order.trackingCode` is written by a Caravan job; the generated form offered a human a text box for it the moment the column existed, and whatever they typed would be overwritten seconds later by a worker. Both halves of the answer already exist and do not meet: Litestone emits `readOnly: true` in the JSON Schema for computed / generated / `@from` fields ([jsonschema.js:249](packages/litestone/src/jsonschema.js#L249)), and **Sierra reads `readOnly` nowhere** — no hit in `src/junction/`. What is missing in between is the annotation for an ordinary stored column that only the system writes. The example carries the repo's only hard-coded field name as a result, marked as such. **It bites harder than a stray text box, found 2026-08-09**: since `createResource` validates by default, a model whose REQUIRED columns are system-written cannot be created from the browser at all. Basecamp's `ApiKey` is `required: ["workspaceId", "userId", "name", "tokenHint"]` and three of those four are server-side — the workspace is a header, the owner is the caller, and `tokenHint` derives from a token that does not exist until auth mints it — so every create was refused before the request was made, naming fields the caller was never meant to send. The symptom is the documented one: the button does nothing. The only escape today is `{ validate: false }`, which turns the whole resource off to describe three columns. Ruling is [FJS-D22](#fjs-d22) | open | 2026-08-09 | [example/web/src/routes/orders/create.mesa](example/web/src/routes/orders/create.mesa) § SYSTEM_WRITTEN · [basecamp/web/src/resources/ApiKey.mesa](packages/basecamp/web/src/resources/ApiKey.mesa) |
| FJS-043 | junction | **`/metrics` reports `actions: []` for every service** — `health.ts:248` reads `svc.actions`, a key that does not exist. Manifest and OpenAPI were fixed; this needs `customMethodNames()`. Ten-minute fix | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 10 |
| FJS-044 | junction | **Bulk partial success is creates-only** — `PATCH` and `DELETE` still all-or-nothing against the `errors[]` ruling | open | 2026-08-02 | [DECISIONS.md:421](DECISIONS.md#L421) |
| FJS-045 | junction | **Double broadcast risk** — in-flight code using the old `publish()` hook *and* a service declaring `channel:` publishes twice. The hook still works; grep before merging | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) |
| FJS-046 | junction | **~80 root exports, no public/internal tier** — every battery both root- and subpath-exported, `plugins/ai` a self-described shim | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 4 |
| FJS-047 | junction | **Sibling ownership overlaps** — scheduler/Caravan, mail/notifications, outbound/Conduit. Smaller than it sounds: `plugins/scheduler` is 6 lines | open | 2026-08-02 | [HANDOFF.md § crosswalk](HANDOFF.md#the-ledger--moved-to-issuesmd-2026-08-05) — was ledger 8 |
| FJS-048 | caravan | **The `junction.config.js` caravan section is partial** — `register()` honours only `jobsDir` and `cleanupAfter`. `db`, `admin`, `pollInterval`, `queues` cannot work as written: `createCaravan()` opens the database at construction, before any config exists. Fixing it means deferring construction to `boot()` | open | 2026-08-04 | [caravan/PROJECT_STATE.md:166](packages/caravan/PROJECT_STATE.md#L166) |
| FJS-049 | notifications | **`plugin.ts` implements only `register()`** — no `boot()`, so "mailerPlugin must be configured first" is discovered at first send rather than startup; no `shutdown()` releases driver resources | open | 2026-08-03 | [notifications/PROJECT_STATE.md:209](packages/notifications/PROJECT_STATE.md#L209) |
| FJS-050 | notifications | **Unconfirmed: `ctx.app.notify` from a service hook**, as `plugin.ts`'s doc comment claims. `ctx.app` was never exercised. And no consumer of the in-app record shape exists in the repo | open | 2026-08-03 | [notifications/PROJECT_STATE.md:209](packages/notifications/PROJECT_STATE.md#L209) |
| FJS-052 | email-kit | **`index.js` builds absolute paths from `import.meta.url.pathname`** — wrong on Windows (leading `/` before the drive letter). Use `fileURLToPath`, as `render.js` already does. Also: a documented `autoImport: true` option that nothing implements | open | 2026-08-03 | [email-kit/PROJECT_STATE.md:83](packages/email-kit/PROJECT_STATE.md#L83) |
| FJS-053 | email-kit | **Never opened in a real mail client.** Outlook conditional-comment handling is fragile | open | 2026-08-05 | [CLAUDE.md](CLAUDE.md) § Packages |
| FJS-054 | ui | **A component takes its handler as a prop (`onclick`), never `on:click`** — a consumer writing the directive on a component gets no error and no handler. Needs a README line at minimum | open | 2026-08-05 | [ui/PROJECT_STATE.md:132](packages/ui/PROJECT_STATE.md#L132) |
| FJS-055 | ui | **A kit control puts a real `required` on the input**, so a form owning its own messages needs `novalidate` — otherwise the browser silently refuses to fire submit. **Narrowed 2026-08-06:** `<Form>` sets `novalidate` by default, so this now only bites a hand-written `<form>` | open | 2026-08-05 | [ui/PROJECT_STATE.md:132](packages/ui/PROJECT_STATE.md#L132) |
| FJS-056 | ui | **`Btn`/`Button` overlap** · **`CommandPalette` draws its own surface** rather than composing `.dialog`+`.field`+`.items` · **`themeStore` writes `localStorage['mesa-ui-theme']`** and has never been reconciled with the css package's six `theme-*` classes | open | 2026-08-05 | [ui/PROJECT_STATE.md:132](packages/ui/PROJECT_STATE.md#L132) |
| FJS-057 | sierra | **`widget` is a config shape with no build loop** — a declared target that does not build | open | 2026-08-05 | [CLAUDE.md](CLAUDE.md) § Packages |
| FJS-058 | sierra | **Undeclared dependencies** in `package.json` | stale? | 2026-07-31 | [drift-report.md:186](drift-report.md#L186) |
| FJS-059 | jetty | **`src/resources/` is a diverged hand-copy of Sierra's**, and jetty hardcodes Feathers-style event names | stale? | 2026-07-31 | [drift-report.md:309](drift-report.md#L309) · see [FJS-D16](#fjs-d16) |
| FJS-060 | mesa | **`externalSignals` is hand-maintained** — Sierra tells Mesa which imports are reactive, by hand. Slated for retirement, no replacement designed | open | 2026-08-05 | [CLAUDE.md](CLAUDE.md) § Bridge index |
| FJS-061 | cli | **`/api/env` ReferenceError** · **duplicate `dev` alias** | stale? | 2026-07-31 | [drift-report.md:292](drift-report.md#L292) |
| FJS-062 | conduit | **Doc/version skew** — conduit is a version behind its consumers' docs | stale? | 2026-07-31 | [drift-report.md:263](drift-report.md#L263) |
| FJS-063 | auth | **Login is a user-enumeration timing oracle** · **`plugin.ts` types `app` and `ctx` as `any`** | open | 2026-08-02 | [auth/PROJECT_STATE.md:222](packages/auth/PROJECT_STATE.md#L222) |
| FJS-064 | litestone | **The unscoped `litestone` npm name is blocked** by npm's similarity check — support ticket filed, not chased. `@frontierjs/litestone` is publishable now | open | 2026-08-04 | [litestone/PROJECT_STATE.md:212](packages/litestone/PROJECT_STATE.md#L212) |
| FJS-065 | cli | **`admin:generate` emits `.svelte`**, wrong paths, lowercase-plural model names. Repair or retire — it advertises a feature that does not run | stale? | 2026-08-02 | [IDEAS/ecosystem-gaps.md](IDEAS/ecosystem-gaps.md) · wave 0.4 |
| FJS-090 | caravan | **A job's name is a string stated up to three times and nothing reconciles them.** `defineJob('send-email', fn)` names it, every `dispatch('send-email', data)` names it again, and `autoload.ts` scans `**/*.job.{ts,js}` while **using only the default export's name — the filename is never checked against it**, so `send-email.job.ts` may define `send-emial` and the only symptom is a dispatch that silently never runs. The payload is `unknown` on both sides, so a shape change is caught by nobody. Laravel's job-as-class collapses all three statements into one declaration and — the part worth stealing — lets an existing synchronous effect move to the queue by declaration (`implements ShouldQueue`) without touching a call site, which is the move FJS makes everywhere else and did not make here | open | 2026-08-06 | [caravan/src/autoload.ts:17](packages/caravan/src/autoload.ts#L17) |
| FJS-091 | junction/cli | **There is no way to ask what the HTTP surface is.** `routePaths(method)` exists ([router.ts:201](packages/junction/src/transport/router.ts#L201)) and `fli` has no command that calls it — `commands/api/` has no route listing, and the two `route.md` files are `make:route` and `web:route`, both scaffolds. The surface is emergent: services auto-mount, plugins register their own, and `hasRoute()` is a *matching* question rather than an existence one (`CLAUDE.md` § Live hazards), so "is this mounted" has no cheap answer. **[FJS-012](#) is what that costs** — plugin routes skip `apiPrefix`, so `@frontierjs/auth` defaults to `/auth` while the browser client hardcodes `/api/auth/login`, and with default config on both sides they never meet. A route list would have shown that in one line. Laravel's real win here is not `route:list`, it is that `routes/web.php` makes the surface *a file you can read*; the cheap FJS version is `fli api:routes` over the function that already exists | open | 2026-08-06 | [CLAUDE.md](CLAUDE.md) § Live hazards |
| FJS-112 | repo | **Performance has no owner in the register and no regression gate.** The 2026-07-18 litestone audit ends with a *still open* list — H8 transform set-based SQL, H9 attach-based sharding + persistent worker pool, JSONL cold-load streaming early-exit, S3 streaming/multipart, and a low-impact tail (read-path clone reduction, policy SQL precompilation, tokenizer charCode dispatch, seeder batching) — and **none of it has an id**, so by this file's own rule none of it is open. Worse, the only harness had not been run in three weeks and had a broken case (`gate-getlevel` used a plural accessor and died, silently skipping the measurement that proves the GatePlugin cache still works). Re-verified and repaired 2026-08-06; `bun run bench` now exists. What is still missing is a **gate**: there is no CI ([FJS-009](#)), no committed baseline numbers, and absolute µs are not comparable across machines — the only method that showed anything was interleaving the same bench against the pre-change tree on one box. `mesa/mesa-bench/` cannot even be installed ([FJS-026](#)) | open | 2026-08-06 | [litestone docs/PERFORMANCE_AUDIT.md](packages/litestone/docs/PERFORMANCE_AUDIT.md) § Re-verified |
| FJS-083 | sierra | **URL search params are not reactive and not on `page`.** `PAGE_RESERVED` is `path params meta route pending data error slots` — no `query` — and `.query` appears nowhere in `router/index.js`. `setParams`/`updateParams`/`goto(path, queryParams)` can WRITE them; reading them back means calling `parseQueryParams(window.location.search)` imperatively, so filters and pagination cannot be URL-driven without hand-wiring. Sierra has an unusually clean shot here that it has not taken: `ctx.directives` is already `{limit, offset, orderBy, select}` and Invariant 10 already rules `$` transport-only, so "the search params ARE `ctx.query` + `ctx.directives`" shrinks the mental model rather than adding to it | open | 2026-08-06 | [IDEAS/client-data-lifecycle.md](IDEAS/client-data-lifecycle.md) |
| FJS-084 | junction | **The browser client cannot ask for a relation.** `FindParams` is `{ query, offset, limit, orderBy, select }` ([client/index.ts:48](packages/junction/src/client/index.ts#L48)) — no `populate`. The wire and the server both support it: the bridge parses `$populate` ([bridge.ts:70](packages/junction/src/transport/bridge.ts#L70)) and `service.ts:799` destructures `include`. So a component cannot declare its own data shape and over/under-fetching is decided server-side in a hook, while Litestone underneath has the property that makes field-level selection safe — batched includes, one `IN` per relation level, no N+1 anywhere | open | 2026-08-06 | [litestone PERFORMANCE_AUDIT.md:206](packages/litestone/docs/PERFORMANCE_AUDIT.md#L206) |

---

## S4 — low

| Id | Pkg | Title | Status | Verified | Detail |
| --- | --- | --- | --- | --- | --- |
| FJS-066 | cli | `sourceURL` pragma works on Node, ignored on Bun · frontmatter regex breaks on a stray `---` in a body · `mod.prose` left over from the segments refactor · `utils:qrcode` needs a manual `npm install qrcode` | open | 2026-08-02 | [cli/PROJECT_STATE.md:369](packages/cli/PROJECT_STATE.md#L369) |
| FJS-067 | mesa | `tick()` not implemented · `{#virtual each}` produces no SSR output (by design, wants a `{:static}` fallback) · variable-height `{#virtual each}` deferred · full hydration SSR deferred to v1.1 · TypeScript deferred | open | 2026-08-02 | [mesa/PROJECT_STATE.md:282](packages/mesa/PROJECT_STATE.md#L282) |
| FJS-068 | mesa | `renderComponent` resolves bare imports from Mesa's own package root (SSR_SPEC W1) | open | 2026-08-02 | [mesa/docs/SSR_SPEC.md](packages/mesa/docs/SSR_SPEC.md) |
| FJS-069 | mesa | The White Paper's version number in the file header is not updated — do it before the next publish | open | 2026-08-02 | [mesa/PROJECT_STATE.md:282](packages/mesa/PROJECT_STATE.md#L282) |
| FJS-070 | repo | **`drift-report.md` line numbers reflect the 2026-07-31 tree** — verify before citing any of them | open | 2026-08-05 | [CLAUDE.md](CLAUDE.md) § Live hazards |
| FJS-077 | ui | **Controls that resolve nothing from the form context.** Nine read it (`Field`, `Input`, `Textarea`, `Select`, `NumberInput`, `Combobox`, `MultiSelect`, `FileUpload`, `Button`); `Checkbox`, `Switch`, `RadioGroup`, `Slider` and `DatePicker` do not, so inside a `<Form>` they neither show their own server error nor take `required` from the schema. One `const form = $context.form` each. **Second half found 2026-08-06 and fixed for `Select`/`Textarea` only:** a control that computes `nameToLabel(name)` and passes it to `<Field>` as an explicit label SHADOWS the schema's `@label`, so `@label("Customer")` rendered as "Customer Id" and the annotation was unreachable through the kit. Every remaining control that wraps `Field` needs the same `label={label \|\| undefined}` change | open | 2026-08-06 | [ui/CHANGES.md](packages/ui/CHANGES.md) 2026-08-06 |
| FJS-078 | ui | **`<Select>` does not derive its options from the schema.** `fields.status.enum` is right there and every enum select in the repo maps it by hand. Blocked behind nothing; the same is true of `RadioGroup` | open | 2026-08-06 | [ui/CHANGES.md](packages/ui/CHANGES.md) 2026-08-06 |
| FJS-079 | ui | **A `DateTime` column gets no control.** `Input` deliberately refuses to map `format: date-time` to `datetime-local` — Litestone stores a zone and that input neither accepts nor emits one, so the round trip shifts the time silently. `DatePicker` exists (1200 lines) and is unverified ([FJS-028](#)), so the schema-derived path for dates is a hole, not a choice | open | 2026-08-06 | [ui/components/forms/Input.mesa](packages/ui/components/forms/Input.mesa) |

---

## Contested — the diagnosis is doubted

| Id | Pkg | Title | Status | Verified | Detail |
| --- | --- | --- | --- | --- | --- |
| FJS-071 | litestone | **"Audit logger writes 0 rows."** `CLAUDE.md` records the opposite: the logger buffers ~1s and flushes on exit, so a read straight after a write returns 0 rows and the `.jsonl` may not exist yet — **not a bug**. Litestone's own examples read too early and report 0. Separately real: `path` resolves relative to process CWD, not the schema file. Settle which it is, then close or rewrite | contested | 2026-08-04 | [IDEAS/framework-shape.md](IDEAS/framework-shape.md) 4 (wave 0.3) vs [CLAUDE.md](CLAUDE.md) § Live hazards |
| FJS-072 | notifications | **"Empty email bodies" / "accessor ×3" / "broken sierra example."** The package's own state file records the drivers-never-called-`build()` bug as **fixed**, and `HANDOFF.md` notes the diagnosis "does not match the tree — the package has no `src/`". Re-verify before acting | contested | 2026-08-02 | [drift-report.md:277](drift-report.md#L277) vs [notifications/PROJECT_STATE.md:72](packages/notifications/PROJECT_STATE.md#L72) |

---

## Needs a decision

These are questions, not defects. A ruling moves to `DECISIONS.md` and closes the
row here.

| Id | Area | Question | Detail |
| --- | --- | --- | --- |
| <a id="fjs-d25"></a>FJS-D25 | css | **Ship `Lead` and `Kicker` as terms?** Both are concepts the package already names in `tokens.css` and then provides no way to write. `--text-lg` is commented "**lead paragraph**" and `--text-2xs` is "**the uppercase overline**" — the rungs are tuned for these two jobs and neither has a class, so every consumer hand-rolls one. **Lead**: the standfirst under a page title — larger than body, muted, held to a measure. The guide's `.sg-lead` is 18px + `--ink-soft` + 60ch, and two of those three are now Prose's, so the term may be as small as the size. **Kicker**: the short uppercase label ABOVE a title ("Structure", "Learn") — the thing publishing calls a kicker, distinct from a Badge (a status, inline) and from `.divider-label` (a break between things, not a label on one). Evidence it needs a name: the guide drifted **two** of them with no shared owner — `.sg-eyebrow` at 12px/500/accent and `.sg-wiz-eyebrow` at 11px/600/`--ink-mute` — and `.navlist-label` is a third instance of the same shape already shipped but scoped to nav. Three implementations, one idea, no name is the condition that produces exactly that drift. Open questions: does Kicker carry a tone (the guide's is accent-coloured, the wizard's is muted); is Lead just `.text-xl` + Prose's ink, in which case it is a utility composition and not a term; and does a Kicker + title + Lead group want naming as page-header anatomy, or is it a Section with three children (leaning the latter — naming it adds a term that composes entirely from terms). Not to be confused with `.sg-h1`, which is a display treatment (44px/500/serif vs the h1 rung's 36px/700) and is legitimately the guide's own | [packages/css/guide/guide.js](packages/css/guide/guide.js) § pageHeader() · [packages/css/src/foundation/tokens.css](packages/css/src/foundation/tokens.css) § type scale · [packages/css/vocabulary.js](packages/css/vocabulary.js) |
| <a id="fjs-d24"></a>FJS-D24 | css | **Is there a term for a horizontal group that does not wrap — and does Bar mean "a strip" or "a section"?** Two halves of one gap, found putting the guide's topbar onto shipped terms. **First half:** `.cluster` is the horizontal group, and wrapping is its *definition* (`layout.css` § "cluster — horizontal group that wraps"), not a default. Inside anything with a fixed height that is wrong — a Topbar has `--topbar-height`, so a wrapped second line is drawn OUTSIDE the bar rather than growing it. Measured at 640px: the guide's version badge landed at y=46 in a 56px bar starting at y=8, on top of the page below. Nothing in the vocabulary says *row, together, one line*: Split forces `space-between`, Toolbar promises arrow-key movement the app must implement, and Bar is the right geometry with the wrong scale. Candidate is a `.cluster.nowrap` modifier — one line, `NOT_A_TERM` as a modifier, and it generalises to every fixed-height container. **Second half:** the guide now writes `bar start` for the two groups inside its Topbar, which works and reads oddly — Bar is Region-tier and means *a horizontal strip*, so two of them inside a Topbar says there are three strips where there is one. The missing concept may be a Bar-shaped thing at a smaller scale (a *group* within a strip) rather than a Cluster modifier; if that is the answer it subsumes the first half. Deliberately unruled — the guide's use is marked in place | [packages/css/guide/guide.js](packages/css/guide/guide.js) § topbar() · [packages/css/src/foundation/layout.css](packages/css/src/foundation/layout.css) · [packages/css/src/patterns/bars.css](packages/css/src/patterns/bars.css) |
| <a id="fjs-d01"></a>FJS-D01 | junction | **The structural refactor: go or no go?** Definition/compiled Service split → export tiering → middleware/hook naming. Proposed and sequenced, awaiting the word. Fixes [FJS-016](#), [FJS-017](#), [FJS-046](#) | [drift-report.md:42](drift-report.md#L42) |
| FJS-D02 | junction | **Custom service methods are called `actions`** — the name is under review. Dispatch via `X-Service-Method` is already decided | [DECISIONS.md](DECISIONS.md) |
| FJS-D03 | repo | **Rule the hook context shape.** Junction's split (`auth`/`client`/`route`/`locals` + `query`/`directives`) is the candidate standard; other realms conform as subsets. Needs a `DECISIONS.md` entry more than it needs code | [IDEAS/one-mental-model.md](IDEAS/one-mental-model.md) 3 |
| <a id="fjs-d04"></a>FJS-D04 | litestone | **How does a subscriber attach to `onEvent` after construction?** It is fixed at `createClient` and there is no post-construction subscribe, unlike `$tapQuery(fn)`. Mirroring `$tapQuery`'s shape is the obvious move. Unblocks [FJS-010](#) | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) |
| FJS-D05 | litestone | **Multi-tenancy config API.** db-per-tenant only today; the config shape is undesigned | [CLAUDE.md](CLAUDE.md) § Open questions |
| FJS-D06 | repo | **The coherence-review vocabulary proposals** — Hook/Guard/Observer/Delegate split, Provider, Job, Target, Component/Binding, Slice axis, Manifest→Release. Argued, not adopted into `ARCHITECT.md`. **Three neighbouring questions were ruled 2026-08-06** and are no longer part of this row: Signal vs Event (both legal, the old ban was aimed at the wrong sense), Policy (two meanings, a third refused), Projection (adopted for a read model only). The list above is untouched by those | [drift-report.md:42](drift-report.md#L42) |
| <a id="fjs-d07"></a>FJS-D07 | junction | **How does a service declare a narrower method set?** `methods: ['find','get']` allow-list vs `readOnly: true`. The allow-list generalises — it also covers "no bulk delete" without a hook. Fixes [FJS-004](#) | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) |
| <a id="fjs-d08"></a>FJS-D08 | junction | **Is a custom method's return shape a contract?** Should `createService` warn when a method on a model service returns an object with no id? Fixes [FJS-020](#) | [junction/PROJECT_STATE.md:77](packages/junction/PROJECT_STATE.md#L77) |
| FJS-D09 | litestone | **Migrations, second tier** — rollback stance (`--backup`?), row-count assertion after rebuilds, second-granular timestamp collisions | [DECISIONS.md:421](DECISIONS.md#L421) |
| FJS-D10 | junction | **The deferred API cluster** — `IAuth` partial acceptance (`auth: { verifySession }` sufficing), `publish()` string shorthand / `write` phase alias, typed `createSchema` inference, `createClient` options grouping + `setters`/`getters` hook naming. Deferred 2026-08-01 pending [FJS-D06](#) | [DECISIONS.md:421](DECISIONS.md#L421) |
| FJS-D11 | junction | **Partial success for bulk PATCH and REMOVE** — the `errors[]` ruling covers creates; extend or scope it deliberately | [DECISIONS.md:421](DECISIONS.md#L421) |
| FJS-D12 | ui | **i18n must be decided before schema→UI ships** — a generated form generates labels, and every string written before the decision is a string to find again | [IDEAS/ecosystem-gaps.md](IDEAS/ecosystem-gaps.md) 4 |
| FJS-D13 | junction | **Does a stream sit outside the result envelope, or does the envelope grow a third `kind`?** SSE exists in the transport ([http.ts:680](packages/junction/src/transport/http.ts#L680)); what has no ruling is how a streamed response relates to `wrapResult`/`unwrapResult` | [IDEAS/ecosystem-gaps.md](IDEAS/ecosystem-gaps.md) 12 |
| FJS-D14 | repo | **`packages/orion/` — name it or remove it.** It contains a README and nothing else, and no other document references it | `packages/orion/README.md` |
| <a id="fjs-d15"></a>FJS-D15 | email-kit | ~~**`@frontierjs/mesa-email` or `@frontierjs/email-kit`?**~~ **Ruled 2026-08-06: `@frontierjs/email-kit`** — the directory was already right and every other package's name matches its own. Fixed [FJS-051](#). `DECISIONS.md` § Naming | [DECISIONS.md](DECISIONS.md) |
| <a id="fjs-d16"></a>FJS-D16 | repo | **Four duplications, one decision each: close or accept?** `auth/install.md`'s schema copy ([FJS-038](#)) · jetty's copy of sierra's `resources/` ([FJS-059](#)) · the HMR algorithm copied into sierra ×2 and jetty · `externalSignals` ([FJS-060](#)) | [CLAUDE.md](CLAUDE.md) § Open questions |
| FJS-D17 | sierra | **The UI plugin system is limited** — JSON Schema → UI drives `make()` only. What else should a UI plugin be able to contribute? | [CLAUDE.md](CLAUDE.md) § Open questions |
| FJS-D18 | mesa | **`$: { (a, b) }` vs `$: (a, b)`** — block form is an auto-tracked effect, sequence form is a multi-path proxy watch. Semantically different, not called out in the spec or the §4.7 table | [mesa/PROJECT_STATE.md:282](packages/mesa/PROJECT_STATE.md#L282) (White Paper §4.2/§4.4) |
| FJS-D19 | litestone | **Litestone `Plugin` has no `name`** — the missing identity blocks introspection, ordering and `/metrics`. Rename the concept while ruling it | [IDEAS/one-mental-model.md](IDEAS/one-mental-model.md) 1 |
| <a id="fjs-d23"></a>FJS-D23 | repo | **How does a payload declare a field that is NOT a column?** Ruled 2026-08-08 as far as practice goes — capture it in a `before` hook, because `autoValidate` deletes every undeclared key and user hooks run first ([DECISIONS.md](DECISIONS.md) § API design). What is NOT ruled is whether the framework should know about it. Today a wire-only field is a convention held by a hook and a comment: nothing distinguishes it from a typo, nothing documents it, and the symptom of forgetting the hook is the service reporting the field as *missing* on a request that carried it. Candidates: a service-level `accepts: { secret: 'string' }` that `autoValidate` spares and the bridge parks on `ctx.locals`; a Litestone `@transient` the schema declares and the DDL skips; or leaving it a hook concern and settling for a documented pattern. **This is [FJS-D22](#fjs-d22) from the other side** — that one asks how a column says *the system writes this*, this one asks how a payload says *this is not a column* — and they want one answer, not two. Surfaced building basecamp's `channels`, where the field is a plaintext credential on its way into a `Secret` | [packages/basecamp/api/src/services/channels/channels.service.ts](packages/basecamp/api/src/services/channels/channels.service.ts) · [DECISIONS.md](DECISIONS.md) |
| <a id="fjs-d22"></a>FJS-D22 | repo | **How does a column say "the system writes this, not the user"?** Candidates: a Litestone annotation emitting JSON Schema `readOnly` for stored columns (the mechanism already exists for computed/generated ones), field-level `@allow('write', …)`, or leaving it a UI concern. Whichever wins, Sierra's `make()` and `@frontierjs/ui`'s `<Form>` have to honour it — today neither reads `readOnly` at all. **See [FJS-D23](#fjs-d23)**, the same seam from the other side. Fixes [FJS-095](#) | [example/web/src/routes/orders/create.mesa](example/web/src/routes/orders/create.mesa) |
| <a id="fjs-d21"></a>FJS-D21 | junction | **Does a custom action announce?** ~~Open~~ **Ruled 2026-08-06: yes, under its own name** (`orders pay`); reads opt out with `ctx.dispatch = false`. Fixed [FJS-033](#). `DECISIONS.md` § API design | [DECISIONS.md](DECISIONS.md) |
| FJS-D20 | auth | **The developer-facing auth API.** `/auth/*` intentionally bypasses the Service abstraction (login cannot be gated by login) — what the surface *should* be is unsettled | [CLAUDE.md](CLAUDE.md) § Open questions |

---

## Closed

Kept so a stale document cannot reopen them. Newest first.

| Id | Title | Closed | How |
| --- | --- | --- | --- |
| FJS-138 | css | **The Surface lineage had no notion of a link — `<a class="card">` rendered underlined and did nothing on hover.** Both halves fixed 2026-08-09. (1) `src/foundation/reset.css`, one rule in a new first layer: `a:where([class])` loses the UA underline while a bare `<a>` in prose keeps it, because colour alone is not an accessible link affordance and a wider selector would strip it silently. (2) `surface.css` gives `:where(a, button):where(.surface, .card, .tile)` a cursor, a 1px lift and a tone-following border. It keys on the ELEMENT rather than a `.hover` modifier — the opt-in pattern elsewhere (`.rows.hover`, `.items.menu`) exists because an `<li>` or `<tr>` is not focusable and an unconditional hover would advertise an interaction the markup cannot deliver, and an `<a href>` has no such problem. So the affordance cannot be attached to a static card or forgotten on a link. **Three near-misses, each of which rendered plausibly and is now pinned by a test**: moving `--surface-bg` to `--surface-raised` did nothing in the default theme (the two tokens are both `#ffffff`) and erased `.outlined`, whose whole variant is a transparent background; writing the border as `var(--surface-tint-border, <brand>)` fired the fallback only when the tint was invalid — never on a toned card — so `.card.danger` hovered to exactly the red it already had; and without `(hover: hover)` a tap left the card lifted, reading as a stuck selection. The border is now one `color-mix` toward `--bg-mix`, so untoned goes brand-blue and danger goes a deeper red. Alert, Toast, Popover, Dialog and Drawer are deliberately excluded: a notification is not a navigation target. Found by asking what in the guide's next-page footer could not be replaced by a shipped term; that footer now declares nothing. 7 tests in `core-gaps.spec.js`, all four mutation-verified | **closed** | 2026-08-09 | [css/src/foundation/surface.css](packages/css/src/foundation/surface.css) · [css/src/foundation/reset.css](packages/css/src/foundation/reset.css) |
| FJS-134 | auth — **an API key could be issued and could never authenticate anything** | 2026-08-09 | `createApiKey()` worked, stored an HMAC, returned a token — and every request carrying that token was anonymous. Junction's HTTP transport resolves a Bearer token through **`auth.verifySession()` and calls `verifyApiKey` nowhere** (`transport/http.ts:385`); the better-auth provider knows this and falls through, the native one did not. So the whole API-key half of `IAuth` was reachable only by calling it yourself, which nothing does. The failure is silent in the worst direction: minting looks like it worked, and the key fails as *unauthenticated* rather than as *not a key*, so it reads as a bad token. `verifySession` now routes on the `fjs_` prefix — the format is this package's own, so a machine request costs no wasted session lookup — and falls back to `verifyApiKey` for any token that missed, which covers a key issued before the prefix existed. Found building basecamp's `/api-keys/`. Verified in a real browser against a running app: an issued key returns 200 from `GET /servers`. 3 tests |
| FJS-135 | auth — **the scopes a key was issued with were stored and then dropped, so every key had its owner's full standing** | 2026-08-09 | `createApiKey(userId, { scopes })` writes them space-joined into `Credential.scope`, and `verifyApiKey` returned `toContext(user, 'apiKey')` — built from the USER row, which has no idea a key exists. `SessionContext.scopes` was therefore always `undefined`: a key issued `servers:read` authenticated with exactly the permissions of the person who made it, and nothing downstream could tell the difference between that and an unrestricted key. A scope checkbox that changes nothing is worse than no scopes at all, because it reads as a control. The session now carries `scopes` (absent, not `[]`, when a key was issued without any — absence means *no opinion*, the same rule `verifiedAt` follows) and a new `credentialId`, without which an app cannot say WHICH of a user's keys made a call: two keys on one user produce sessions that are otherwise identical, so per-key usage and per-key revocation have nothing to key on. 4 tests |
| FJS-136 | auth — **`revokeApiKey` silently revoked nothing on any app whose `Credential.id` is not an `Int`** | 2026-08-09 | It ran `delete({ where: { id: Number(keyId) } })`, because this package's own `schema.ts` fragment declares `Credential.id Int @id`. But the fragments are a starting point apps edit — basecamp declares `String @default(uuid())`, like every other id in that schema — and `Number('a1b2-…')` is `NaN`, which matched no row **and threw nothing** (verified: `delete where id=NaN → no throw`). Revoke returned success and the key kept working, indefinitely, which is the worst outcome available to this function. The coercion is gone — Litestone coerces a where-value to the column type either way, verified against both shapes — the delete is filtered on `type: 'apiKey'` as well as the id so a wrong id cannot take somebody's password with it, and a revoke that matches nothing now throws instead of lying. One behaviour change came with it: **verifying is quiet, issuing is loud.** `verifyApiKey` used to throw `AuthConfigError` with no `encryptionKey`; it answers `null` now, because it runs on attacker-supplied input on every request and `verifySession` falls through to it — a 500 anyone could trigger with a Bearer header. `createApiKey` still throws, which is where a developer is. 4 tests |
| FJS-133 | css — **the demo's pagination was unstyled, because it wrote a class the package refused to ship** | 2026-08-09 | Every control carried `class="page"` and the gap `class="page-gap"`. Neither is shipped, and `nav.css` documents by name why not: *"the control is a link, not a page. Shortening the class to `.page` puts one word on two subjects"* — Page is also a vocabulary TIER, and Previous/Next are neither a page nor numbered. So the demo rendered raw UA links: measured `rgb(0,0,238)`, `padding: 0`, `border-radius: 0`, no fill on the current page. The demo is the package's flagship consumer and its README calls the markup *"the structural half of the system, followed strictly"*. Third instance in three days of the same hole — markup that no spec owns can name anything at all (`FJS-130` the wizard, `FJS-131` the demo's Feeds). Closed by `test/specs/demo.spec.js`, two checks: every class in `index.html` is shipped or is demo-owned (`demo-` prefix), and every class-carried vocabulary term appears at least once. `run.js` now inlines the demo markup — and it must be `escapeForInlineScript`'d even though `JSON.stringify` quoted it, because the demo is an HTML document carrying its own `</script>` tags, which end the inline block whatever the JavaScript around them means. That cost one debugging round: the spec read `undefined` and reported all 54 terms absent. Proven to bite: reverting the rename turns it red. The kit's `Pagination.mesa` was already correct; the one other hit in the repo is a stale `example/web/dist/` artefact predating that fix |
| FJS-131 | css — **the demo's two Feeds rendered with no timeline, the same defect as `FJS-130`** | 2026-08-09 | Both wrote `<li class="feed-item">`, collapsing the two elements ANATOMY keeps apart. The connecting line is `.feed > li:not(:last-child) .feed-item::after` — a DESCENDANT selector — so the collapsed form matches nothing: measured `content: none`, zero height, a column of dots with no line between them. Found by covering the whole vocabulary in the demo, days after the identical bug was fixed in the guide's wizard, which is the argument for the check in `FJS-130` being data rather than review: three separate authors wrote this same shape and no test in the package could see any of them, because every check asks whether a class is PRESENT. The `<article>` inside the `<li>` is load-bearing and now says so in a comment at both sites |
| FJS-130 | css — **the Learn wizard taught markup that does not render, for four terms** | 2026-08-09 | The wizard's sample is the end of every path and the only markup in the guide no reference page owns. Four of the 52 omitted parts their own term is built from: **Feed** wrote `<ol class="feed"><li><article>` with no `.feed-item`, `.feed-dot` or `.feed-content` — a plain list, no dot column and no connecting line, which is exactly the distinction the term exists to make; **Steps** wrote no `.step-marker`, and the numbered disc AND the connector between discs are both drawn off it; **Disclosure** and **Empty** wrote bare elements where class-styled parts belong. Invisible to every existing check: `anatomy.spec.js` renders ANATOMY's own block, which is correct, and `decisions.spec.js` asked only whether the classes a sample DID write are shipped — a class you never write is never wrong. Found while fixing the Feed blurb, which described a "chronological stream" and never mentioned the timeline the CSS draws (VOCAB, the wizard option and its lead all now say so). Closed by `decisions: a wizard sample writes the parts its term cannot render without`, which resolves a borrowed part's optionality **through the term that owns it** — `uses` is a bare class list and the `'optional'` flag lives on the owner, so reading it as required reported all three Surface sub-regions against Card, Dialog, Drawer and Popover, four terms that are right. That distinction is the design: a wizard sample is the smallest thing that renders, not the exhaustive reference. 19 reported → 7 real. Proven to bite: breaking both `.feed-dot`s turns it red |
| FJS-126 | ui — **`DropdownItem` carried a control reset `@frontierjs/css` ships, and had drifted the row** | 2026-08-08 | The local `<style>` is deleted; `lists.css`'s `.items :is(button, a).item` owns the reset and `.item` owns the layout. Four of the eight local declarations restated `.item`, and one of those — `gap: 0.625rem` — disagreed with the `var(--space-sm)` every other Item reads, so a menu row's icon sat a rung further from its label than an identical row outside a menu **and did not move inside a `.dense` region**, which the literal made impossible rather than merely wrong. `DropdownMenu` renders `<div class="items menu">`, so the descendant selector matches and the disabled pair keeps its `.items` scoping, which is what lets it outrank the `(0,3,0)` hover. Verified in a real browser: `example`: `verify:ui` 27/27 including `menu.items` and `menu.escapeCloses`, plus `verify` 37/37 |
| FJS-127 | ui — **`Drawer` styled `.drawer[open]` locally, in the kit, for the package's own term** | 2026-08-08 | Four lines making the drawer a flex column so a `.surface-body` between a header and a footer takes the remaining height. It is now in `drawers.css`, the file that owns `.drawer`. The `[open]` scoping is the whole subtlety and it survives the move with a comment: an author `display: flex` beats the UA's `display: none`, so declared on `.drawer` the drawer renders on screen while shut — the exact trap `overlay: a closed <dialog> stays closed` already records. Pinned by a new test that asserts both halves, and proven to fail: dropping `[open]` turns two tests red |
| FJS-104 | basecamp — **no workspace-wide read for `ServerEvent`** | 2026-08-08 | Server events were reachable only through `servers.events`, per server, so any screen wanting the fleet's stream needed one request per server merged in the browser — and `/activity/` therefore covered `AuditEvent` alone, a record of what PEOPLE did, while the mock's feed is mostly what the machines did. `servers.feed` is the collection-level read: the server ids in this workspace, then the events on them, two indexed queries instead of N+1 over the network. `ServerEvent` still carries no `workspaceId` of its own — an event is meaningless without its server, and denormalising the tenancy fact onto it would be a second owner — so the scope is the join. The server's NAME is resolved server-side rather than shipping the whole row per event. `/activity/` now merges both sources by time, because an operator reading "what happened" does not care which table it came from, and a drain followed by the agent confirming it only reads as one story if they sit together. Reaching it needed `FJS-122` first: there is no subject row, so it is `POST /servers` with the header
| FJS-085 | repo — **no suite probed a production build, so a blank page shipped green** | 2026-08-08 | Basecamp's `web/index.html` mentioned the body tag inside a comment; Vite injects the built `<script>`/`<link>` at the first textual match without skipping comments, so both landed *inside* the comment and `dist/` loaded no JS and no CSS — white screen, empty console, `bun run build` exit 0. Fixed in basecamp 2026-08-06 and in `example/` on 08-04: twice, found twice by eye. `example` gained `verify:build` (the same 37 assertions against the built output); **basecamp had nothing**, which is where the trap actually landed. It now has `bun run verify:build` — `web/test/preview.mjs` serving `dist/client` with the API proxied (basecamp mounts services at the ROOT, so the proxy matches a route list rather than an `/api` prefix), and `verify-build.mjs` checking two layers. The file: comments STRIPPED FIRST, then require a `<script>` and a stylesheet `<link>` to survive — a regex over the raw file passes on exactly the broken output, since the tags are there and inert. The page: loaded in a real browser, required to render something, to have fetched its own JS and CSS, and to log no errors — a file can be well-formed and still throw on first execution. Deliberately not all 150 assertions against the build: those need an empty database and a full setup run, and the unasked question was only *does the built page come up at all*
| FJS-118 | litestone — **raw SQL always ran on the READONLY connection, so the documented escape hatch could not write** | 2026-08-08 | `_runRawSql` sent every statement to `readDb`, which is opened `readonly` with `query_only = ON`. Every raw write through every surface failed with `SQLITE_READONLY: attempt to write a readonly database` — a message about a connection, naming nothing the caller wrote. It hit `db.sql`, `db.$setAuth(u).sql`, `db.asSystem().sql` and **the system client a JS migration is handed**, which is the caller most likely to need a raw `ALTER`/`UPDATE` and least able to route around it. On a schema declaring access rules `asSystem().sql` is the ONLY way to run a raw statement, so raw writes were unreachable outright. Statements now route by kind: `SELECT`/`EXPLAIN`/`VALUES` stay on the reader, everything else goes to the writer, which reads perfectly well. `WITH` counts as "everything else" on purpose — `WITH x AS (…) DELETE FROM …` is legal SQLite, so a CTE cannot be assumed to be a read; leading comments are stripped before the test so `-- why\nDELETE …` is not misread. Found in `basecamp`, hard-deleting a row to prove an FK cascade fires — the one thing `.remove()` cannot do on a `@@softDelete` model. 3 tests; 1458 pass |
| FJS-120 | litestone — **a JSON Schema `default` was emitted as a value of the WRONG TYPE, and the boundary then refused its own default** | 2026-08-08 | An array or `Json` field spells its default as a STRING in the `.lite` source, because that is how it is stored: `tags String[] @default("[]")`. `generateJsonSchema` passed it through verbatim, producing `{"type":"array","default":"[]"}` — a schema whose own default fails its own type check. Junction's `autoValidate` fills the default in and then validates it, so **every create that OMITTED the field 400'd with `tags must be an array`, naming a field the caller never sent**; sending `tags: []` explicitly worked, which is the opposite of what a default is for. Structured defaults are now parsed before emission, keyed on the FIELD's type rather than on the literal looking like JSON — a `String` column defaulting to the two characters `[]` still means those two characters — and a literal that does not parse is dropped rather than emitted wrong. Found declaring basecamp's `FeatureFlag`. 4 tests |
| FJS-121 | conduit — **any non-JSON response was a `server_error`, so a Slack webhook could never succeed** | 2026-08-08 | The HTTP transport refused every content type that was not JSON. The check was written for one real case — a 200 carrying HTML is a captive portal, a proxy interstitial or a provider error page — but spelled as "not JSON", which is a far wider net: **a Slack incoming webhook answers `200 text/plain: ok`**, so `app.conduit.send()` reported a failure for a notification that had already been delivered. Now only markup fails (`text/html`, `application/xhtml+xml`); anything else non-JSON comes back as the text it is. Found wiring basecamp's notification channels through the outbound boundary, where the test delivery arrived at the sink and was reported as an error. 1 test; 193 pass |
| FJS-122 | junction · sierra — **a collection-level custom action was unreachable from the browser** | 2026-08-08 | The bridge dispatches on the `X-Service-Method` header BEFORE it looks at `params.id`, so the server has always accepted an action about a whole service rather than one row. Both client layers interpolated the id unconditionally — `${base}/${id}` — so the only way to reach one was to invent a throwaway id and post to `/{service}/null`. `action(name, id?, data?, query?)` now posts to the service root when `id` is null or omitted, and carries a plain filter query (`?limit=50`, not the `$`-prefixed directive syntax `buildQueryString` emits — an action declares its own query vocabulary). Found writing basecamp's fleet-wide event feed and its flag `resolve`, neither of which has a subject row. 4 junction tests; 884 junction, 809 sierra, 5/5 `test:safety` |
| FJS-110 | mesa — **two components separated only by whitespace shared one compiled anchor** | 2026-08-07 | A pending label request is satisfied by the next text node, and the compiler's `tpl` keeps those as separate entries while the emitted template is one **string** — where adjacent text parses as a single DOM `Text` node. So `<Button>Attach</Button>` and `<Button>Cancel</Button>` on consecutive lines inside an `{#if}` both resolved their anchor to the one whitespace node between and after them. **The DOM was correct** — each component inserts before the same node, in source order, so it rendered, laid out and clicked exactly as written — but `_componentRegistry` is keyed BY ANCHOR, so the second `registerComponentAnchor` replaced the first and the first component **never received another prop push for the life of the page**. That is why a plain `<button>` two lines up, carrying the identical expression, kept following it: an attribute binding writes the node directly and needs no registry. Fixed by giving every component invocation an anchor comment of its own instead of letting it adopt the text beside it — which also made several templates *smaller*, since a component no longer needs a filler comment plus a separate label. 8 mesa tests over the shapes (`{#if}`, `{:else}`, `{#each}`, inside an element, at component root, adjacent with and without whitespace); 4 of them fail if reverted, checked. `basecamp`'s `verify` now asserts the button follows the picker (135), `example` 37 dev + 37 built + 27 ui + 21 public. **The lesson is the eight probes that missed it**: every one put a static element between the two components, which is exactly the thing that made the anchors distinct. A reproduction that "simplifies" the layout can delete the bug |
| FJS-117 | litestone — **`$checkWhere` existed only on the ROOT client, so every list read by a signed-in caller 500'd** | 2026-08-06 | `FJS-109` added `db.$checkWhere` inline in the top-level proxy's `get` trap. The three derived clients — `$setAuth`, `asSystem`, `$scopedBy` — never got it, and a Litestone proxy **throws** on an unknown property rather than answering `undefined`, deliberately, so a typo'd accessor is loud. Junction hands a service `$setAuth`'s client on `ctx.locals.db`, so `autoFilter`'s capability probe `typeof client?.$checkWhere !== 'function'` was itself the throwing expression — and it sat *outside* the try/catch guarding the call. Every list read in **both** apps died with `"$checkWhere" is not a table in this schema`, naming a table nobody had written. Which keys are filterable is a question about the SCHEMA; auth and scope have no bearing on the answer, so it is now defined **once** in `createClient` scope and handed to all four proxies, which give identical answers (pinned per flavour). Junction's probe now reads inside a `try` as well — belt and braces, and independently correct: `db` is app-supplied and a client that cannot answer must no-op, not 500. **The lesson is what it looked like**: the browser symptom was navigation, not data — basecamp's setup and login redirects never committed and `example/`'s `<Select>` options never populated, because the page each landed on did a list read that threw. It was blamed on a concurrent edit to `mesa/src/runtime.js` and recorded in HANDOFF as such; that was wrong. mesa's 1027 and sierra's 809 unit tests were green throughout, as were litestone's and junction's — **no suite reached the seam, because every test client was the root one**. `example`: 37/37, `basecamp`: 134/134, `sierra`: `test:safety` 5/5 |
| FJS-116 | ui — **`<Textarea>` silently ignored `oninput`, while `<Input>` and `<Select>` both honoured it** | 2026-08-06 | Textarea declared `onchange`/`onblur`/`onfocus` and kept its own `handleInput` for the internal value and autogrow, so a caller writing `oninput={…}` got no handler and no warning — an undeclared prop is just ignored. Found filing a TLS certificate through `<Form>` in basecamp: the two PEM fields stayed empty and the service answered `certPem and keyPem are both required`, with nothing on either side saying why. `handleInput` now calls `oninput?.(e)` after the internal write and the autogrow, so a handler that re-renders cannot cancel either. 64/64 compile, 25/25 render, 7/7 form |
| FJS-109 | junction — **an unknown query key silently filtered to nothing** | 2026-08-06 | `?bogusColumn=7`, `?limit=100` (a directive in the filter slot) and a genuinely empty table all answered `200 {"data":[],"total":0}`. **Litestone knew the whole time** — it validates where-keys, rejects them on writes, and on reads prints *Unknown field 'bogusColumn' … Valid fields: id, name* to the SERVER'S stderr, invisible to whoever typed it. That warn-on-read / throw-on-write split is right for the ORM (a typo'd filter on a write is a mis-scoped destructive operation) and wrong at a boundary that can answer 400. So the boundary asks: litestone gained `db.$checkWhere(accessor, where)` — the same rule, the same Levenshtein typo hint, the same AND/OR/NOT descent, no warning and no query — and junction's `autoFilter` derived hook turns a non-empty answer into a `BadRequest` naming **every** bad key, its suggestion, the filterable fields, and the `$limit`/`$offset`/`$orderBy`/`$select` directives it was probably meant to be. One definition, junction contributing only the status — same division as `gateAuth`. A client without `$checkWhere` no-ops: *I cannot judge this* is not *this is wrong*. 8 junction tests over real HTTP + 7 litestone. Two existing tests asserted on the LENGTH of a compiled `before` list and broke on the second derived hook; both now filter the exported `DERIVED_HOOKS`, which is what their own comments already said they wanted |
| FJS-114 | mesa — **`{@attach}` ran on a DETACHED element, so every overlay in the kit was invisible** | 2026-08-06 | VISION §10.6 says the attachment runs when the element *mounts*; it ran when the element was BUILT, before anything inserts it, so every attachment saw `isConnected === false` and `parentNode === null`. `focus()` is a no-op there and a rect is all zeros — but the killer is `el.animate([{opacity:0},{opacity:1}], { fill: 'forwards' })`, which on a disconnected node returns an animation with `startTime: null` that **never starts even after the element is connected**, and is not listed in `getAnimations()`. The element paints at keyframe 0 for good. `@frontierjs/ui`'s CommandPalette was therefore a `position: fixed; inset: 0; z-index: 9000` backdrop that was **completely invisible and swallowed every click on the page** — reported as "clicking Search ⌘K does nothing and the whole app freezes". Toast, Popover, DropdownMenu, ConfirmationPopover and AlertProvider use the same pattern. Fixed by deferring the attachment to a microtask — the queue `$onMount` already uses — when the element is not yet connected; an already-connected element still runs synchronously, so nothing correct is reordered. 3 mesa tests + `example`'s `verify:ui` now opens the palette by CLICKING the header button (only ⌘K was ever driven) and asserts computed opacity, hit-testing and size, not presence. Both fail if reverted, checked. **The lesson is the assertions**: 26/26 passed against an invisible overlay for as long as it existed, because every check asked whether the DOM was there |
| FJS-111 | basecamp — **the schema's own default was a value the API refused** | 2026-08-06 | `AlertRule.severity` was `String @default("medium")`; the alerts service accepted only `info \| warning \| critical`. Two owners of one vocabulary, disagreeing — and unnoticed for as long as it was, because `AlertRule` had no service at all, so nothing could reach the model to try it. Found the moment the screen submitted a form without touching the severity control. Fixed by making the schema the seed: `enum AlertSeverity { info warning critical }`, `@default(warning)`, migration regenerated, and the service's hand-written list **deleted** — the column now carries a CHECK, `autoValidate` refuses a bad value before the method runs, and the UI's `<Select>` reads `alerts.fields.severity.enum`. One declaration, three consumers. The verify check leaves severity untouched on purpose, so the default has to arrive from the schema for it to pass |
| FJS-014 | litestone — **`Object.keys()` on a client threw, and `asSystem()` answered wrongly** | 2026-08-06 | Two strings. `$setAuth` and `$db` were on the proxy target AND in the `ownKeys` trap's hand-written list, and a duplicate makes the *engine* throw — so `Object.keys`, `getOwnPropertyNames`, `{...db}`, `for…in` and **`JSON.stringify(db)`** all failed on the top-level client and on a transaction proxy, with a message about proxy internals rather than either name. All five traps now go through one `dedupeKeys()` rather than having the two names deleted: the literal lists have grown before. **Probing turned up the quieter half** — `asSystem()`'s proxy had a `get` trap and nothing else, so it did not throw, it lied: `'user' in db.asSystem()` was `false` while `db.asSystem().user` worked, and it enumerated no tables at all, so a guard reading `if ('user' in db)` silently skipped the table under a system client. It now carries the same three traps. Junction's `try/catch` around `Object.keys(scopedDb)` stays — `db` is app-supplied and a stand-in must not turn *your model name is wrong* into a stack trace — but its comment no longer describes a live bug, and the list it produces was itself wrong the moment it started working: it offered `asSystem`, `sql` and `query` as model names. It now filters against `$schema.models`. 6 litestone tests + 2 junction, the latter against a REAL client — every existing test in that file used a plain object, which is exactly why the list went unverified |
| FJS-108 | sierra — **a prerendered page linked no stylesheet and carried no body class** | 2026-08-06 | `wrapDocument` emitted the page's own scoped `<style>` blocks and nothing else, because a prerendered document is assembled by Sierra rather than by Vite's HTML transform — so a `target: 'static'` page shipped every `@frontierjs/css` class name in the app and not one rule behind them, and no theme, while the SPA built from the same source looked right. The build already emits the stylesheet; nothing linked it. `wrapDocument` now takes `stylesheets` (the CSS assets of that build, linked BEFORE the page's own scoped rules, which are the more specific statement) and `bodyClass` from a new `document: { bodyClass, lang }` config key. Read in `writeBundle`, not `generateBundle` — Vite's CSS plugin emits the asset in its own `generateBundle`, which runs after Sierra's, so a hook earlier saw an empty asset list and linked nothing silently. 5 sierra tests, and `example/`'s `verify:public` asserts the link, the class and a theme token resolving in a real browser |
| FJS-107 | mesa — **every prerendered `<input>` carried `formaction="http://localhost/"`** | 2026-08-06 | happy-dom's `cloneNode` re-derives an input's attributes from its default PROPERTIES, so each clone of a template gained `formaction` (the build machine's own URL) and `formmethod=""`, and an authored relative `formaction="/search"` came back absolutised. Not cosmetic: `formaction` overrides its form's action, so a prerendered form posted to whatever machine built the site, and that machine's localhost URL shipped in a public file. `template()` now parses per instance on the server instead of cloning a cached parse — the same path that produced the original, so what ships is what was written; the client still clones. 3 tests, all three fail if reverted |
| FJS-106 | mesa — **a `.mesa` route with frontmatter was compiled as MARKDOWN** | 2026-08-06 | `compileSource` routed any source beginning with `---` to the Markdown compiler whatever its extension, and a `---` block is how every Sierra route states its title and render mode. Markdown escapes what it does not recognise, so `<CatalogList client:load products={data?.products ?? []} />` came out as a paragraph of ESCAPED TEXT with the props stringified into it — while `<LiveStock />` beside it, having no attributes, compiled as a component. It only bit callers handing the compiler a raw file: Sierra's Vite path strips frontmatter first, so dev and the SPA build were right and the PRERENDERER was wrong, for the same file. The extension decides the language now (VISION always said `.md` is the Markdown one), and `compile()` strips a leading block, exposes it as `ctx.frontmatter`, and replaces it with blank lines so warnings keep their line numbers. Mesa's three Markdown REPL examples are named `.md`. 6 tests |
| FJS-105 | sierra — **`@version` reached the browser and stopped there** | 2026-08-06 | `createResource` now keeps the version of every record it reads — from `get`, `find`, `load()`, `create` and each patch response — and puts it on the next patch. Kept per record rather than read off `store`, because a form usually loads one record with `get()`, which does not populate the list store at all; `load()` needed its own call, since it bypasses `_call` and a list whose rows are unpatchable is the same bug. A caller-supplied version still wins, and with nothing remembered the patch goes up without one so the server refuses — better than inventing a number that would silently win a race. Also `resource.version(id)` and `.versionField`. **The conflict needed one thing junction was not sending**: a 409 alone cannot separate a race (`VersionConflictError`, `TransitionConflictError` — re-read and retry) from a domain refusal (`TransitionViolationError` — whose own message is the right thing to show), so `retryable` is now adopted in `toFrameworkError` and serialized by `FrameworkError.toJSON`. `isStaleWrite()` reads it and `toFieldErrors` returns a sentence instead of a column name and two integers. Verified end to end through a real litestone update and a real HTTP round-trip: 409 retryable → the sentence, 409 non-retryable → its own message, 400 → the required-version explanation, success → version 1 → 2. 16 sierra tests + 3 junction |
| FJS-102 | mesa — **`prop=""` on a component compiled to `prop={true}`** | 2026-08-06 | `inspectProp` decided the valueless-boolean form (`<Table striped>` → `striped={true}`) by testing the value for falsiness, and an explicit empty string is falsy. So the one spelling every `@frontierjs/ui` component documents for *suppress this* produced its opposite. The parser had never lost the distinction — valueless is `{ value: undefined }`, `prop=""` is `{ value: '' }` — so the fix is `value === undefined`. Found by wiring `<Select placeholder="">` into basecamp's workspace switcher: the placeholder rendered as an `<option>` whose visible text was the word **`true`**, which basecamp's browser suite caught as an unexpected third workspace. Silent by construction — a valid prop of the wrong type, a clean compile, a component that rendered. Element attributes take a different path and were never affected. 2 tests; mesa 1015, sierra 789, ui 64/25/7 all green |
| FJS-092 | litestone — **the bulk write paths skipped every `ctx.auth` stamp** | 2026-08-06 | Wider than filed. `upsertMany` stamped nothing — not `@default(auth().id)`, not `@createdBy`, not `@updatedBy` — and probing the whole table turned up a second hole: **`updateMany` never stamped `@updatedBy` either**, so `$setAuth(ann).updateMany(…)` left the column naming whoever last wrote through `update()`. That one was the worse of the pair: `@updatedAt` is a SQL trigger ([ddl.js:379](packages/litestone/src/core/ddl.js#L379)) so the timestamp kept moving, and a row read *just edited by Bob* when Ann edited it — a wrong name, not a missing one. Both paths now run the stamps, and in `upsertMany` a create-time column is held out of the `ON CONFLICT … SET` list so a conflict cannot rewrite the author — but only when *we* filled it, since a caller who names the column meant it. The four inline copies of the stamp collapsed to two functions that state the distinction: `stampFromAuth` (principal wins) and `applyAuthDefaults` (payload wins). 8 tests; **4 fail if the stamps are removed** |
| FJS-101 | A notification's email body could only be a list of lines | 2026-08-06 | `MailMessage` was `{ subject, lines, to }` and the driver rendered `lines` and nothing else — so `@frontierjs/notifications` and `@frontierjs/email-kit`, in the same repo, could not be used together at all. A receipt with a table of items was unexpressible. `mail()` gains `.html()` / `.text()`, and the driver prefers a rendered body **per field**, so a template supplies the HTML while the builder still writes the plain-text alternative. 5 tests |
| FJS-102 | A `.mesa` import by package specifier could not be resolved by the SSR renderer | 2026-08-06 | `import Email from '@frontierjs/email-kit/components/Email.mesa'` — the usage that package's own README documents — was `path.resolve`d against the importing file and produced `…/api/emails/@frontierjs/email-kit/components/Email.mesa`. It worked inside the kit only because its own templates import `../components/…` relatively; the first consumer outside it hit the wall. Two halves: mesa's `compileTree` now resolves a bare specifier with `import.meta.resolve`, and the kit's `exports` map (`"./components/*" → "./components/*.mesa"`, which turned `Email.mesa` into `Email.mesa.mesa`) gained the extension-carrying pattern. Same family as [FJS-100](#) — correct from within, broken from without |
| FJS-103 | A template's `subject` export could not depend on the render data | 2026-08-06 | `<script module>` runs once at import, before props exist, so `export const subject` could only be a constant — and a receipt's subject names the order. Module exports already came back as values, so a function export needed one change to work: the email document wrapper called `subject.replace()` to build its `<title>` and threw `subject.replace is not a function` out of the renderer. It now titles the document only when the export is a string and hands the value back as exported |
| FJS-051 | The email kit's package name did not match its directory | 2026-08-06 | Ruled [FJS-D15](#fjs-d15): it is `@frontierjs/email-kit`. `package.json` already said so; the old name survived in prose, comments, a user-facing error string and one filename, all swept. `drift-report.md` keeps it — a dated audit's findings are not rewritten |
| FJS-100 | Every test in `@frontierjs/email-kit` had been failing, and the package's own state file said "34 tests green" | 2026-08-06 | Two causes, found by sweeping the rename. (1) The kit probes for mesa's `render-component.js` at `../mesa/render-component.js` — the pre-`src/` layout — so both sibling probes missed, and its not-found test read only Node's phrasing while Vite says `Failed to load url … Does the file exist?`, turning a missed candidate into a rethrow before the candidate that works. (2) The real one, in mesa: `findMesaDir()` tested `dirname(import.meta.url)` for a `package.json`, and that file moved into `src/`, which has none — so temp render modules landed in the OS temp dir, where `import '@frontierjs/mesa/runtime.js'` cannot resolve. It now walks up to the nearest `package.json`. **Guarded 2026-08-06**: mesa's suite could not see this — every test in it runs from the mesa package root, which is the one cwd where the broken lookup worked — so `test/render-component.test.js` gained a case that renders in a CHILD PROCESS with its cwd outside the repo. Reverting the fix fails it with `Cannot find package '@frontierjs/mesa' imported from /tmp/__mesa_render_…`. The last-resort branch also warns now instead of silently writing somewhere nothing resolves. email-kit 34/34, mesa **1013**, sierra 789 |
| FJS-097 | A Junction session reached the Data boundary with no `id`, so every row policy matched nothing | 2026-08-06 | Junction's `SessionContext` names the caller `userId`; Litestone's policy language reads `auth().id` — its documented spelling. `$setAuth(ctx.auth.user)` therefore handed the Data boundary a principal with no id, and `@@allow('read', userId == auth().id)` compared a column to `undefined`: an empty list, no error. Gates were unaffected, because `sessionGateLevel()` was written against Junction's shape — so the half that was translated worked and the half that was not failed silently. Fixed with `toDataPrincipal()`, the one owner of that translation, applied at both `$setAuth` call sites; 9 tests. Found by `example/`, where a signed-in user's own notifications were invisible to them and plainly there under `asSystem()` |
| FJS-098 | The audit log could not record a String actor id | 2026-08-06 | Litestone's synthetic audit model declared `actorId Int`, and the jsonl driver's index is a STRICT table — so the first audited write with a known actor threw `cannot store TEXT value in INTEGER column auditLogs_idx.actorId` and took the request with it. Every FrontierJS app is exposed: `@frontierjs/auth` issues `id String @id @default(uuid())`. **It was masked by [FJS-097](#)** — with no `id` on the principal, `actorId` was always null, and NULL fits an INTEGER column; fixing one uncovered the other in the same afternoon. `actorId` is now `Any` (a real SQLite STRICT type), and an index whose columns no longer match the schema is dropped **and refilled from the `.jsonl`**, so history stays queryable rather than looking truncated. 3 tests |
| FJS-099 | `methods:` was silently ignored by `createBaseService` | 2026-08-06 | The allow-list added for [FJS-004](#) was read by `createService` and neither read nor forwarded by the other factory — the one the loader is built around (`export function createPostsService() { return createBaseService({}) }`). So the same `methods: 'readOnly'` that makes an audit trail append-only through one factory did nothing at all through the other, and the only symptom was a write that succeeded. Carried through now, resolved in the one place that validates it; 4 tests. Found declaring `methods: ['find','get','patch']` on `example`'s notifications service and getting a create that reached the model's gate |
| FJS-033 | Live updates were untested against a second client — and a custom action announced nothing at all | 2026-08-06 | Tested, and it found the gap. A watcher tab (signed out, never acts) sees `orders created` and `orders removed` cross the socket and apply with no reload, so channels DO reach a stranger — but the `pay` between them was never published: `callService` gated announcements on `AUTO_EVENT_MAP`, five CRUD methods, and an action is in none of them. The browser client had listened for action events since it was written. Fixed in junction (`?? (isAction ? method : undefined)`, 6 tests), ruled as [FJS-D21](#); the example's orders table dropped its `find()` refetch — that refetch was the mask. Pinned by `bun run verify:live`, 12 assertions, of which 4 fail if the one line is reverted |
| FJS-086 | `verify-ui.mjs` reported a rate-limited sign-in as a timeout | 2026-08-06 | `verify.mjs`'s `signIn()` copied across: wait for the badge **or** `.alert.danger`, then read the alert and name the 429. Proved by injecting a `.alert.danger` reading `sign-in failed: HTTP 429` before the click — the drive printed the limiter explanation and exited 1 instead of `waitFor timed out`. 26/26 still pass with the injection removed. The limiter is an in-process per-IP `Map` in `auth/plugin.ts`, so an API restart resets the window, which is what the message now says |
| FJS-005 | `$setAuth(user).sql` was byte-identical to unscoped `sql` — raw SQL bypassed every access rule | 2026-08-06 | Raw SQL is now available through `asSystem()` only, on any schema declaring `@@gate`/`@@allow`/`@guarded`/`@encrypted`/`@scoped`. Measured before: `$setAuth({id:1})` gave 1 row via the ORM and 3 via `.sql`, with a `@guarded` column in plaintext — and the UNSCOPED client was wider still (ORM 0 rows vs `.sql` 3), which is why the rule covers `db.sql` too and overturns the idea file's "unchanged". Coarse per schema rather than per statement on purpose: a hand-written SQL validator that is subtly wrong grants a false guarantee, and `bun:sqlite` exposes no authorizer (verified). Three byte-identical copies of `sql` collapsed to one runner. 9 tests, 5 fail if reverted. Ruled in `DECISIONS.md` § Access control. **Scoped raw SQL (the view set in `IDEAS/scoped-sql.md`) is deliberately NOT built** — revisit with `herald` |
| FJS-002 | `cookieAuth: true` could not authenticate a request | 2026-08-06 | Junction's `extractToken()` read only `authorization: Bearer` and `x-api-key`, so the cookie the auth plugin set was never read back and the documented mode gave you a session you could not use. `extractToken` now takes an optional cookie name and @frontierjs/auth calls `app.http.setAuthCookie('session')` from its own `register()`, so cookie mode is ONE switch — requiring the app to also write `config.auth.cookie` would have left a half-configured state reproducing the original bug. Cookies stay **off by default** as a CSRF decision (a Bearer token cannot be forged cross-site, a cookie travels automatically; `SameSite=Lax` is what makes it safe when on), and an explicit credential always beats the ambient one. Covers the WebSocket upgrade too — otherwise a cookie-authed app connected its socket anonymously, silently. Marker test flipped from 401 to 200; 12 junction tests + 5 auth tests, 7 fail if reverted |
| FJS-006 | `@encrypted` on a `Json` field silently destroyed the value | 2026-08-06 | `encryptField` does `String(plaintext)`, so an object became `'[object Object]'` and what got stored was a faithful ciphertext of that literal — unrecoverable, nothing thrown. A Json field is now serialized before encryption and parsed after decryption, mirroring the pipeline either side (write encrypts then serializes; read parses then decrypts), keyed on the DECLARED type captured in `buildFieldPolicyMap`. Objects, nested structures, arrays and JSON scalars all round-trip; the column stays ciphertext and no plaintext reaches the file. `@secret`, `$rotateKey` and `searchable` verified too. 9 tests, 4 fail if reverted. The `CLAUDE.md` hazard telling people to use `String @encrypted` instead is removed |
| FJS-081 | A `render: static` route could publish gated data as a public HTML file | 2026-08-06 | The prerenderer now collects each route's read set and refuses to emit a page whose data outranks what the route declares. Read set comes from litestone's `$tapQuery` around `load()` — **not** the render, which is where `IDEAS/static-safety.md` proposed watching and where nothing is read (static data arrives from the `.meta.js` companion as a prop, so a render-watcher would have observed an empty set and passed everything). Fail-closed: a route that pulls data the build cannot observe is refused, and the only escape is a per-route `publishes: N` written in frontmatter. 39 tests + a 5-check bun script against a REAL Litestone client (`bun run test:safety`); both halves fail when the check is removed. Exercised in `example/` — `bun run build:public` prints `/catalog/ 0 Product(0)` and exits 1 with the route, model and fix named when pointed at a gated model |
| FJS-004 | A model service had no way to say "read only" — an admin could forge a row into an append-only audit trail | 2026-08-06 | `methods: ['find','get']` \| `'readOnly'`, enforced in `callService` so an in-process caller is refused exactly as the wire is. CRUD and actions share the list; unknown names throw at construction; `/manifest`, `/metrics` and OpenAPI filter by the same predicate. 21 tests, 7 fail if the check is removed. Basecamp's `/audit` dropped four hand-written stubs for one line and was re-probed live: `405 {"message":"Service 'audit' does not offer 'create' (allowed: find, get)"}`. Ruled as [FJS-D07](#fjs-d07) |
| FJS-D07 | **Decision:** how does a service declare a narrower method set? | 2026-08-06 | One key, two forms — `methods: [...]` allow-list, `methods: 'readOnly'` sugar on the same key. The allow-list generalises to "no patch, no remove, one action", which a boolean cannot say. See `DECISIONS.md` § API design (Junction) |
| FJS-074 | `updateMany` / `deleteMany` wrote no audit entry at all on an `@@log` model | 2026-08-05 | All five uninstrumented paths log (`updateMany`, `deleteMany`, `removeMany`, `restore`, `upsertMany`); bulk ops take `RETURNING` on a logged model so the entry names its rows, which also fixed `createMany`'s empty `records`. 8 tests, `docs/audit-logging.md` corrected |
| FJS-076 | `<Form>`'s state machine had never run in a browser | 2026-08-06 | `example/web/src/routes/orders/create.mesa` rewritten onto it — ~90 lines of hand-rolled state machine deleted. `bun run verify` is **37/37 in dev and in the production build**, including all six live-validation assertions, which now pass against the rule as `<Form>` implements it rather than a per-app copy |
| FJS-080 | `bun run verify` could only be run once per database | 2026-08-06 | `seed()` guards per table and restores the three seeded orders by reference, resetting a status the drive moved. Demonstrated: a restart against a database with **zero** orders brought all three back in their expected states, and the drive then passed three times consecutively |
| FJS-075 | An unpicked relation picker silently selected the first row | 2026-08-06 | The placeholder `<option>` was `disabled`, and a disabled option cannot hold the selection — so a select whose options arrive late (every relation picker) lost the placeholder when the list repopulated. An unpicked "customer" became customer #1 and the order was filed against them. `example`'s `form.customerStartsEmpty` had been failing since before 2026-08-05 |
| FJS-073 | `<mesa:boundary>` watched every async value in the component | 2026-08-05 | The watch set is the async values the body reads; the whole-component union is kept when the body reads none (that is how you gate a region on everything) or renders a snippet defined elsewhere. 9 tests |
| FJS-C01 | `css` is not a package | 2026-08-03 | v0.10.0 — real package, tested, with a demo app (was ledger 23) |
| FJS-C02 | Sierra's `static` target is not wired to the renderer | 2026-08-02 | `prerender.js` composes route + layout chain through `renderComponent` |
| FJS-C03 | A handler ran once per ancestor delegation root (Mesa double-dispatch) | 2026-08-03 | Nearest delegation root owns an event — now Invariant 11 |
| FJS-C04 | A click inside `<mesa:portal>` never reached its handler | 2026-08-05 | Portals register their target, reference-counted |
| FJS-C05 | All four `@frontierjs/ui` stores were inert | 2026-08-05 | Writes go through the `watchProxy`, not a plain object |
| FJS-C06 | Sierra's `mesa-plugin` ignored `analysis.errors` | 2026-08-05 | The transform throws; ruled in `DECISIONS.md` |
| FJS-C07 | Gate resolver graded every session `VISITOR(1)` | 2026-08-04 | Absent ≠ null; standing checked before lifecycle |
| FJS-C08 | The result envelope · one event origin · the gate seam · internal pagination · `publish` reserved | 2026-08-02 | Ledger 3, 6, 11, 13 — all ruled in `DECISIONS.md` |
| FJS-C09 | `packages/basecamp` invisible to the workspace glob | 2026-08-04 | Has a `package.json`, installs |
| FJS-C10 | `example/`'s production build loaded no JavaScript | 2026-08-04 | Vite injects at the first body-tag match without skipping comments |

---

## See also

- `CLAUDE.md` § Live hazards — traps that are **correct behaviour**; knowing them saves a day. Not issues.
- `DECISIONS.md` — settled rulings. A `FJS-D##` closing here lands there.
- `IDEAS/` — roadmap and design records for work not started. `IDEAS/overview.md`'s wave 0 is the subset of this file that happens to be argued in an ideas file.
- `HANDOFF.md` — per-session narrative, newest first. History, not a ledger.
- `VERIFYING.md` — how to promote a `stale?` row to `open` or `closed`.
