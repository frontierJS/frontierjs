<script module>
  // web/src/resources/CustomField.mesa — what this shop records about a person.
  //
  // Named for the model, exporting the accessor. An ordinary resource over an
  // ordinary model: the fact that its ROWS decide what another model's form
  // offers is the settings screen's business, not this file's.
  import { createResource } from '@frontierjs/sierra/junction'

  export const customFields = createResource('customFields')
</script>
