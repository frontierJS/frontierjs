# Jobs snapshot

<!-- source: src/app.ts -->
<!-- generated by: junction jobs --app src/app.ts --services src/services -->

Generated by `junction jobs --app src/app.ts --services src/services`. **Do not edit.**

What this app runs when nobody asked. Read off a built app, because a job
file registers itself by being autoloaded and a plugin registers timers of its
own — neither is visible in any one source file.

A schedule that stops being registered is *nothing happening*, which is
indistinguishable from nothing needing to happen. That is what this file is
for: the change becomes a diff.

Live state is deliberately absent — no next run, no queue depth, nothing
paused. Those move between two boots of identical code and could not be
committed. This is what the app **declared**.

## Durable work — `app.jobs`

A row in the queue database, a retry budget, and the principal that asked for
it re-resolved when it runs. Survives a restart.

| Job | Queue | Schedule | Zone | Attempts | Retry delay | Timeout |
| --- | --- | --- | --- | --- | --- | --- |
| `announce-payment` | `fulfilment` | on dispatch | — | 5 | 30000, 120000, 600000 | **none** |
| `book-courier` | `fulfilment` | on dispatch | — | 5 | 60000, 300000, 1800000 | 30000ms |
| `calculate-payslip` | `default` | on dispatch | — | 3 | default | **none** |
| `collect-invoice` | `default` | on dispatch | — | 4 | default | **none** |
| `dun-subscriptions` | `default` | `0 6 * * *` | — | 3 | default | **none** |
| `release-holds` | `default` | `*/5 * * * *` | — | 3 | default | **none** |
| `renew-subscription` | `default` | on dispatch | — | 3 | default | **none** |
| `renew-subscriptions` | `default` | `0 * * * *` | — | 3 | default | **none** |
| `retention` | `default` | `0 4 * * *` | — | 3 | default | **none** |
| `send-payslip` | `default` | on dispatch | — | 5 | default | **none** |
| `sweep-abandoned` | `default` | `0 3 * * *` | — | 3 | default | **none** |

11 handler(s), 5 of them on a clock.

**10 with no timeout.** Absent means no bound, honestly — but a
handler that never settles holds its slot for the life of the process, and on a
single-concurrency queue everything behind it waits (`FJS-295`). Worth knowing
which ones those are, which is why the column is here rather than omitted when
empty.

## In-process timers — `app.scheduler`

No persistence, no retry, no principal; gone with the process, and running in
**every** replica rather than once across them (`FJS-D36`). A timer that
dispatches into the queue belongs on the list above instead — registered with
`app.jobs.schedule()`, which buys the queue's durability rather than looking
like it has it.

None registered.
