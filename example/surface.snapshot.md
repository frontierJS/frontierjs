# API surface snapshot

<!-- generated by: junction surface --app api/src/app.ts --services api/src/services -->

Generated from `api/src/app.ts` by `junction surface`. **Do not edit.**

What the API answers, read off a BUILT app rather than scanned: the methods
each service will serve, its custom methods, the hook chain in the order it runs,
and every path the router actually mounted. None of it is visible in the source —
an option key and a method look identical, `apiPrefix` moves every route, and
a plugin mounts paths nobody wrote. Regenerate after a change and read the diff.

```
20 services · 32 routes · 10 plugins · prefix /api
```

## App hooks

Run around EVERY service call, machine-facing endpoints included. `all` is not
a method — it applies to each one.

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `withTenantDb` → `anonymous` |

## Services

`methods` is policy-applied — what the service will answer, not what it
defines. A custom method is a non-CRUD one `collectCustomMethods` resolved at
construction; a name that stopped being one is a line that disappears here.
`model` is what the service reports for the result envelope, which is its own
name when it declares none.

### `account` · model `account`

- **methods** — `get`, `changePassword`
- **custom methods** — `changePassword`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `api-keys` · model `api-keys`

- **methods** — `find`, `create`, `remove`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `carts` · model `Cart`

- **methods** — `get`, `open`, `addLine`, `setQuantity`, `removeLine`, `applyDiscount`, `setShipping`, `removeDiscount`, `handoff`, `redeem`, `checkout`
- **custom methods** — `open`, `addLine`, `setQuantity`, `removeLine`, `applyDiscount`, `setShipping`, `removeDiscount`, `handoff`, `redeem`, `checkout`
- **input** — `applyDiscount` takes `DiscountCode`
- **input** — `checkout` takes `CheckoutDetails`
- **input** — `setShipping` takes `ShippingChoice`
- **broadcasts on** — `carts`
- **transactional** — `addLine`, `setQuantity`, `removeLine`, `checkout`, `redeem`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` → `transactionScope` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |
| before | `applyDiscount` | `validateInput` |
| before | `setShipping` | `validateInput` |
| before | `checkout` | `validateInput` |

### `colours` · model `Colour`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **broadcasts on** — `colours`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `connections` · model `connections`

- **methods** — `find`, `remove`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `customers` · model `customers`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **broadcasts on** — `customers`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `discounts` · model `Discount`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **broadcasts on** — `discounts`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `inventory` · model `InventoryMovement`

- **methods** — `find`, `get`, `levels`, `receive`, `adjust`
- **custom methods** — `levels`, `receive`, `adjust`
- **input** — `adjust` takes `StockAdjustment`
- **input** — `receive` takes `StockReceipt`
- **transactional** — `receive`, `adjust`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` → `transactionScope` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |
| before | `receive` | `validateInput` |
| before | `adjust` | `validateInput` |

### `notifications` · model `notifications`

- **methods** — `find`, `get`, `patch`
- **broadcasts on** — `notifications`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `anonymous` → `autoFilter` → `autoSort` |
| before | `patch` | `anonymous` → `autoValidate` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `update` | `autoValidate` |

### `orderLines` · model `OrderLine`

- **methods** — `find`, `get`
- **also answers to** — `order-lines`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `orders` · model `orders`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `pay`, `ship`, `refund`, `cancel`, `paymentCode`, `recordTracking`
- **custom methods** — `pay`, `ship`, `refund`, `cancel`, `paymentCode`, `recordTracking`
- **input** — `recordTracking` takes `TrackingUpdate`
- **broadcasts on** — `orders`
- **transactional** — `pay`, `refund`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` → `transactionScope` |
| before | `create` | `checkOrderRules` → `autoValidate` |
| before | `update` | `checkOrderRules` → `autoValidate` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `patch` | `autoValidate` |
| before | `recordTracking` | `validateInput` |

### `paymentEvents` · model `PaymentEvent`

- **methods** — `find`, `get`
- **also answers to** — `payment-events`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `payments` · model `Payment`

- **methods** — `find`, `get`, `start`, `refund`, `record`
- **custom methods** — `start`, `refund`, `record`
- **broadcasts on** — `payments`
- **transactional** — `record`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` → `transactionScope` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `productImages` · model `ProductImage`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **also answers to** — `product-images`
- **broadcasts on** — `product-images`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `products` · model `products`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **broadcasts on** — `products`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `productVariants` · model `ProductVariant`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `availability`, `embed`
- **custom methods** — `availability`, `embed`
- **also answers to** — `product-variants`
- **broadcasts on** — `product-variants`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `sessions` · model `sessions`

- **methods** — `find`, `remove`, `revokeOthers`
- **custom methods** — `revokeOthers`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `shippingMethods` · model `ShippingMethod`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **also answers to** — `shipping-methods`
- **broadcasts on** — `shipping-methods`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `shopfront` · model `shopfront`

- **methods** — `settings`
- **custom methods** — `settings`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

### `taxRates` · model `TaxRate`

- **methods** — `find`, `get`, `create`, `update`, `patch`, `remove`, `restore`
- **also answers to** — `tax-rates`
- **broadcasts on** — `tax-rates`

| Phase | Method | Chain |
| --- | --- | --- |
| around | `all` | `gateAuth` |
| before | `find` | `autoFilter` → `autoSort` |
| before | `get` | `autoFilter` |
| before | `create` | `autoValidate` |
| before | `patch` | `autoValidate` |
| before | `update` | `autoValidate` |

## Routes

Read off the router, so a path appears here whether or not anything meant to
mount it. A `{service}` path is the CRUD handler and names every service at
once; everything else was registered by hand or by a plugin.

| Method | Path | Kind |
| --- | --- | --- |
| OPTIONS | `/*` | raw |
| DELETE | `/api/{service}` | service |
| GET | `/api/{service}` | service |
| PATCH | `/api/{service}` | service |
| POST | `/api/{service}` | service |
| PUT | `/api/{service}` | service |
| DELETE | `/api/{service}/{id}` | service |
| GET | `/api/{service}/{id}` | service |
| PATCH | `/api/{service}/{id}` | service |
| POST | `/api/{service}/{id}` | service |
| PUT | `/api/{service}/{id}` | service |
| GET | `/api/auth/email/verify` | raw |
| POST | `/api/auth/email/verify/request` | raw |
| POST | `/api/auth/login` | raw |
| POST | `/api/auth/logout` | raw |
| GET | `/api/auth/oauth` | raw |
| POST | `/api/auth/password-reset/confirm` | raw |
| POST | `/api/auth/password-reset/request` | raw |
| POST | `/api/auth/register` | raw |
| GET | `/api/channels/stats` | raw |
| GET | `/api/health` | raw |
| GET | `/api/jobs` | raw |
| GET | `/api/jobs/{id}` | raw |
| POST | `/api/jobs/{id}/cancel` | raw |
| POST | `/api/jobs/{id}/retry` | raw |
| POST | `/api/jobs/run/{name}` | raw |
| GET | `/api/jobs/schedules` | raw |
| GET | `/api/manifest` | raw |
| GET | `/api/metrics` | raw |
| GET | `/api/migrations` | raw |
| POST | `/api/webhooks/payments` | raw |
| POST | `/api/webhooks/stripe` | raw |

## Plugins

In configure order, which is what `requires:` is checked against.

1. `health`
2. `manifest`
3. `@frontierjs/auth`
4. `caravan`
5. `outbox`
6. `conduit`
7. `mailer`
8. `notifications`
9. `channels`
10. `corsPlugin`
