# JSON Schema snapshot

<!-- generated by: litestone jsonschema --snapshot --schema schema.lite -->

Generated from `schema.lite` by `litestone jsonschema --snapshot`. **Do not edit.**

The document three packages read: Junction validates requests against it,
Sierra re-checks the same rules in the browser, and `<Form>` renders a control
from it. Nothing breaks when a keyword stops being emitted — a form just stops
validating — so the diff is the only place that shows up.

Rendered from `mode: full`, with the create-mode differences called out per
model. Doc comments (`description`) are omitted: they are prose, they are long,
and no reader branches on them.

```
5 definitions · 4 models · 1 enums · 0 other
```

## Definitions

`$defs` is the whole table and travels whole. An enum field emits
`{"$ref": "#/$defs/Name"}` and both sides resolve it, so a name that
disappears from here is a reference that resolves to nothing in a browser.

| Name | Kind |
| --- | --- |
| `Product` | model |
| `Customer` | model |
| `Order` | model |
| `Notification` | model |
| `OrderStatus` | enum |

## Enums

A value removed here is a row already in the database that no longer
validates, and a select that silently drops an option.

- `OrderStatus` — `pending`, `paid`, `shipped`, `refunded`, `cancelled`

## Models

`Required` is the full-mode requirement. `Rules` are the keywords a validator
branches on plus any `x-` extension carried on the field; `Messages` are the
rule names `x-messages` answers for, which is what a failure is allowed to say.

### `Product`

- gate `read:0 create:4 update:4 delete:5` · closed (`additionalProperties: false`)

| Field | Type | Required | Label | Rules | Messages |
| --- | --- | --- | --- | --- | --- |
| `id` | `integer` | — | — | — | — |
| `name` | `string` | yes | — | `minLength: 1` `maxLength: 80` | — |
| `sku` | `string` | yes | — | `minLength: 3` `maxLength: 20` | — |
| `price` | `number` | yes | — | `minimum: 0` | — |
| `barcode` | `string`? | — | — | — | — |
| `active` | `boolean` = `true` | — | — | — | — |

**On create**: required — `name`, `sku`, `price` · not accepted — `id`

### `Customer`

- gate `read:0 create:4 update:4 delete:5` · closed (`additionalProperties: false`)
- relation `orders` — hasMany `Order`

| Field | Type | Required | Label | Rules | Messages |
| --- | --- | --- | --- | --- | --- |
| `id` | `integer` | — | — | — | — |
| `name` | `string` | yes | — | `minLength: 1` `maxLength: 80` | — |
| `email` | `string` | yes | — | `format: "email"` | — |
| `notes` | `string`? | — | — | `x-litestone-read-policy` | — |

**On create**: required — `name`, `email` · not accepted — `id`

### `Order`

- gate `read:0 create:4 update:4 delete:5` · closed (`additionalProperties: false`)
- relation `customer` — belongsTo `Customer` via `customerId` · on delete Cascade
- transitions on `status` — `pay`: pending → paid · `ship`: paid → shipped · `refund`: paid → refunded @5 · `cancel`: pending|paid → cancelled

| Field | Type | Required | Label | Rules | Messages |
| --- | --- | --- | --- | --- | --- |
| `id` | `integer` | — | — | — | — |
| `reference` | `string` | yes | — | `minLength: 3` `maxLength: 20` | `length` `maxLength` `minLength` |
| `status` | `OrderStatus` = `"pending"` | — | — | — | — |
| `total` | `number` = `0` | — | — | `minimum: 0` | — |
| `note` | `string`? | — | — | — | — |
| `customerId` | `integer` | yes | Customer | — | `required` |
| `trackingCode` | `string`? | — | Tracking | — | — |

**On create**: required — `reference`, `customerId` · not accepted — `id`

### `Notification`

- gate `read:0 create:8 update:4 delete:8` · closed (`additionalProperties: false`)

| Field | Type | Required | Label | Rules | Messages |
| --- | --- | --- | --- | --- | --- |
| `id` | `integer` | — | — | — | — |
| `userId` | `string` | yes | — | — | — |
| `type` | `string` | yes | — | — | — |
| `data` | `json` | yes | — | — | — |
| `contextType` | `string`? | — | — | — | — |
| `contextId` | `integer`? | — | — | — | — |
| `readAt` | `string`? | — | — | `format: "date-time"` | — |

**On create**: required — `userId`, `type`, `data` · not accepted — `id`
